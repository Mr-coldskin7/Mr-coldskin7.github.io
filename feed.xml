<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="zh"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://leesanyee.me/feed.xml" rel="self" type="application/atom+xml"/><link href="https://leesanyee.me/" rel="alternate" type="text/html" hreflang="zh"/><updated>2026-09-13T08:02:03+00:00</updated><id>https://leesanyee.me/feed.xml</id><title type="html">leesanyee</title><subtitle>LeeSanYee 的技术博客，记录学习笔记与思考。 </subtitle><entry><title type="html">自我改进 Agent 统一拆解：θ / Σ 双路线</title><link href="https://leesanyee.me/blog/2026/Self-Improving-Agents-%E7%BB%9F%E4%B8%80%E6%8B%86%E8%A7%A3/" rel="alternate" type="text/html" title="自我改进 Agent 统一拆解：θ / Σ 双路线"/><published>2026-09-13T07:57:00+00:00</published><updated>2026-09-13T07:57:00+00:00</updated><id>https://leesanyee.me/blog/2026/Self-Improving-Agents-%E7%BB%9F%E4%B8%80%E6%8B%86%E8%A7%A3</id><content type="html" xml:base="https://leesanyee.me/blog/2026/Self-Improving-Agents-%E7%BB%9F%E4%B8%80%E6%8B%86%E8%A7%A3/"><![CDATA[<h1 id="自我改进-agent-统一拆解θ--σ-双路线">自我改进 Agent 统一拆解：θ / Σ 双路线</h1> <blockquote> <p>基于三篇综述的整合：Schmidhuber 团队《Self-Improvements in Modern Agentic Systems》、厦门大学《Self-Evolving Agents: From Model-Centric to Environment-Driven Co-Evolution》，以及 STAIR（作为反例参照）。</p> </blockquote> <h2 id="总框架">总框架</h2> <p>自我改进 = 把学到的东西存下来。能存的地方只有两个：</p> <ul> <li><strong>θ（模型权重）</strong>：靠梯度更新写入。慢，但一劳永逸——之后每次生成自动生效。</li> <li><strong>Σ（模型外面的一切）</strong>：prompt、记忆库、工具集、控制代码。靠直接编辑写入，快、可读、可回滚。</li> </ul> <p>判断标准就一条：<strong>改动是否跨任务保留</strong>。推理时多采样几次、反思一下，这些行为随任务结束就消失，不算自我改进。</p> <p>所有方法都跑同一个三步循环：</p> <ol> <li><strong>产生信号</strong>：让模型干活，产出轨迹、答案、对比。</li> <li><strong>验证信号</strong>：判断哪些是好的——这是最关键的一步。</li> <li><strong>写入介质</strong>：把验证过的东西存进 θ 或 Σ。</li> </ol> <hr/> <h2 id="一改-θ-的路线">一、改 θ 的路线</h2> <h3 id="1-信号从哪来可靠性分四档">1. 信号从哪来：可靠性分四档</h3> <p><strong>(a) 自评（最弱）</strong></p> <p>同一题采样多次，多数投票，留下多数派答案对应的轨迹。问题在于：模型自信地错时，采样越多错得越整齐，投票反而把错误固定下来。它测的是「收敛程度」，不是「对错」。</p> <p><strong>(b) 可执行验证（可靠）</strong></p> <p>用程序判对错：单元测试 pass/fail、代码跑不跑得通、数学答案对不对。模型不用会判断好坏，执行器替它判。Absolute Zero 全靠这一条——Challenger 出题自带验证程序，Solver 对错由执行器裁决，所以能零人工数据训练。</p> <p><strong>(c) 自我对弈（解决冷启动）</strong></p> <p>把模型拆成两个角色：<strong>Challenger 出题，Solver 解题</strong>。Solver 解出来，Solver 得分；解不出来，Challenger 得分。效果是 Challenger 自动去出「刚好比 Solver 当前水平难一点」的题——难度自适应，不需要人排课程表。两个角色共享同一套权重，用 GRPO 更新。</p> <p><strong>(d) 轨迹级信号（信息最全，也最贵）</strong></p> <p>整条交互过程都是训练数据，监督分三种粒度：</p> <ul> <li><strong>结果监督</strong>：只对最终成败给分。简单，但 20 步的轨迹错了，不知道错在哪一步。</li> <li><strong>过程监督</strong>：每步打分。精细，但标注贵。</li> <li><strong>步骤对比</strong>：拿成功和失败轨迹对比，找到分歧步骤构造训练对。好处是失败轨迹不是垃圾，而是可以反向修评估器——<strong>策略和评估器一起进化</strong>。</li> </ul> <h3 id="2-目标函数三种写法">2. 目标函数三种写法</h3> <p><strong>SFT（模仿验证过的自己）</strong></p> <p>就是交叉熵，细节在过滤：硬过滤（只留全对的）信号干净，但剩不下难题；软加权（按置信度给样本加权）能留住难例。这个方法的硬伤是：它只会在「已经被验证对的区域」里加概率，<strong>不会产生新行为</strong>——这就是 offline 合成路线天花板低的机制原因。</p> <p><strong>DPO 系（用好坏对比）</strong></p> <p>构造「改前 vs 改后」或「本轮 vs 上一轮」的偏好对，闭式求解不用跑 RL。一个关键选择：reference model 用固定的还是滚动的。固定的稳但保守，滚动能逃离当前分布但容易失控。</p> <p><strong>GRPO（当前主流）</strong></p> <p>同一题采样 G 条，用组内通过率当基线，按相对表现算优势。好处有两个：不需要单独训价值网络；二元对错这种稀疏奖励，经组内对比变成了排序信号，正好配自对弈。</p> <h3 id="3-写入粒度和防翻车">3. 写入粒度和防翻车</h3> <ul> <li><strong>全参数更新</strong>：表达力最强，但改错了难追溯。</li> <li><strong>LoRA</strong>：可以多个补丁并存、热切换、单独回滚。TT-SI 的玩法是推理时发现模型在某个输入上不确定，现场造几个针对性样本、快速训一个 LoRA 补丁、用完就扔——这是 θ 路线能做到的最快速度。</li> <li><strong>回滚机制</strong>：保留历史 checkpoint，任何一代在测试集上退化就退回去。</li> </ul> <p><strong>θ 路线特有的三个坑：</strong></p> <ol> <li><strong>坍缩</strong>：递归用自生成数据训练，输出分布越来越窄，先丢多样性再丢正确性，而且可能要几代之后才暴露。</li> <li><strong>钻判据空子</strong>：验证器本身是用语言写的（LLM judge、任务描述），模型可以学会满足判据的字面意思而不是真实意图。</li> <li><strong>能力磨损</strong>：在窄任务上 RL 太多，预训练带来的通用能力会掉。</li> </ol> <hr/> <h2 id="二改-σ-的路线">二、改 Σ 的路线</h2> <p>θ 不动，编辑四个部件。</p> <h3 id="1-prompt改怎么指示模型">1. Prompt：改「怎么指示模型」</h3> <p>按反馈信号的信息量，四种做法：</p> <ol> <li><strong>标量搜索</strong>：只告诉模型「这个 prompt 得了 0.6 分」，让它提议下一个。没有方向信息，靠试（APE / OPRO）。</li> <li><strong>文本批评</strong>：失败后让模型写一段诊断（「你没处理空输入」），按诊断改 prompt。MAPS 的做法是：从失败案例归纳出规则，验证规则有效，再注入 prompt。</li> <li><strong>群体进化</strong>：维护一堆 prompt，用 LLM 当遗传算子——crossover 是「融合两个 prompt 的优点」，mutation 是「换个说法」。Promptbreeder 进一步把「怎么变异」的指令也放进进化池，算子自己也被进化。</li> <li><strong>文本梯度</strong>：把 agent 系统看成计算图，失败后用 LLM 反向推出一段诊断文本（相当于梯度），再逐节点回传修正（TextGrad）。</li> </ol> <h3 id="2-memory改存什么经验">2. Memory：改「存什么经验」</h3> <p>三个独立的设计选择。</p> <p><strong>存什么</strong></p> <ul> <li>原始轨迹：保真但吃 context。</li> <li>蒸馏后的规则 / 教训 / 工作流：省空间，迁移性看蒸馏质量。</li> <li>代码技能：把成功操作固化成函数，迁移性最好——代码的语义由执行器保证，不靠语言理解。</li> <li>隐式向量：latent token 直接注进 KV cache，最省空间但不可读、不可审计。</li> </ul> <p><strong>怎么组织</strong></p> <ul> <li>向量索引：语义相似检索，强在找类似案例，弱在处理不了「用户换新工作了」这种需要覆盖旧事实的情况。</li> <li>时序知识图（Zep）：带时间戳的实体关系，支持事实修正。</li> <li>卡片互链（A-MEM）、层级结构（H-MEM）。</li> </ul> <p><strong>怎么增删改查</strong></p> <p>最新的趋势是把记忆操作本身变成可学习的：用 RL 学「这条信息值不值得写」（Mem-α）、学「这轮该不该查记忆」、学遗忘时机（MemEvolve 直接进化记忆系统的结构）。</p> <p>本质一句话：<strong>记忆只有被检索出来塞进 context 才起作用</strong>，所以记忆优化最终都收敛成 context 工程——context 从被动容器变成被设计、被评测的对象。</p> <h3 id="3-tool改能做什么动作">3. Tool：改「能做什么动作」</h3> <p>三个动作。</p> <p><strong>路由（从现有工具里选）</strong>，四种实现：</p> <ul> <li>embedding 匹配：工具多了噪声大。</li> <li>依赖图（ToolNet）：把工具间前置条件建成 DAG，路由变成带约束的路径搜索，选第一步时就能预判多步组合可不可行。</li> <li>学出来的路由策略：多轮场景能纠错，但分布变了会脆。</li> <li>生成式（ToolGen）：工具 ID 变成特殊 token，选择 + 填参统一进生成过程，代价是工具集更新要重训。</li> </ul> <p><strong>精炼（修工具）</strong></p> <p>固定闭环：生成代码 → 沙箱执行 → 抓错误栈 → 反馈修订 → 验证通过才入库。三个改进方向：请专门的 critic 模型做诊断；把修好的轨迹蒸馏成可复用子程序（修「这一类问题」而不是「这一次调用」）；DRAFT 发现很多失败其实是文档和行为对不上——修文档比修代码便宜。</p> <p><strong>创造（造新工具）</strong></p> <p>完整流程：写代码 → 装依赖 → 沙箱调试 → 写文档 → 标准化注册 → 进池。铁律：<strong>没验证过的工具不允许被路由到</strong>——坏工具入库后会被反复调用，错误按复利放大。</p> <h3 id="4-控制代码改系统本身">4. 控制代码：改「系统本身」</h3> <p>写入对象从部件升级为代码。谱系：</p> <ul> <li><strong>AFlow</strong>：把 agent 编排写成代码，用 MCTS 搜索更好的版本。</li> <li><strong>ADAS</strong>：meta-agent 自己写系统代码、跑、看结果、重写。</li> <li><strong>Gödel Agent / DGM</strong>：直接改自己的运行时代码，DGM 还维护一棵 agent 后代树。</li> </ul> <p>这一层必须有<strong>验证门控</strong>：改完的代码跑一遍测试，通过才替换，不通过就回滚。没有这道门，自我修改的期望结果就是自我损坏——一次 prompt injection 会从临时攻击变成永久漏洞。</p> <hr/> <h2 id="三两条路的关系">三、两条路的关系</h2> <h3 id="1-改进内容其实和存储位置无关">1. 改进内容其实和存储位置无关</h3> <p>「学会处理 API 超时」这件事，可以存成：</p> <table> <thead> <tr> <th>存哪</th> <th>读取成本</th> <th>能看懂吗</th> <th>跨任务迁移</th> </tr> </thead> <tbody> <tr> <td>θ（行为倾向）</td> <td>零</td> <td>看不懂</td> <td>强</td> </tr> <tr> <td>prompt（一段指令）</td> <td>每轮费 token</td> <td>能</td> <td>中</td> </tr> <tr> <td>记忆库（一条规则）</td> <td>检索时费 token</td> <td>能</td> <td>中</td> </tr> <tr> <td>工具集（一个重试封装函数）</td> <td>调用时</td> <td>能</td> <td>强，还能给别人用</td> </tr> </tbody> </table> <p>所以很多「方法属于哪一类」的争执只是表象——同一个改进，换个介质而已。</p> <h3 id="2-真正的安全分界线判据在不在自己的控制回路里">2. 真正的安全分界线：判据在不在自己的控制回路里</h3> <p>比 θ/Σ 更本质的区分：</p> <ul> <li><strong>间接自改</strong>：模型产出数据和任务，由外部规则（梯度、选择压力）决定采纳。判据在回路外。</li> <li><strong>直接自改</strong>：模型输出本身就是对自己的修改（改 prompt、注册工具、patch 代码）。判据在回路内。</li> </ul> <p>风险不来自「改了哪里」，而来自「谁说了算」。判据在自己手里 → 必然会学着讨好判据（reward hacking、坍缩）；判据在外面且可验证 → 有锚。三篇综述关于安全的所有讨论，都是这一条的具体展开。</p> <h3 id="3-最终形态是嵌套循环">3. 最终形态是嵌套循环</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Σ 快循环（试错、反思、累积经验）
        ↓ 验证稳定后
θ 慢循环（蒸馏进权重，摊销到所有未来任务）
        ↓ 更好的先验
Σ 快循环
</code></pre></div></div> <p>中间那步转换（scaffold → 参数蒸馏）还是开放问题：失败的时候，系统要自动判断该改 prompt、改工具还是走梯度——三个介质的验证周期和回滚成本差着量级，目前没有统一的决策机制。</p> <hr/> <h2 id="四一张表收束">四、一张表收束</h2> <p>每个方法 = 在表里选一个格子，再决定验证做得多硬：</p> <table> <thead> <tr> <th> </th> <th>信号靠自评</th> <th>信号靠执行器</th> <th>信号靠交互/同伴</th> </tr> </thead> <tbody> <tr> <td><strong>写 θ</strong></td> <td>自蒸馏 SFT、SPIN</td> <td>STaR、Absolute Zero</td> <td>WebRL、SWE-RL</td> </tr> <tr> <td><strong>写 prompt</strong></td> <td>Self-Refine</td> <td>GEPA、MAPS</td> <td>多智能体辩论</td> </tr> <tr> <td><strong>写记忆</strong></td> <td>自反思入库</td> <td>按执行结果过滤的经验库</td> <td>多 agent 共享记忆</td> </tr> <tr> <td><strong>写工具</strong></td> <td>自生成+自评</td> <td>沙箱验证后注册</td> <td>技能社区共享</td> </tr> <tr> <td><strong>写代码</strong></td> <td>裸改自己（危险）</td> <td>测试门控的代码进化</td> <td>去中心化共同进化</td> </tr> </tbody> </table> <p>三条结论：</p> <ol> <li><strong>信号来源比介质选择更重要</strong>——同样是写 θ，有没有执行器验证，是进化和坍缩的分水岭。</li> <li><strong>自改每深入一层，验证就必须硬一档</strong>——改输出 &lt; 改组件 &lt; 改元规则 &lt; 改运行时。</li> <li><strong>offline/online、θ/Σ、快/慢是三对独立的轴</strong>——比较方法之前先说清在哪根轴上比。</li> </ol> <hr/> <h2 id="附录核心公式速查">附录：核心公式速查</h2> <p><strong>1. 自我改进的通用算子形式（Schmidhuber 综述 §3.2）</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>A_{t+1} = U( A_{1:t}, E( π_{θ_t,Σ_t}; Σ_t, C_t ) )
</code></pre></div></div> <p>agent 配置 A = (θ, Σ)，Σ = (p, m, T, g)。E 是执行过程（产出轨迹/批评/编辑建议），U 是把信号写回介质的更新算子。</p> <p><strong>2. 两条路线的分解</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>θ 路线：θ_{t+1} = IMPROVE_θ(θ_{1:t}; S_t),   Σ_{t+1} = Σ_t
Σ 路线：Σ_{t+1} = IMPROVE_Σ(Σ_{1:t}; S_t),   θ_{t+1} = θ_t
</code></pre></div></div> <p><strong>3. 自生成数据 + SFT 的训练目标（厦大综述 §5.1）</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>θ_{t+1} = argmin_θ  L(θ; D_t) + λ·Ω(θ, θ_0)
D_t = D_base ∪ Filter(D_gen)
</code></pre></div></div> <p>Ω 是相对初始点的正则项，用来防遗忘；Filter 是质量门控（多数投票 / 执行器 / 过程奖励）。</p> <p><strong>4. GRPO 的组内优势（Absolute Zero / R-Zero 等自对弈的训练核心）</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>A_i = r_i − mean( r_1 … r_G )        （同一 prompt 采 G 条轨迹，组内相对表现）
</code></pre></div></div> <p>二元对错奖励经组内对比变成排序信号，不需要价值网络。</p> <p><strong>5. Prompt 精修的迭代形式（§6.1.2）</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>p_{t+1} = Refine( p_t, c_t ),   c_t = Critique( Output(p_t) )
</code></pre></div></div> <p><strong>6. 文本梯度的更新规则（§6.1.4）</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>p_{t+1} = p_t ⊕ g(p_t)
</code></pre></div></div> <p>g 是由 LLM 从失败案例反推出的结构化诊断文本，⊕ 表示由 LLM 语义化地执行修正。</p> <p><strong>7. 全脚手架自改的验证门控（§6.4，安全的关键式）</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Σ_{t+1} =  Σ̃_{t+1}   若 V(Σ̃_{t+1}) = 1   （测试/安全校验通过）
         =  Σ_t       否则               （回滚）
</code></pre></div></div> <p><strong>8. 环境的形式化（厦大综述 §2.B）</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>E = ⟨S, V⟩
</code></pre></div></div> <p>S 是状态空间（任务上下文 + 可交互的外部知识），V 是验证反馈机制（执行器、单元测试、证明器等确定性判据）。V 的质量决定自进化系统的上限。</p> <p><strong>9. 自对弈的 Challenger–Solver 奖励结构（Absolute Zero）</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Solver 解出题   →  Solver +r,  Challenger −r   （题太简单）
Solver 失败    →  Challenger +r              （题有区分度）
</code></pre></div></div> <p>双向压力自动把题目难度推向 Solver 能力的边缘（自适应课程）。</p>]]></content><author><name></name></author><category term="Agent"/><category term="agent"/><category term="self-improvement"/><category term="self-evolution"/><category term="reinforcement-learning"/><category term="grpo"/><category term="prompt-optimization"/><category term="memory"/><category term="tool-use"/><summary type="html"><![CDATA[三篇综述整合——自我改进 Agent 的本质是把验证过的信号写进模型权重 θ 或外部脚手架 Σ，关键分界线在判据是否在控制回路内。]]></summary></entry><entry><title type="html">CS146S 学习笔记：从 Prompt 技术全景到 AI IDE 设计文档规范</title><link href="https://leesanyee.me/blog/2026/CS146S-Prompt%E4%B8%8EAI-IDE%E8%AE%BE%E8%AE%A1%E6%96%87%E6%A1%A3/" rel="alternate" type="text/html" title="CS146S 学习笔记：从 Prompt 技术全景到 AI IDE 设计文档规范"/><published>2026-09-01T13:00:00+00:00</published><updated>2026-09-01T13:00:00+00:00</updated><id>https://leesanyee.me/blog/2026/CS146S-Prompt%E4%B8%8EAI-IDE%E8%AE%BE%E8%AE%A1%E6%96%87%E6%A1%A3</id><content type="html" xml:base="https://leesanyee.me/blog/2026/CS146S-Prompt%E4%B8%8EAI-IDE%E8%AE%BE%E8%AE%A1%E6%96%87%E6%A1%A3/"><![CDATA[<h1 id="cs146s-学习笔记从-prompt-技术全景到-ai-ide-设计文档规范">CS146S 学习笔记：从 Prompt 技术全景到 AI IDE 设计文档规范</h1> <blockquote> <p>本文整合 CS146S（斯坦福《现代软件开发者》）前 3 周的核心内容： <strong>Week 1 的 8 种 Prompt 技术 + 通用 Best Practices</strong>，与 <strong>Week 3 的「产品经理式」设计文档（8 字段 Spec + 完整模板）+ 让代码库对 Agent 友好的 Best Practices</strong>。</p> <p>一张图理解递进关系：<strong>提示词（让 LLM 说话）→ 工具/MCP（让 LLM 做事）→ 设计文档（让 Agent 替你写复杂代码）</strong>。</p> </blockquote> <hr/> <h2 id="一week-1llm-高效提示power-prompting的-8-种技术">一、Week 1：LLM 高效提示（Power Prompting）的 8 种技术</h2> <p>讲义《Power Prompting for LLMs》把提示技术分成两大类：<strong>「不给例子」与「给例子」</strong>，再加上<strong>让模型自我纠错</strong>的高级玩法。核心观点：Prompting 既是艺术也是科学——LLM 是黑盒，但有经验证有效的固定套路。</p> <h3 id="11-zero-shot-prompting零样本提示">1.1 Zero-shot Prompting（零样本提示）</h3> <p>直接提要求，<strong>不给支持、不给例子</strong>。</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Write me a Rust for-loop that iterates over a list of strings, printing every value in an even index
</code></pre></div></div> <table> <thead> <tr> <th>维度</th> <th>说明</th> </tr> </thead> <tbody> <tr> <td>适用</td> <td>简单、常规、模型已经见过的任务</td> </tr> <tr> <td>优点</td> <td>省事、快</td> </tr> <tr> <td>缺点</td> <td>对格式/风格敏感的任务容易翻车</td> </tr> </tbody> </table> <h3 id="12-k-shot-prompting少样本提示--in-context-learning">1.2 K-shot Prompting（少样本提示 / In-Context Learning）</h3> <p>在 prompt 里塞几个「怎么做」的例子。k 通常取 <strong>1、3、5</strong>（有实证支撑）。<strong>适合推理步骤不多的任务</strong>——例子越多模型越容易照葫芦画瓢，但推理型任务光给例子不够。</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Write a for-loop iterating over a list of strings using the naming convention in our repo.
Here are some examples of how we typically format variable names:
&lt;example&gt;var StRaRrAy = ['cat', 'dog', 'wombat']&lt;/example&gt;
&lt;example&gt;def func CaPiTaLiZeStR = () =&gt; {}&lt;/example&gt;
</code></pre></div></div> <h3 id="13-chain-of-thought-prompting思维链-cot">1.3 Chain-of-Thought Prompting（思维链 CoT）</h3> <p>让模型<strong>先推理再作答</strong>，适合多步骤逻辑任务（编程、数学）。三种变体：</p> <ul> <li><strong>Multi-shot CoT</strong>：给出带推理过程的示例</li> <li><strong>Zero-shot CoT</strong>：直接加一句「Let’s think step-by-step」</li> <li><strong>显式推理标签</strong>：要求输出放在 <code class="language-plaintext highlighter-rouge">&lt;reasoning&gt;</code> 标签里</li> </ul> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Write a function to find the longest increasing subsequence in an array.
Think step by step about the subproblems before coding.
Include worked out examples of subarrays you are considering as you answer this question.
</code></pre></div></div> <h3 id="14-self-consistency-prompting自洽性采样">1.4 Self-consistency Prompting（自洽性采样）</h3> <p>对同一个问题<strong>采样多次（通常配 CoT），取多数结果</strong>。本质是「模型集成」——通过多样化推理路径投票，抑制幻觉和偶发错误。适合对正确性敏感、但成本可接受的场景（如抓 bug）。</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>What's the root cause for this error: &lt;traceback&gt;...&lt;/traceback&gt;
# 同一问题 Prompt 5 次，取多数答案
</code></pre></div></div> <h3 id="15-tool-use工具使用">1.5 Tool Use（工具使用）</h3> <p>允许 LLM <strong>把动作外包给外部系统</strong>（执行测试、查询 API）。讲义强调：这是<strong>降低幻觉、让 LLM 获得自主性</strong>最重要的技术之一——也是后续 MCP、Agent 的起点。</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Fix the IndexError. Ensure the CI tests still pass once you have made the fix.
Here are the available tools:
&lt;tools&gt;
pytest -s /path/to/unit_tests
pytest -v /path/to/integration_tests
&lt;/tools&gt;
</code></pre></div></div> <h3 id="16-retrieval-augmented-generationrag检索增强生成">1.6 Retrieval Augmented Generation（RAG，检索增强生成）</h3> <p>把<strong>上下文数据注入 prompt</strong>：不重训模型就保持信息最新、天然带引用和可解释性、减少幻觉。</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Here is how the UserAuthService works now:
&lt;code_snippet&gt;...&lt;/code_snippet&gt;
Here is the path to the requests-oauthlib documentation:
&lt;url&gt;https://requests-oauthlib.readthedocs.io/en/latest/&lt;/url&gt;
</code></pre></div></div> <h3 id="17-reflexion反思">1.7 Reflexion（反思）</h3> <p>让 LLM <strong>批评自己的输出</strong>并重试，通过多轮对话把环境反馈重新注入上下文。</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Turn 1: 模型给出初版答案
Turn 2: "Now critique your answer. Was it correct? If not, explain why and try again."
</code></pre></div></div> <p>讲义里的例子展示了完整闭环：<strong>observe（测试报 JSONDecodeError）→ reflect（我扩展了 company_location，需兼容字符串输入）→ 扩展 prompt 重试</strong>。</p> <h3 id="18-基础术语别搞混">1.8 基础术语（别搞混）</h3> <table> <thead> <tr> <th>术语</th> <th>含义</th> </tr> </thead> <tbody> <tr> <td><strong>System prompt</strong></td> <td>发给 LLM 的第一条消息（用户通常看不到），定义人设、输出规则、风格</td> </tr> <tr> <td><strong>User prompt</strong></td> <td>人类真正的请求/指令（前面所有例子都是）</td> </tr> <tr> <td><strong>Assistant</strong></td> <td>LLM 实际生成的内容（即 <code class="language-plaintext highlighter-rouge">role=assistant</code> 的历史输出）</td> </tr> </tbody> </table> <hr/> <h2 id="二week-1-best-practices通用提示工程">二、Week 1 Best Practices：通用提示工程</h2> <ol> <li><strong>用 Prompt Improver 迭代</strong>——Anthropic 官方有专门的 prompt 改进工具，写完先过一遍。</li> <li><strong>清晰第一</strong>——把你的 prompt 给一个毫无上下文的人看：<strong>如果他会困惑，LLM 一定也会</strong>。</li> <li><strong>大胆用 Role Prompting</strong>——让 system prompt 更强：<code class="language-plaintext highlighter-rouge">"You are a helpful assistant that loves programming at the level of a senior software developer and is very detailed and pedantic in your answers."</code></li> <li><strong>结构化格式</strong>——用 <code class="language-plaintext highlighter-rouge">&lt;log&gt;</code>、<code class="language-plaintext highlighter-rouge">&lt;error&gt;</code> 等标签把日志和堆栈包起来，模型更容易定位。</li> <li><strong>明确说明想要什么</strong>——语言、技术栈、库、约束，别让模型猜。</li> <li><strong>拆解任务</strong>——大任务拆成小步骤，一次只让模型做一件定义清楚的事。</li> </ol> <hr/> <h2 id="三week-3复杂任务--产品经理思维8-字段-spec">三、Week 3：复杂任务 = 产品经理思维（8 字段 Spec）</h2> <p>Week 3 讲义（The AI IDE: Fundamentals to Power User）提出一个关键转变：</p> <blockquote> <p><strong>简单改动，不需要太费心思写 prompt；复杂任务，你要变成产品经理——精心撰写规格文档（spec doc）。</strong></p> </blockquote> <p>简化版规格文档只需 <strong>8 个字段</strong>，但正是「给 Agent 写需求」的最小完备集：</p> <table> <thead> <tr> <th>#</th> <th>字段</th> <th>含义</th> </tr> </thead> <tbody> <tr> <td>1</td> <td><strong>Goal</strong></td> <td>这次改动的目的是什么</td> </tr> <tr> <td>2</td> <td><strong>Definitions</strong></td> <td>LLM 需要先知道哪些前置知识/术语</td> </tr> <tr> <td>3</td> <td><strong>Plan</strong></td> <td>高层实现拆解</td> </tr> <tr> <td>4</td> <td><strong>Source files being changed</strong></td> <td>涉及哪些源码文件，为什么相关</td> </tr> <tr> <td>5</td> <td><strong>Test cases</strong></td> <td>测试怎么做</td> </tr> <tr> <td>6</td> <td><strong>Edge cases</strong></td> <td>有哪些特殊情况要处理</td> </tr> <tr> <td>7</td> <td><strong>Out-of-scope</strong></td> <td><strong>明确不要改什么</strong>（防止 Agent 顺手重构）</td> </tr> <tr> <td>8</td> <td><strong>Extensions</strong></td> <td>以后哪些改动会相关，让 LLM 提前做可扩展设计、别走捷径</td> </tr> </tbody> </table> <blockquote> <p>💡 实用提示：<strong>「Out-of-scope」往往是 8 个字段里最容易被新手忽略、但对 Agent 最管用的一个</strong>——不说清边界，Agent 很容易顺手做超出范围的重构。</p> </blockquote> <h3 id="31-完整版-design-doc-模板">3.1 完整版 Design Doc 模板</h3> <p>课程仓库还提供了一个<strong>更完整的 12 节设计文档模板</strong>（<code class="language-plaintext highlighter-rouge">design_doc_template.md</code>），适合真正的大改动，与上面的 8 字段正好互补：</p> <table> <thead> <tr> <th>模块</th> <th>内容</th> </tr> </thead> <tbody> <tr> <td><strong>Current Context</strong></td> <td>现有系统概览、关键组件关系、要解决的痛点</td> </tr> <tr> <td><strong>Requirements</strong></td> <td>功能需求（必须做什么）+ 非功能需求（性能/可扩展/可观测/安全）</td> </tr> <tr> <td><strong>Design Decisions</strong></td> <td>每个重大决策：选什么方案 + 理由 + 备选方案权衡</td> </tr> <tr> <td><strong>Technical Design</strong></td> <td>核心组件、数据模型（带类型注解）、集成点、<strong>Files Changes（唯一受影响文件清单）</strong></td> </tr> <tr> <td><strong>Implementation Plan</strong></td> <td>分阶段：Phase 1 初期实现 → Phase 2 增强 → Phase 3 生产就绪</td> </tr> <tr> <td><strong>Testing Strategy</strong></td> <td>单元测试（mock 策略、覆盖预期）+ 集成测试（场景、环境、数据）</td> </tr> <tr> <td><strong>Observability</strong></td> <td>日志（关键点/级别/结构化格式）、指标（采集方式/告警阈值）</td> </tr> <tr> <td><strong>Future Considerations</strong></td> <td>潜在增强、已知限制、技术债</td> </tr> <tr> <td><strong>Dependencies</strong></td> <td>构建工具、测试框架、开发工具</td> </tr> <tr> <td><strong>Security</strong></td> <td>认证授权、数据保护、合规</td> </tr> <tr> <td><strong>Rollout Strategy</strong></td> <td>开发 → 测试 → 预发布 → 生产 → 监控</td> </tr> <tr> <td><strong>References</strong></td> <td>关联设计文档、外部文档、相关标准</td> </tr> </tbody> </table> <p><strong>用法建议</strong>：8 字段 Spec 是「电梯版」，适合把任务交给 Agent 时写进 prompt；12 节模板是「正片版」，适合要评审的大改动。小任务用 8 字段，大任务把 12 节模板作为仓库里的文档资产。</p> <hr/> <h2 id="四week-3-best-practices让代码库对-agent-友好">四、Week 3 Best Practices：让代码库对 Agent 友好</h2> <p>设计文档解决「这次任务」，下面的实践解决「这个仓库」——两者叠加，Agent 才能高效工作。</p> <h3 id="41-优化代码库让人类和-agent-都能看懂">4.1 优化代码库，让人类和 Agent 都能看懂</h3> <blockquote> <p>大量 LLM 的困惑源于<strong>在混乱的仓库上硬完成任务</strong>。为 LLM 提供最佳上下文 = 把以下内容<strong>全部写成文档</strong>：</p> </blockquote> <ul> <li>Repo 导航（Repo orientation）</li> <li>文件结构（File structure）</li> <li>环境与启动（Setup and environment）</li> <li>最佳实践（Best practices）</li> <li>代码风格（Code style）</li> <li>访问模式（Access patterns）</li> <li>API 与契约（APIs and contracts）</li> </ul> <p>💡 讲义特别建议：<strong>仓库用 monorepo 结构</strong>，减少 Agent 跨仓库找文件的成本。</p> <h3 id="42-用-agent-配置文件提供导航">4.2 用 Agent 配置文件提供「导航」</h3> <table> <thead> <tr> <th>文件</th> <th>作用</th> </tr> </thead> <tbody> <tr> <td><strong>CLAUDE.md</strong></td> <td>Claude 启动对话时自动拉入上下文：常用 bash 命令、核心文件与工具函数、代码风格、测试指引</td> </tr> <tr> <td><strong>.cursorrules</strong></td> <td>Cursor 的规则文件（类似用途）</td> </tr> <tr> <td><strong>AGENTS.md</strong></td> <td>开放格式的跨工具 Agent 指引标准</td> </tr> <tr> <td><strong>llms.txt</strong></td> <td>给爬取网页的 LLM 看的站点导航</td> </tr> </tbody> </table> <blockquote> <p>⚠️ 注意：<strong>Agent 不一定会遵守这些描述/指令</strong>——它们只是「指引」（guidance），不是硬约束。别指望写了 CLAUDE.md 模型就 100% 照做，它只是显著提高正确率。</p> </blockquote> <h3 id="43-附赠ai-ide-底层原理知道这些写-prompt-才有手感">4.3 附赠：AI IDE 底层原理（知道这些，写 prompt 才有手感）</h3> <ul> <li><strong>Tab 补全</strong>：围绕当前代码的小上下文窗口 → 发送到服务器跑 infilling 模型 → 返回补全建议。</li> <li><strong>Chat 模式</strong>：代码块以 embedding 存入语义索引（服务端，文件名+代码混淆）→ 查询时检索最相关 chunks 作为上下文 → IDE 定期重索引，<strong>chunk 差异用 Merkle 树计算</strong>以高效同步更新。</li> </ul> <p>理解「Chat 模式 = 语义检索 + 拼上下文」，就能明白为什么设计文档、CLAUDE.md 这类<strong>结构化上下文</strong>对 AI IDE 如此重要。</p> <hr/> <h2 id="五一页速查表">五、一页速查表</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Week 1 — 8 种 Prompt 技术
╔══════════════════════╦══════════════════════════════════════════╗
║ Zero-shot            ║ 不给例子直接问                            ║
║ K-shot               ║ 给 1/3/5 个例子（in-context learning）    ║
║ Chain-of-Thought     ║ 先推理再答；"think step-by-step"          ║
║ Self-consistency     ║ 采样多次取多数，抗幻觉                    ║
║ Tool Use             ║ 外包给外部系统，自主性的起点              ║
║ RAG                  ║ 注入检索上下文，带引用、更新鲜            ║
║ Reflexion            ║ 自己批评自己，多轮重试                    ║
║ System/User/Assistant║ 人设 / 请求 / 模型输出                    ║
╚══════════════════════╩══════════════════════════════════════════╝

Week 3 — 把任务交给 Agent 前
1. 简单改动 → 直接 prompt
2. 复杂改动 → 8 字段 Spec（Goal/Definitions/Plan/Files/Tests/Edge cases/Out-of-scope/Extensions）
3. 大改动   → 12 节 Design Doc 模板
4. 仓库治理 → CLAUDE.md + 文档化的 repo 导航 + monorepo
</code></pre></div></div> <p><strong>一句话总结</strong>：Week 1 教你怎么和 LLM「说话」，Week 2 教你怎么让它「用工具」（MCP），Week 3 教你怎么把「做复杂项目的项目管理能力」也交给它——<strong>写好设计文档，是你从使用者变成 Agent 管理者的分水岭</strong>。</p>]]></content><author><name></name></author><category term="CS146S"/><category term="cs146s"/><category term="llm"/><category term="prompt-engineering"/><category term="ai-ide"/><category term="agent"/><category term="design-doc"/><summary type="html"><![CDATA[CS146S 学习笔记：从 Prompt 技术全景到 AI IDE 设计文档规范]]></summary></entry><entry><title type="html">二分查找双模板 + searchInsert 逐行拆解：从模板到边界</title><link href="https://leesanyee.me/blog/2026/%E4%BA%8C%E5%88%86%E6%9F%A5%E6%89%BE%E5%8F%8C%E6%A8%A1%E6%9D%BFsearchInsert%E6%8B%86%E8%A7%A3/" rel="alternate" type="text/html" title="二分查找双模板 + searchInsert 逐行拆解：从模板到边界"/><published>2026-08-26T15:00:00+00:00</published><updated>2026-08-26T15:00:00+00:00</updated><id>https://leesanyee.me/blog/2026/%E4%BA%8C%E5%88%86%E6%9F%A5%E6%89%BE%E5%8F%8C%E6%A8%A1%E6%9D%BFsearchInsert%E6%8B%86%E8%A7%A3</id><content type="html" xml:base="https://leesanyee.me/blog/2026/%E4%BA%8C%E5%88%86%E6%9F%A5%E6%89%BE%E5%8F%8C%E6%A8%A1%E6%9D%BFsearchInsert%E6%8B%86%E8%A7%A3/"><![CDATA[<h1 id="二分查找双模板--searchinsert-逐行拆解从模板到边界">二分查找双模板 + searchInsert 逐行拆解：从模板到边界</h1> <blockquote> <p>本文由三部分组成：<strong>双模板框架</strong>（找最小 / 找最大）、<strong>searchInsert 逐行拆解</strong>（模板一的最干净应用），以及一个藏在 searchInsert 里的<strong>边界 bug</strong>——它会让「target 大于所有元素」时返回错误答案。</p> </blockquote> <h2 id="一句话理解">一句话理解</h2> <p><strong>所有二分查找只有两种题型：找「满足条件的最小值」和「满足条件的最大值」。</strong> 各自对应一个 4 行模板。searchInsert 是模板一的应用，但它暴露了一个模板笔记里常见的盲区——<strong>答案域的右边界</strong>。</p> <hr/> <h2 id="一前提条件单调性是二分的命根子">一、前提条件：单调性是二分的命根子</h2> <p>二分查找只能用于<strong>单调</strong>的数据：</p> <ul> <li>数组必须有序（递增或递减）</li> <li>或者<strong>答案具有单调性</strong>：如果 <code class="language-plaintext highlighter-rouge">x</code> 满足条件，则 <code class="language-plaintext highlighter-rouge">x+1</code> 也满足（找最小）；或者 <code class="language-plaintext highlighter-rouge">x</code> 满足则 <code class="language-plaintext highlighter-rouge">x-1</code> 也满足（找最大）</li> </ul> <p>没有单调性，二分会漏掉正确答案——这是所有二分题的第一步判断。</p> <h3 id="基础二分-vs-二分答案">基础二分 vs 二分答案</h3> <table> <thead> <tr> <th> </th> <th>基础二分</th> <th>二分答案</th> </tr> </thead> <tbody> <tr> <td>搜索空间</td> <td>数组索引 <code class="language-plaintext highlighter-rouge">[0, n-1]</code></td> <td>答案的取值范围（如 <code class="language-plaintext highlighter-rouge">[1, max]</code>）</td> </tr> <tr> <td>比较对象</td> <td><code class="language-plaintext highlighter-rouge">nums[mid]</code> 和 <code class="language-plaintext highlighter-rouge">target</code></td> <td><code class="language-plaintext highlighter-rouge">check(mid)</code> 的结果</td> </tr> <tr> <td>单调性来源</td> <td>数组有序</td> <td><code class="language-plaintext highlighter-rouge">check</code> 函数具有单调性</td> </tr> </tbody> </table> <p><code class="language-plaintext highlighter-rouge">nums[mid] &gt;= target</code> 就是一个「check 函数」，所以 searchInsert 在两种框架下都能理解——它既是在索引里找位置，也是在做「第一个满足 <code class="language-plaintext highlighter-rouge">nums[pos] &gt;= target</code> 的位置」的二分答案。</p> <hr/> <h2 id="二双模板框架背这两个就够">二、双模板框架（背这两个就够）</h2> <h3 id="模板一找满足条件的最小值最左--下界">模板一：找满足条件的最小值（最左 / 下界）</h3> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">left</span> <span class="o">=</span> <span class="err">最小可能值</span><span class="p">,</span> <span class="n">right</span> <span class="o">=</span> <span class="err">最大可能值</span><span class="p">;</span>

<span class="k">while</span> <span class="p">(</span><span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">mid</span> <span class="o">=</span> <span class="n">left</span> <span class="o">+</span> <span class="p">(</span><span class="n">right</span> <span class="o">-</span> <span class="n">left</span><span class="p">)</span> <span class="o">/</span> <span class="mi">2</span><span class="p">;</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">check</span><span class="p">(</span><span class="n">mid</span><span class="p">))</span> <span class="p">{</span>
        <span class="n">right</span> <span class="o">=</span> <span class="n">mid</span><span class="p">;</span>        <span class="c1">// 满足条件，答案在左边（含 mid）</span>
    <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
        <span class="n">left</span> <span class="o">=</span> <span class="n">mid</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>     <span class="c1">// 不满足，答案在右边（不含 mid）</span>
    <span class="p">}</span>
<span class="p">}</span>
<span class="k">return</span> <span class="n">left</span><span class="p">;</span>  <span class="c1">// left == right，就是答案</span>
</code></pre></div></div> <p><strong>记忆口诀：满足往左缩，不满足往右跳。</strong></p> <ul> <li>为什么 <code class="language-plaintext highlighter-rouge">right = mid</code>？mid <strong>满足条件</strong>，它<strong>有可能是最终答案</strong>，不能排除。</li> <li>为什么 <code class="language-plaintext highlighter-rouge">left = mid + 1</code>？mid <strong>不满足</strong>，它<strong>一定不是答案</strong>，直接排除。</li> </ul> <h3 id="模板二找满足条件的最大值最右--上界">模板二：找满足条件的最大值（最右 / 上界）</h3> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">left</span> <span class="o">=</span> <span class="err">最小可能值</span><span class="p">,</span> <span class="n">right</span> <span class="o">=</span> <span class="err">最大可能值</span><span class="p">;</span>

<span class="k">while</span> <span class="p">(</span><span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">mid</span> <span class="o">=</span> <span class="n">left</span> <span class="o">+</span> <span class="p">(</span><span class="n">right</span> <span class="o">-</span> <span class="n">left</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span> <span class="o">/</span> <span class="mi">2</span><span class="p">;</span>  <span class="c1">// 注意 +1！</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">check</span><span class="p">(</span><span class="n">mid</span><span class="p">))</span> <span class="p">{</span>
        <span class="n">left</span> <span class="o">=</span> <span class="n">mid</span><span class="p">;</span>         <span class="c1">// 满足条件，答案在右边（含 mid）</span>
    <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
        <span class="n">right</span> <span class="o">=</span> <span class="n">mid</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span>    <span class="c1">// 不满足，答案在左边（不含 mid）</span>
    <span class="p">}</span>
<span class="p">}</span>
<span class="k">return</span> <span class="n">left</span><span class="p">;</span>
</code></pre></div></div> <p><strong>记忆口诀：满足往右扩，不满足往左砍。mid 要加一防死锁。</strong></p> <p><strong>为什么模板二的 mid 要 +1？</strong> 假设 <code class="language-plaintext highlighter-rouge">left = 3, right = 4</code>：</p> <ul> <li>不加 1：<code class="language-plaintext highlighter-rouge">mid = 3</code>，若满足条件则 <code class="language-plaintext highlighter-rouge">left = mid = 3</code> → <strong>死循环</strong></li> <li>加 1：<code class="language-plaintext highlighter-rouge">mid = 4</code>，若满足条件则 <code class="language-plaintext highlighter-rouge">left = 4</code>，循环正常结束</li> </ul> <p><strong>防死锁的本质</strong>：模板二用 <code class="language-plaintext highlighter-rouge">left = mid</code>，为了让区间<strong>必然缩小</strong>，mid 必须偏向右边（上中位数，<code class="language-plaintext highlighter-rouge">(left+right+1)/2</code>）。模板一用 <code class="language-plaintext highlighter-rouge">right = mid</code>，同理 mid 必须偏向左边（下中位数，<code class="language-plaintext highlighter-rouge">(left+right)/2</code>）。<strong>「哪边用 <code class="language-plaintext highlighter-rouge">=</code> 收边，mid 就往对面偏。」</strong></p> <hr/> <h2 id="三while-left--right-vs-while-left--right">三、<code class="language-plaintext highlighter-rouge">while (left &lt; right)</code> vs <code class="language-plaintext highlighter-rouge">while (left &lt;= right)</code></h2> <table> <thead> <tr> <th>特性</th> <th><code class="language-plaintext highlighter-rouge">left &lt; right</code></th> <th><code class="language-plaintext highlighter-rouge">left &lt;= right</code></th> </tr> </thead> <tbody> <tr> <td>循环结束</td> <td><code class="language-plaintext highlighter-rouge">left == right</code>（重合）</td> <td><code class="language-plaintext highlighter-rouge">left = right + 1</code>（交叉）</td> </tr> <tr> <td>返回值</td> <td><code class="language-plaintext highlighter-rouge">left</code> 或 <code class="language-plaintext highlighter-rouge">right</code> 都行</td> <td>必须返回 <code class="language-plaintext highlighter-rouge">left</code></td> </tr> <tr> <td>分支数</td> <td>2 个（满足/不满足）</td> <td>容易写出 3 个分支（多了 <code class="language-plaintext highlighter-rouge">==</code>）</td> </tr> </tbody> </table> <p><strong><code class="language-plaintext highlighter-rouge">left &lt;= right</code> 的交叉现象</strong>（地铁闸机比喻）：循环条件是<strong>进门时</strong>检查的，而 left/right 是在<strong>门里面</strong>被改变的。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>left = 3, right = 3;
while (left &lt;= right) {   // 3 &lt;= 3？是，进门！
    int mid = 3;
    right = mid - 1;      // right 变成 2，已经交叉了
}                         // 回到门口：3 &lt;= 2？否，挡在门外
</code></pre></div></div> <p>循环不会在 <code class="language-plaintext highlighter-rouge">right = 2</code> 的那一瞬间中断——先刷卡进门，在站台里把卡弄丢了，想再进一次时被挡在外面。模板框架选 <code class="language-plaintext highlighter-rouge">left &lt; right</code>，就是因为「重合即结束、返回 left」这个语义最干净，不用处理交叉后的语义。</p> <hr/> <h2 id="四searchinsert-逐行拆解lc-35">四、searchInsert 逐行拆解（LC 35）</h2> <p>问题：给定递增数组 <code class="language-plaintext highlighter-rouge">nums</code> 和 <code class="language-plaintext highlighter-rouge">target</code>，返回它应该被插入的下标（如果存在则返回其下标）。<strong>这定义的正是「第一个 <code class="language-plaintext highlighter-rouge">&gt;= target</code> 的位置」，即 C++ 的 <code class="language-plaintext highlighter-rouge">std::lower_bound</code>。</strong></p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">searchInsert</span><span class="p">(</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;&amp;</span> <span class="n">nums</span><span class="p">,</span> <span class="kt">int</span> <span class="n">target</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">left</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="n">right</span> <span class="o">=</span> <span class="n">nums</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span>   <span class="c1">// ← 有坑，见第五节</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span><span class="p">)</span> <span class="p">{</span>
        <span class="kt">int</span> <span class="n">mid</span> <span class="o">=</span> <span class="n">left</span> <span class="o">+</span> <span class="p">(</span><span class="n">right</span> <span class="o">-</span> <span class="n">left</span><span class="p">)</span> <span class="o">/</span> <span class="mi">2</span><span class="p">;</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">mid</span><span class="p">]</span> <span class="o">&gt;=</span> <span class="n">target</span><span class="p">)</span> <span class="p">{</span>           <span class="c1">// check(mid)：满足吗？</span>
            <span class="n">right</span> <span class="o">=</span> <span class="n">mid</span><span class="p">;</span>                      <span class="c1">// 满足 → 往左缩（模板一）</span>
        <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
            <span class="n">left</span> <span class="o">=</span> <span class="n">mid</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>                   <span class="c1">// 不满足 → 往右跳</span>
        <span class="p">}</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">left</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p><strong>循环不变量：答案一定在 <code class="language-plaintext highlighter-rouge">[left, right]</code> 内。</strong> 每步保持不变性，直到缩成单点。</p> <h3 id="完整算例nums--1356-target--4答案--2">完整算例：<code class="language-plaintext highlighter-rouge">nums = [1,3,5,6]</code>, <code class="language-plaintext highlighter-rouge">target = 4</code>（答案 = 2）</h3> <table> <thead> <tr> <th>轮次</th> <th>left</th> <th>right</th> <th>mid</th> <th>nums[mid]</th> <th>动作</th> </tr> </thead> <tbody> <tr> <td>1</td> <td>0</td> <td>3</td> <td>1</td> <td>3</td> <td><code class="language-plaintext highlighter-rouge">3 &gt;= 4</code>? 否 → <code class="language-plaintext highlighter-rouge">left = 2</code>（排除 mid）</td> </tr> <tr> <td>2</td> <td>2</td> <td>3</td> <td>2</td> <td>5</td> <td><code class="language-plaintext highlighter-rouge">5 &gt;= 4</code>? 是 → <code class="language-plaintext highlighter-rouge">right = 2</code>（保留 mid）</td> </tr> <tr> <td>结束</td> <td>2</td> <td>2</td> <td>—</td> <td>—</td> <td>返回 2 ✅</td> </tr> </tbody> </table> <h3 id="为什么比较符是--而不是-一个条件统一两种语义">为什么比较符是 <code class="language-plaintext highlighter-rouge">&gt;=</code> 而不是 <code class="language-plaintext highlighter-rouge">&gt;</code>：一个条件统一两种语义</h3> <table> <thead> <tr> <th>情况</th> <th><code class="language-plaintext highlighter-rouge">&gt;=</code> 找到的</th> <th>示例（nums=[1,3,5,6]）</th> </tr> </thead> <tbody> <tr> <td>target 存在</td> <td>第一个 target（重复时最左）</td> <td>target=5 → 2</td> </tr> <tr> <td>target 不存在</td> <td>第一个比 target 大的位置 = 插入点</td> <td>target=4 → 2</td> </tr> </tbody> </table> <p>写成 <code class="language-plaintext highlighter-rouge">&gt;</code> 就会在处理「存在」时跳过正确下标。<strong>searchInsert 的灵魂就是：用「第一个 <code class="language-plaintext highlighter-rouge">&gt;= target</code>」这一个条件，把「找得到」和「找不到」两种情况统一掉。</strong></p> <hr/> <h2 id="五循环不变量证明三件套从背模板到懂模板">五、循环不变量：证明三件套（从「背模板」到「懂模板」）</h2> <p>这一节回答一个问题：<strong>凭什么相信这个循环一定对？</strong> 答案是「循环不变量 + 证明三件套」——CLRS《算法导论》证明算法正确性的标准方法，形式上就是 Hoare 逻辑（C.A.R. Hoare, 1969）的 while 循环规则。</p> <h3 id="什么是循环不变量">什么是循环不变量</h3> <p><strong>循环不变量（loop invariant）= 一条「每次进入循环时都必然为真」的命题。</strong> 「不变」不是说 left/right 不变——它们每轮都在变；不变量描述的是<strong>循环走到这一步，算法已经确定了什么</strong>。</p> <p>对 searchInsert，不变量是：</p> <blockquote> <p><strong>答案一定在 <code class="language-plaintext highlighter-rouge">[left, right]</code> 内（right 含）。</strong></p> </blockquote> <h3 id="为什么三件套就够因为它是归纳法">为什么三件套就够：因为它是归纳法</h3> <p>三个步骤恰好对应数学归纳法的三块：</p> <table> <thead> <tr> <th>证明三件套</th> <th>归纳法对应</th> <th>内容</th> <th>直觉</th> </tr> </thead> <tbody> <tr> <td>① 初始成立</td> <td>基础步</td> <td>循环开始前，不变量成立</td> <td>能推倒第一张多米诺</td> </tr> <tr> <td>② 保持</td> <td>归纳步</td> <td>某轮开始前成立 ⟹ 这轮结束后仍成立</td> <td>每张牌都会推倒下一张</td> </tr> <tr> <td>③ 终止</td> <td>收尾</td> <td>循环必然结束；结束时不变量 + 退出条件 ⟹ 答案正确</td> <td>最后一张牌倒下后拿到答案</td> </tr> </tbody> </table> <p>①② 合起来就是归纳法：<strong>任意执行了 k 轮之后，不变量都成立。</strong> ③ 是单独的一步——循环若不终止，①② 只证明「已执行的轮次都对」，但你永远得不到答案。</p> <h3 id="更强的双栅栏不变量为什么必须有序">更强的双栅栏不变量：为什么必须有序</h3> <p>只用「答案 ∈ [left, right]」足够证明正确，但不够解释「为什么」。换成<strong>双栅栏不变量</strong>，答案一目了然：</p> <blockquote> <p><strong>左栅栏</strong>：所有 <code class="language-plaintext highlighter-rouge">i &lt; left</code> 的元素都 <code class="language-plaintext highlighter-rouge">&lt; target</code>（太小，已确定不是答案）<br/> <strong>右栅栏</strong>：所有 <code class="language-plaintext highlighter-rouge">i ≥ right</code> 的元素都 <code class="language-plaintext highlighter-rouge">≥ target</code>（够大，答案不可能超过 right）</p> </blockquote> <p>验证「保持」——注意每一步都<strong>必须用到数组递增</strong>：</p> <table> <thead> <tr> <th>分支</th> <th>对新区间的论证</th> <th>另一侧栅栏</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">nums[mid] ≥ target</code> → <code class="language-plaintext highlighter-rouge">right = mid</code></td> <td><code class="language-plaintext highlighter-rouge">nums[mid] ≥ target</code> + 递增 ⟹ <strong>mid 之后全 <code class="language-plaintext highlighter-rouge">≥ target</code></strong>，右栅栏建立</td> <td>左栅栏没动，仍成立</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">nums[mid] &lt; target</code> → <code class="language-plaintext highlighter-rouge">left = mid+1</code></td> <td><code class="language-plaintext highlighter-rouge">nums[mid] &lt; target</code> + 递增 ⟹ <strong>mid 及之前全 <code class="language-plaintext highlighter-rouge">&lt; target</code></strong>，左栅栏扩展覆盖 mid</td> <td>右栅栏没动，仍成立</td> </tr> </tbody> </table> <p><strong>这就是「必须有序」的根源</strong>：没有递增性，你无法从「<code class="language-plaintext highlighter-rouge">nums[mid] ≥ target</code>」推出「mid 后面全都 ≥ target」，两个栅栏当场塌掉。<strong>单调性不是二分的「使用条件」，而是这个证明的燃料。</strong></p> <p>验证「初始成立」也顺带成立：左栅栏 <code class="language-plaintext highlighter-rouge">∀i &lt; 0</code>、右栅栏 <code class="language-plaintext highlighter-rouge">∀i ≥ n</code> 都是空集，恒真。</p> <h3 id="-终止两个证据">③ 终止：两个证据</h3> <ol> <li><strong>循环必然结束</strong>：每轮区间长度 <code class="language-plaintext highlighter-rouge">right - left</code> 严格减小——分支 1 使 <code class="language-plaintext highlighter-rouge">right = mid &lt; right</code>，分支 2 使 <code class="language-plaintext highlighter-rouge">left = mid+1 &gt; left</code>。长度是正整数，不可能无限减小。这同时解释了模板二为什么死循环：<code class="language-plaintext highlighter-rouge">left = mid</code> 时 left 可能原地踏步。</li> <li><strong>结束时答案正确</strong>：退出时 <code class="language-plaintext highlighter-rouge">left == right = L</code>，不变量说答案 ∈ [left, right] = {L} ⟹ <strong>答案就是 L</strong>。</li> </ol> <h3 id="三件套的实战用法抓-bug-的位置">三件套的实战用法：抓 bug 的位置</h3> <p>版本 A（<code class="language-plaintext highlighter-rouge">right = n-1</code>）<strong>死在 ① 初始成立</strong>：它的右栅栏要求「<code class="language-plaintext highlighter-rouge">∀i ≥ n-1</code> 的元素 <code class="language-plaintext highlighter-rouge">≥ target</code>」，而当 <code class="language-plaintext highlighter-rouge">target &gt; nums[n-1]</code> 时这是假命题——不变量「出生即死」，之后怎么缩区间都救不回来。<strong>这就是为什么必须 <code class="language-plaintext highlighter-rouge">right = n</code>：不是题型特殊，而是让第 ① 步能成立。</strong></p> <h3 id="这个框架不止二分">这个框架不止二分</h3> <p>任意循环都能用同一套证明。最简单的例子——</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">sum</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="n">sum</span> <span class="o">+=</span> <span class="n">a</span><span class="p">[</span><span class="n">i</span><span class="p">];</span>
</code></pre></div></div> <ul> <li>① 初始：0 次迭代后 <code class="language-plaintext highlighter-rouge">sum = 0</code> = 空和 ✅</li> <li>② 保持：k 轮后 <code class="language-plaintext highlighter-rouge">sum = a[0]+…+a[k-1]</code>；第 k+1 轮加上 <code class="language-plaintext highlighter-rouge">a[k]</code> ✅</li> <li>③ 终止：n 轮后 <code class="language-plaintext highlighter-rouge">sum = a[0]+…+a[n-1]</code>，<code class="language-plaintext highlighter-rouge">i = n</code> 退出 ✅</li> </ul> <p>滑动窗口的 <code class="language-plaintext highlighter-rouge">[left, right)</code> 记法、这里的 <code class="language-plaintext highlighter-rouge">[left, right]</code> 不变量，都是同一套纪律：<strong>先定义「区间里是什么」，再让每一步维护它。</strong></p> <hr/> <h2 id="六️-隐藏边界-bugtarget-大于所有元素">六、⚠️ 隐藏边界 bug：target 大于所有元素</h2> <p><strong>问题出在 <code class="language-plaintext highlighter-rouge">right = nums.size() - 1</code>。</strong></p> <p>这个问题的答案域是 <strong><code class="language-plaintext highlighter-rouge">[0, n]</code></strong>——target 可能插在数组最后面（下标 n）。但 <code class="language-plaintext highlighter-rouge">right = n-1</code> 把答案域错误地限制成了 <code class="language-plaintext highlighter-rouge">[0, n-1]</code>。</p> <p>验证：<code class="language-plaintext highlighter-rouge">nums = [1,3,5,6]</code>, <code class="language-plaintext highlighter-rouge">target = 7</code></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>left=0, right=3 → mid=1 → 3&gt;=7? 否 → left=2
mid=2 → 5&gt;=7? 否 → left=3
left==right==3 → 返回 3  ❌（正确答案是 4）
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">nums=[1]</code>, <code class="language-plaintext highlighter-rouge">target=5</code> 同理：返回 0，正确答案 1。</p> <p><strong>根因</strong>：模板笔记里模板一的 right 是「答案的最大可能值」，而这里最大可能值是 <strong>n</strong> 不是 n-1。这属于「<strong>right 初始值设错</strong>」——速查表里常见错误的「left 初始值设错」的对偶版本。</p> <h3 id="修复-a标准做法stdlower_bound-的姿势半开区间">修复 A（标准做法，<code class="language-plaintext highlighter-rouge">std::lower_bound</code> 的姿势）：半开区间</h3> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">searchInsert</span><span class="p">(</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;&amp;</span> <span class="n">nums</span><span class="p">,</span> <span class="kt">int</span> <span class="n">target</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">left</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="n">right</span> <span class="o">=</span> <span class="n">nums</span><span class="p">.</span><span class="n">size</span><span class="p">();</span>   <span class="c1">// 关键：right 可以等于 n</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span><span class="p">)</span> <span class="p">{</span>
        <span class="kt">int</span> <span class="n">mid</span> <span class="o">=</span> <span class="n">left</span> <span class="o">+</span> <span class="p">(</span><span class="n">right</span> <span class="o">-</span> <span class="n">left</span><span class="p">)</span> <span class="o">/</span> <span class="mi">2</span><span class="p">;</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">mid</span><span class="p">]</span> <span class="o">&gt;=</span> <span class="n">target</span><span class="p">)</span> <span class="n">right</span> <span class="o">=</span> <span class="n">mid</span><span class="p">;</span>
        <span class="k">else</span> <span class="n">left</span> <span class="o">=</span> <span class="n">mid</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">left</span><span class="p">;</span>   <span class="c1">// left == right，范围 [0, n]，全对</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="修复-b保住原代码末尾补一行">修复 B（保住原代码，末尾补一行）</h3> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">left</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="n">right</span> <span class="o">=</span> <span class="n">nums</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span>
<span class="k">while</span> <span class="p">(</span><span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span><span class="p">)</span> <span class="p">{</span> <span class="cm">/* 原逻辑不变 */</span> <span class="p">}</span>
<span class="k">return</span> <span class="p">(</span><span class="n">left</span> <span class="o">&lt;</span> <span class="n">nums</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">&amp;&amp;</span> <span class="n">nums</span><span class="p">[</span><span class="n">left</span><span class="p">]</span> <span class="o">&gt;=</span> <span class="n">target</span><span class="p">)</span> <span class="o">?</span> <span class="n">left</span> <span class="o">:</span> <span class="n">left</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>
</code></pre></div></div> <h3 id="为什么标准库坚持半开区间-first-last">为什么标准库坚持半开区间 <code class="language-plaintext highlighter-rouge">[first, last)</code>？</h3> <p><strong>「插入位置」的语义天然属于半开区间——它可以是 last（尾部）。</strong> 闭区间 <code class="language-plaintext highlighter-rouge">[0, n-1]</code> 把「插到结尾」这种可能性掐死了。这是除了「<code class="language-plaintext highlighter-rouge">&lt;</code> vs <code class="language-plaintext highlighter-rouge">&lt;=</code>」之外二分的第三个维度：<strong>区间开闭性，以及它和答案域的关系。</strong> 判断方法：<strong>先想清楚答案的取值范围（含不含端点），再决定 left/right 的初值和区间的开闭。</strong></p> <hr/> <h2 id="七经典二分答案题套用">七、经典二分答案题套用</h2> <h3 id="1-koko-eating-bananaslc-875找最小速度">1. Koko Eating Bananas（LC 875）——找最小速度</h3> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">minEatingSpeed</span><span class="p">(</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;&amp;</span> <span class="n">piles</span><span class="p">,</span> <span class="kt">int</span> <span class="n">h</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">left</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>                                  <span class="c1">// 速度至少 1</span>
    <span class="kt">int</span> <span class="n">right</span> <span class="o">=</span> <span class="o">*</span><span class="n">max_element</span><span class="p">(</span><span class="n">piles</span><span class="p">.</span><span class="n">begin</span><span class="p">(),</span> <span class="n">piles</span><span class="p">.</span><span class="n">end</span><span class="p">());</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span><span class="p">)</span> <span class="p">{</span>
        <span class="kt">int</span> <span class="n">mid</span> <span class="o">=</span> <span class="n">left</span> <span class="o">+</span> <span class="p">(</span><span class="n">right</span> <span class="o">-</span> <span class="n">left</span><span class="p">)</span> <span class="o">/</span> <span class="mi">2</span><span class="p">;</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">f</span><span class="p">(</span><span class="n">piles</span><span class="p">,</span> <span class="n">mid</span><span class="p">)</span> <span class="o">&lt;=</span> <span class="n">h</span><span class="p">)</span> <span class="n">right</span> <span class="o">=</span> <span class="n">mid</span><span class="p">;</span>       <span class="c1">// 能吃完 → 试试更慢</span>
        <span class="k">else</span> <span class="n">left</span> <span class="o">=</span> <span class="n">mid</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>                       <span class="c1">// 吃不完 → 必须更快</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">left</span><span class="p">;</span>
<span class="p">}</span>
<span class="kt">long</span> <span class="nf">f</span><span class="p">(</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;&amp;</span> <span class="n">piles</span><span class="p">,</span> <span class="kt">int</span> <span class="n">x</span><span class="p">)</span> <span class="p">{</span>                <span class="c1">// 向上取整求耗时</span>
    <span class="kt">long</span> <span class="n">hours</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">p</span> <span class="o">:</span> <span class="n">piles</span><span class="p">)</span> <span class="n">hours</span> <span class="o">+=</span> <span class="p">(</span><span class="n">p</span> <span class="o">+</span> <span class="n">x</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)</span> <span class="o">/</span> <span class="n">x</span><span class="p">;</span>
    <span class="k">return</span> <span class="n">hours</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p><strong>常见错误</strong>：<code class="language-plaintext highlighter-rouge">left</code> 从 1 开始（速度至少 1）；<code class="language-plaintext highlighter-rouge">f(mid) &lt; h</code> 和 <code class="language-plaintext highlighter-rouge">f(mid) == h</code> 拆成两分支会写反——<strong>只要 <code class="language-plaintext highlighter-rouge">&lt;= h</code> 就统一 <code class="language-plaintext highlighter-rouge">right = mid</code></strong>。</p> <h3 id="2-capacity-to-ship-packageslc-1011找最小运载量">2. Capacity To Ship Packages（LC 1011）——找最小运载量</h3> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">shipWithinDays</span><span class="p">(</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;&amp;</span> <span class="n">weights</span><span class="p">,</span> <span class="kt">int</span> <span class="n">days</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">left</span> <span class="o">=</span> <span class="o">*</span><span class="n">max_element</span><span class="p">(</span><span class="n">weights</span><span class="p">.</span><span class="n">begin</span><span class="p">(),</span> <span class="n">weights</span><span class="p">.</span><span class="n">end</span><span class="p">());</span>  <span class="c1">// 必须 ≥ 最重包裹</span>
    <span class="kt">int</span> <span class="n">right</span> <span class="o">=</span> <span class="n">accumulate</span><span class="p">(</span><span class="n">weights</span><span class="p">.</span><span class="n">begin</span><span class="p">(),</span> <span class="n">weights</span><span class="p">.</span><span class="n">end</span><span class="p">(),</span> <span class="mi">0</span><span class="p">);</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span><span class="p">)</span> <span class="p">{</span>
        <span class="kt">int</span> <span class="n">mid</span> <span class="o">=</span> <span class="n">left</span> <span class="o">+</span> <span class="p">(</span><span class="n">right</span> <span class="o">-</span> <span class="n">left</span><span class="p">)</span> <span class="o">/</span> <span class="mi">2</span><span class="p">;</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">f</span><span class="p">(</span><span class="n">weights</span><span class="p">,</span> <span class="n">mid</span><span class="p">)</span> <span class="o">&lt;=</span> <span class="n">days</span><span class="p">)</span> <span class="n">right</span> <span class="o">=</span> <span class="n">mid</span><span class="p">;</span>   <span class="c1">// 天数够 → 试试更小船</span>
        <span class="k">else</span> <span class="n">left</span> <span class="o">=</span> <span class="n">mid</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">left</span><span class="p">;</span>
<span class="p">}</span>
<span class="kt">int</span> <span class="nf">f</span><span class="p">(</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;&amp;</span> <span class="n">weights</span><span class="p">,</span> <span class="kt">int</span> <span class="n">cap</span><span class="p">)</span> <span class="p">{</span>              <span class="c1">// 贪心：算需要几天</span>
    <span class="kt">int</span> <span class="n">dayCount</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">weights</span><span class="p">.</span><span class="n">size</span><span class="p">();</span> <span class="p">)</span> <span class="p">{</span>
        <span class="kt">int</span> <span class="n">remain</span> <span class="o">=</span> <span class="n">cap</span><span class="p">;</span>
        <span class="k">while</span> <span class="p">(</span><span class="n">i</span> <span class="o">&lt;</span> <span class="n">weights</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">&amp;&amp;</span> <span class="n">remain</span> <span class="o">&gt;=</span> <span class="n">weights</span><span class="p">[</span><span class="n">i</span><span class="p">])</span> <span class="p">{</span> <span class="n">remain</span> <span class="o">-=</span> <span class="n">weights</span><span class="p">[</span><span class="n">i</span><span class="p">];</span> <span class="n">i</span><span class="o">++</span><span class="p">;</span> <span class="p">}</span>
        <span class="n">dayCount</span><span class="o">++</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">dayCount</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p><strong>常见错误</strong>：<code class="language-plaintext highlighter-rouge">left</code> 必须是 <code class="language-plaintext highlighter-rouge">max(weights)</code> 不能是 1——运载量小于单个包裹时 <code class="language-plaintext highlighter-rouge">f</code> 里的 <code class="language-plaintext highlighter-rouge">i</code> 不前进，<strong>死循环</strong>。</p> <h3 id="3-split-array-largest-sumlc-410答案不在数组里">3. Split Array Largest Sum（LC 410）——答案不在数组里</h3> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">splitArray</span><span class="p">(</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;&amp;</span> <span class="n">nums</span><span class="p">,</span> <span class="kt">int</span> <span class="n">k</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">left</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="n">right</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">x</span> <span class="o">:</span> <span class="n">nums</span><span class="p">)</span> <span class="p">{</span> <span class="n">left</span> <span class="o">=</span> <span class="n">max</span><span class="p">(</span><span class="n">left</span><span class="p">,</span> <span class="n">x</span><span class="p">);</span> <span class="n">right</span> <span class="o">+=</span> <span class="n">x</span><span class="p">;</span> <span class="p">}</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span><span class="p">)</span> <span class="p">{</span>
        <span class="kt">int</span> <span class="n">mid</span> <span class="o">=</span> <span class="n">left</span> <span class="o">+</span> <span class="p">(</span><span class="n">right</span> <span class="o">-</span> <span class="n">left</span><span class="p">)</span> <span class="o">/</span> <span class="mi">2</span><span class="p">;</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">f</span><span class="p">(</span><span class="n">nums</span><span class="p">,</span> <span class="n">mid</span><span class="p">)</span> <span class="o">&lt;=</span> <span class="n">k</span><span class="p">)</span> <span class="n">right</span> <span class="o">=</span> <span class="n">mid</span><span class="p">;</span>
        <span class="k">else</span> <span class="n">left</span> <span class="o">=</span> <span class="n">mid</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">left</span><span class="p">;</span>
<span class="p">}</span>
<span class="kt">int</span> <span class="nf">f</span><span class="p">(</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;&amp;</span> <span class="n">nums</span><span class="p">,</span> <span class="kt">int</span> <span class="n">x</span><span class="p">)</span> <span class="p">{</span>                   <span class="c1">// 贪心：每段和 ≤ x 时最少几段</span>
    <span class="kt">int</span> <span class="n">segments</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span> <span class="kt">long</span> <span class="n">sum</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">num</span> <span class="o">:</span> <span class="n">nums</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">sum</span> <span class="o">+</span> <span class="n">num</span> <span class="o">&gt;</span> <span class="n">x</span><span class="p">)</span> <span class="p">{</span> <span class="n">segments</span><span class="o">++</span><span class="p">;</span> <span class="n">sum</span> <span class="o">=</span> <span class="n">num</span><span class="p">;</span> <span class="p">}</span>
        <span class="k">else</span> <span class="n">sum</span> <span class="o">+=</span> <span class="n">num</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">segments</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p><strong>注意</strong>：答案 <code class="language-plaintext highlighter-rouge">mid</code> <strong>不一定</strong>是数组中的任何元素或子数组和，它只是一个抽象的整数阈值。单调性来自验证函数本身：如果 <code class="language-plaintext highlighter-rouge">x=18</code> 能搞定（段数 <code class="language-plaintext highlighter-rouge">&lt;= k</code>），那么 <code class="language-plaintext highlighter-rouge">x=19,20...</code> 也一定能搞定。</p> <p><strong>三题的共同模式</strong>：<code class="language-plaintext highlighter-rouge">check(mid) &lt;= 限制</code> → <code class="language-plaintext highlighter-rouge">right = mid</code>（找最小）。换个角度，这三题都是对<strong>同一句问题</strong>的变体：「在最小可行值处，模拟能否达标」——这是二分答案题的通用模板。</p> <hr/> <h2 id="八做题流程">八、做题流程</h2> <ol> <li><strong>判断单调性</strong>：有序数组，或答案随参数单调。</li> <li><strong>找最小还是找最大</strong> → 选模板一还是模板二。</li> <li><strong>确定答案域</strong>：想清楚 <code class="language-plaintext highlighter-rouge">left</code>/<code class="language-plaintext highlighter-rouge">right</code> 的初值含不含端点（<strong>这步最容易被坑</strong>，searchInsert 就是例子）。</li> <li><strong>写 <code class="language-plaintext highlighter-rouge">check(mid)</code></strong>：满足条件返回 true。</li> <li><strong>套模板</strong>（注意模板二 mid 的 <code class="language-plaintext highlighter-rouge">+1</code>）。</li> </ol> <h2 id="九常见错误速查表">九、常见错误速查表</h2> <table> <thead> <tr> <th>错误</th> <th>原因</th> <th>修正</th> </tr> </thead> <tbody> <tr> <td>数组无序就二分</td> <td>前提是有序/单调</td> <td>先排序或确认单调性</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">left</code> 初始值设错</td> <td>答案有下界约束（运载量 ≥ max weight）</td> <td><code class="language-plaintext highlighter-rouge">left = max_element(...)</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">right</code> 初始值设错</td> <td>答案域上限算错（插入位置可到 n）</td> <td><code class="language-plaintext highlighter-rouge">right = n</code>（半开区间）</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">==</code> 分支单独处理</td> <td>把 <code class="language-plaintext highlighter-rouge">&lt;=</code> 拆成 <code class="language-plaintext highlighter-rouge">&lt;</code> 和 <code class="language-plaintext highlighter-rouge">==</code> 两个分支，逻辑写反</td> <td>合并成 <code class="language-plaintext highlighter-rouge">&lt;=</code>，统一 <code class="language-plaintext highlighter-rouge">right = mid</code></td> </tr> <tr> <td>找最大值忘给 mid 加 1</td> <td><code class="language-plaintext highlighter-rouge">left = mid</code> 导致死循环</td> <td><code class="language-plaintext highlighter-rouge">mid = (left + right + 1) / 2</code></td> </tr> <tr> <td>把 <code class="language-plaintext highlighter-rouge">f(mid)</code> 和 <code class="language-plaintext highlighter-rouge">mid</code> 搞混</td> <td>时间是速度的反函数，方向反了</td> <td>时间超 → 要更快/更大；时间够 → 慢/小</td> </tr> <tr> <td>认为答案必须在数组里</td> <td>二分答案是在整数范围找阈值</td> <td>答案可以是任意整数（410 题）</td> </tr> </tbody> </table> <hr/> <h2 id="一句话总结">一句话总结</h2> <blockquote> <p><strong>两个模板</strong>：找最小 → 满足 <code class="language-plaintext highlighter-rouge">check(mid)</code> 就 <code class="language-plaintext highlighter-rouge">right = mid</code>，不满足就 <code class="language-plaintext highlighter-rouge">left = mid + 1</code>；找最大 → 满足就 <code class="language-plaintext highlighter-rouge">left = mid</code>，不满足就 <code class="language-plaintext highlighter-rouge">right = mid - 1</code>，<strong>mid 记得 <code class="language-plaintext highlighter-rouge">+1</code></strong>。<br/> <strong>三个维度</strong>：<code class="language-plaintext highlighter-rouge">&lt;</code> vs <code class="language-plaintext highlighter-rouge">&lt;=</code>（重合 vs 交叉）、区间开闭（含不含端点）、模板选择（哪边用 <code class="language-plaintext highlighter-rouge">=</code>，mid 就往对面偏）。<br/> <strong>一道题</strong>：searchInsert = 模板一 + <code class="language-plaintext highlighter-rouge">check = nums[mid] &gt;= target</code> + <strong><code class="language-plaintext highlighter-rouge">right</code> 初始化为 <code class="language-plaintext highlighter-rouge">n</code></strong>。</p> </blockquote>]]></content><author><name></name></author><category term="LeetCode"/><category term="Algorithm"/><category term="leetcode"/><category term="binary-search"/><category term="template"/><category term="lower-bound"/><category term="search-insert"/><summary type="html"><![CDATA[二分查找双模板 + searchInsert 逐行拆解：从模板到边界]]></summary></entry><entry><title type="html">Agent Memory 全景：30 个记忆技术的模块化拆解</title><link href="https://leesanyee.me/blog/2026/Agent-Memory-30-techniques/" rel="alternate" type="text/html" title="Agent Memory 全景：30 个记忆技术的模块化拆解"/><published>2026-08-26T14:00:00+00:00</published><updated>2026-08-26T14:00:00+00:00</updated><id>https://leesanyee.me/blog/2026/Agent-Memory-30-techniques</id><content type="html" xml:base="https://leesanyee.me/blog/2026/Agent-Memory-30-techniques/"><![CDATA[<h1 id="agent-memory-全景30-个记忆技术的模块化拆解">Agent Memory 全景：30 个记忆技术的模块化拆解</h1> <blockquote> <p>基于 <a href="https://github.com/NirDiamant/Agent_Memory_Techniques">NirDiamant/Agent_Memory_Techniques</a> 的 30 个 notebook 整理。这是一篇「地图式」笔记——不求复述每个 notebook 的代码，只求把 30 个技术收敛成几条底层逻辑，以及它们各自的<strong>运作机制、公式和失败模式</strong>。</p> </blockquote> <h2 id="一句话理解">一句话理解</h2> <p><strong>Agent Memory = 分层存储 + 生命周期维护 + 混合检索。</strong> 30 个技术全是这三件事的变体。如果只记住一句话，记住上面这句。</p> <hr/> <h2 id="总纲30-个技术回答-5-个问题">总纲：30 个技术回答 5 个问题</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>存进去 ← 找回来 ← 维护 ← 组织 ← 评测
 (01-10)   (20)    (14-19)  (12/13)  (28/29)
</code></pre></div></div> <table> <thead> <tr> <th>模块</th> <th>编号</th> <th>回答的问题</th> </tr> </thead> <tbody> <tr> <td>短期记忆（上下文管理）</td> <td>01–05</td> <td>对话越来越长，token 装不下怎么办？</td> </tr> <tr> <td>长期记忆（知识化）</td> <td>06–10</td> <td>跨会话，怎么记住「关于世界的事实」？</td> </tr> <tr> <td>检索</td> <td>20（+06、17）</td> <td>存了几百万条，怎么快速找到对的那条？</td> </tr> <tr> <td>分层架构</td> <td>12、13、26</td> <td>记忆多了，怎么组织才不糊？</td> </tr> <tr> <td>生命周期维护</td> <td>14、15、18、19</td> <td>记忆重复、冲突、过时、爆炸怎么办？</td> </tr> <tr> <td>学习与进化</td> <td>11、16</td> <td>怎么让 agent 越用越好？</td> </tr> <tr> <td>多会话 / 多 agent</td> <td>21、22、23</td> <td>怎么跨会话、跨 agent 共享记忆？</td> </tr> <tr> <td>生产框架</td> <td>24–27</td> <td>别自己造轮子的成品有哪些？</td> </tr> <tr> <td>评测</td> <td>28、29</td> <td>怎么量化记忆系统好不好？</td> </tr> <tr> <td>生产工程</td> <td>30</td> <td>上生产还缺什么？</td> </tr> </tbody> </table> <hr/> <h2 id="模块-1--短期记忆上下文装不下怎么办0105">模块 1 · 短期记忆：上下文装不下怎么办（01–05）</h2> <p>核心问题：对话变长，token 爆了。五个技术是同一个问题的五种权衡——<strong>在「保真」和「成本」之间找分割点</strong>。</p> <table> <thead> <tr> <th>#</th> <th>技术</th> <th>本质</th> <th>取舍</th> </tr> </thead> <tbody> <tr> <td>01</td> <td>Conversation Buffer</td> <td>全量存，每轮重发</td> <td>零损失，但 token 线性增长、累计成本二次上升</td> </tr> <tr> <td>02</td> <td>Sliding Window</td> <td>只留最后 K 条，FIFO 淘汰</td> <td>有界成本，但硬遗忘、无优雅降级</td> </tr> <tr> <td>03</td> <td>Summary</td> <td>历史压成滚动摘要</td> <td>省 token，但 lossy</td> </tr> <tr> <td>04</td> <td><strong>Summary Buffer</strong></td> <td>近期原样 + 远期摘要</td> <td><strong>工业标准答案</strong></td> </tr> <tr> <td>05</td> <td>Token Buffer</td> <td>按 token 预算精确裁剪</td> <td>需要匹配的 tokenizer</td> </tr> </tbody> </table> <p><strong>记住</strong>：04（Summary Buffer）是标准解——最近 K 条消息逐字保留，更早的折进一个滚动摘要。TencentDB Agent Memory 的「符号化短时记忆」就是这个思想，只是把文字摘要换成了 Mermaid 符号画布。</p> <hr/> <h2 id="模块-2--长期记忆对话怎么变知识0610">模块 2 · 长期记忆：对话怎么变知识（06–10）</h2> <p>核心问题：跨会话，怎么记住「关于世界的事实」？这块最基础、也最容易搞混的是 <strong>Episodic 和 Semantic 的区分</strong>。</p> <table> <thead> <tr> <th>#</th> <th>技术</th> <th>本质</th> </tr> </thead> <tbody> <tr> <td>06</td> <td>Vector Store</td> <td>embed 成向量，按语义召回（基础设施）</td> </tr> <tr> <td>07</td> <td>Entity</td> <td>抽命名实体，维护每实体的 facts（像通讯录）</td> </tr> <tr> <td>08</td> <td>Knowledge Graph</td> <td>抽 (主,谓,宾) 三元组建图，支持多跳推理</td> </tr> <tr> <td>09</td> <td>Episodic</td> <td>存「整段经历」带时间戳</td> </tr> <tr> <td>10</td> <td>Semantic</td> <td>抽「无时间戳的永恒事实」+ 去重 + 矛盾检测</td> </tr> </tbody> </table> <h3 id="episodic09存整段经历">Episodic（09）：存「整段经历」</h3> <ul> <li><strong>数据模型</strong>：一个 episode = <code class="language-plaintext highlighter-rouge">{messages, 起止时间戳, summary, topic tags, embedding}</code>。存的是原始消息 + 摘要，不是抽出来的事实。</li> <li><strong>边界检测</strong>：决定一个 episode 在哪结束——会话式（显式 <code class="language-plaintext highlighter-rouge">end_session()</code>）、话题式（LLM 检测话题漂移）、时间式（固定时长）。</li> <li><strong>定稿</strong>：LLM 生成摘要 + 抽 topic tags + 对摘要算 embedding。摘要用于快速检索，原文用于下钻。</li> <li><strong>检索</strong>：综合分 = <code class="language-plaintext highlighter-rouge">α·语义相似度 + β·时间新鲜度</code>。把「意思相近」和「最近发生」两个信号融合。</li> </ul> <p>Episodic 回答的是 <strong>“what happened when”</strong>，适合审计、debug、”上次我们怎么做的”这类问题。失败模式：边界切太粗 → 一个 episode 塞进三个话题；切太细 → 一个任务碎成五段。</p> <h3 id="semantic10抽无时间戳的永恒事实">Semantic（10）：抽「无时间戳的永恒事实」</h3> <ul> <li><strong>事实抽取</strong>：LLM 扫消息，抽陈述性事实。”我上个月换 Mac 了” → <code class="language-plaintext highlighter-rouge">User uses macOS</code>。<strong>主动扔掉「何时说的」</strong>，只要事实本身。</li> <li><strong>去重判定（硬阈值）</strong>：cosine <strong>&gt; 0.85</strong> 判重复 → 不新增，给旧事实 confidence +1；<strong>0.5~0.85</strong> 之间 → 调 LLM 判断同义还是矛盾；<strong>&lt; 0.5</strong> → 新事实入库。</li> <li><strong>矛盾处理</strong>：新事实与旧事实冲突时，归档旧的、存新的（以新为准）。</li> <li><strong>置信度</strong>：每条事实带 confidence，反复提到加分。</li> </ul> <p>Semantic 回答的是 <strong>“what do I know”</strong>，适合紧凑召回、用户画像。失败模式：LLM 把「观点」当「事实」抽进来；没有时间衰减时旧事实永远占坑。</p> <h3 id="两者的关系">两者的关系</h3> <blockquote> <p><strong>Episodic 存「经历」，Semantic 存「结论」。</strong> 生产系统永远双轨并用：episode 保证据，semantic 保判断。二者加上 14（consolidation）就构成「episodic 捕获 → semantic 提取 → 周期性巩固」的三层栈，这是绝大多数生产记忆系统的骨架。</p> </blockquote> <hr/> <h2 id="模块-3--检索管线20怎么把对的记忆找回来">模块 3 · 检索管线（20）：怎么把对的记忆找回来</h2> <p>这是全库最技术的一块。核心结论：<strong>纯向量检索不够，生产系统用多级混合管线</strong>。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>query → ①embedding(语义) ─┐
       → ②BM25(关键词)  ──┼→ ③RRF 融合 → ④cross-encoder 重排 → ⑤MMR 去冗余 → 结果
                                           ↑
                                  ⑥HyDE(查询改写)
</code></pre></div></div> <h3 id="-embedding语义检索">① Embedding（语义检索）</h3> <p>把 query 和每条记忆都变成向量，算余弦相似度：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cos(θ) = (A·B) / (||A||·||B||)
</code></pre></div></div> <p>强在<strong>跨越用词差异</strong>——「怎么调试登录超时」和「auth 接口 600 秒报错」向量很近，即使一个词都不重复。弱在<strong>不认词</strong>：专业术语、人名、API 名这些低频但关键的词，embedding 模型可能没学过，导致漏召回。</p> <h3 id="-bm25关键词检索">② BM25（关键词检索）</h3> <p>经典的词频-逆文档频率模型：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>score(q,d) = Σ IDF(t) · (f(t,d)·(k1+1)) / (f(t,d) + k1·(1−b+b·|d|/avgdl))
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">IDF(t)</code>：词越稀有，权重越高。</li> <li><code class="language-plaintext highlighter-rouge">f(t,d)</code>：词在文档里的出现频率。</li> </ul> <p>强在<strong>精确词匹配</strong>（模型名、函数名、大小写敏感的词一击即中），弱在<strong>不懂同义</strong>（”登录” 和 “login” 是两个字面）。</p> <h3 id="-rrf-融合reciprocal-rank-fusion">③ RRF 融合（Reciprocal Rank Fusion）</h3> <p>把 ① 和 ② 的<strong>排名</strong>合并，而不是合并分数（两者分数量纲不同，不能直接加）：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>RRF_score(d) = Σ 1/(k + rank_r(d))    # k 通常取 60
</code></pre></div></div> <p><strong>为什么用排名不用分数</strong>：向量相似度是 0.9，BM25 是 4.7，直接相加无意义。排名是无量纲的，可以跨方法相加。这就是 TencentDB 配置里 <code class="language-plaintext highlighter-rouge">recall.strategy: "hybrid"</code> 的底层实现。</p> <h3 id="-cross-encoder-重排">④ Cross-encoder 重排</h3> <p>关键在于 <strong>bi-encoder vs cross-encoder</strong>：</p> <table> <thead> <tr> <th> </th> <th>bi-encoder（①②）</th> <th>cross-encoder（④）</th> </tr> </thead> <tbody> <tr> <td>怎么做</td> <td>query 和 doc <strong>分开</strong>各自 embed，再算相似度</td> <td>query 和 doc <strong>拼在一起</strong>喂进模型，直接输出相关分</td> </tr> <tr> <td>速度</td> <td>快（向量可预存）</td> <td>慢（每对都要过模型，50–200ms）</td> </tr> <tr> <td>精度</td> <td>粗</td> <td>准</td> </tr> </tbody> </table> <p>所以管线是两段式：①②③ 用便宜的 bi-encoder 从百万条里粗筛 top-20，④ 再用贵的 cross-encoder 精排这 20 条。<strong>「粗筛 + 精排」</strong>，和 Milvus GraphRAG 里的 CoT rerank 同一个思想。</p> <h3 id="-mmr-去冗余maximal-marginal-relevance">⑤ MMR 去冗余（Maximal Marginal Relevance）</h3> <p>解决「top-10 全是同一个意思的重复」：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>MMR = argmax [ λ·Sim(q,d) − (1−λ)·max_{d'∈已选} Sim(d,d') ]
</code></pre></div></div> <p>第一项要相关，第二项惩罚与已选结果的重复，<code class="language-plaintext highlighter-rouge">λ</code> 控制平衡（通常 0.5~0.7）。避免 token 浪费在冗余上。</p> <h3 id="-hyde查询改写">⑥ HyDE（查询改写）</h3> <p>解决「短 query vs 长记忆」的词表鸿沟：先让 LLM 生成一篇<strong>假设答案</strong>，再用这篇假设答案去 embed 和检索——长文本对长文本，相似度算得更准。核心洞察：用一个「不保证正确但语义对」的假设答案，代替真实 query 做向量匹配。</p> <p><strong>实践建议</strong>：最小可行是 <strong>①+②+③（hybrid）</strong>，④⑤⑥ 是进阶。跨编码器重排要额外的模型部署成本，别一上来全上。</p> <hr/> <h2 id="模块-4--分层架构记忆怎么组织121326">模块 4 · 分层架构：记忆怎么组织（12、13、26）</h2> <p>核心问题：记忆多了，怎么不糊成一团。</p> <table> <thead> <tr> <th>#</th> <th>技术</th> <th>本质</th> </tr> </thead> <tbody> <tr> <td>12</td> <td>Working Memory</td> <td>上下文内的「注意力管理」：salience 打分 + <strong>pin 固定</strong> + 动态淘汰（不是 FIFO）</td> </tr> <tr> <td>13</td> <td>Hierarchical Layers</td> <td>hot(L1 上下文) / warm(L2 向量库) / cold(L3 归档) 三层 + 自动晋升/降级</td> </tr> <tr> <td>26</td> <td>Letta/MemGPT</td> <td>OS 虚拟内存隐喻：core(常驻) / archival(可搜) / recall(全史)，agent 自己分页</td> </tr> </tbody> </table> <p><strong>记住</strong>：分层 = <strong>按访问频率分 tier + 逐级 drill-down</strong>，是 CPU cache 的隐喻，也是 TencentDB Agent Memory 的核心主张。12 的「pin」最实用——系统指令、用户偏好应永远钉在上下文，别被 FIFO 冲走。</p> <hr/> <h2 id="模块-5--生命周期记忆怎么维护14151819">模块 5 · 生命周期：记忆怎么维护（14、15、18、19）</h2> <p>核心问题：记忆会重复、冲突、过时、爆炸。四种武器，两两一组。</p> <h3 id="14-consolidation合并去冲突重算重要性">14 Consolidation：合并、去冲突、重算重要性</h3> <p>一个<strong>后台批处理</strong>（定时/达阈值时跑，不是在每次对话时跑）：</p> <ol> <li><strong>聚类</strong>：按语义相似度把相关记忆聚成一簇。</li> <li><strong>合并</strong>：簇内重复项合成一条更丰富的记忆。</li> <li><strong>解冲突</strong>：三条策略——<strong>以新为准</strong>（默认）、以置信度高为准、以用户确认为准。</li> <li><strong>重算重要性</strong>：按访问频率 + 新鲜度 + 结果好坏调整权重。</li> </ol> <p><strong>关键权衡</strong>：合并是<strong>不可逆的有损操作</strong>——合并完原始的五条就没了。所以 15 才强调「留原文」。</p> <h3 id="15-compaction渐进多级压缩但原文不丢">15 Compaction：渐进多级压缩，但原文不丢</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>L0 原文(全量) ──压缩──→ L1 要点 ──压缩──→ L2 一句话 ──压缩──→ L3 标签
        ↑冷存储保留                      ↑检索时按需选级
</code></pre></div></div> <p>日常检索用 L1/L2（信息密度高、省 token），需要精确细节时<strong>下钻回 L0 冷存储</strong>。宣称能把同样存储预算塞进 10–100 倍语义内容。</p> <p><strong>核心张力（storage-fidelity tradeoff）</strong>：压得越狠越省钱，但可能丢掉以后要用的细节。<strong>解法是「可逆性」</strong>——原文永远留在冷存储，压缩只是「前置的摘要」。</p> <h3 id="18-temporal检索时降权不删">18 Temporal：检索时降权，不删</h3> <p>给每条记忆挂三个时间戳——<code class="language-plaintext highlighter-rouge">created_at</code>、<code class="language-plaintext highlighter-rouge">last_access_at</code>、<code class="language-plaintext highlighter-rouge">event_time</code>（事件实际发生的时间）。</p> <ul> <li><strong>recency 加权</strong>：综合分 = <code class="language-plaintext highlighter-rouge">α·语义相似 + β·新鲜度</code>。</li> <li><strong>时间范围查询</strong>：”过去 24 小时发生了什么”。</li> <li><strong>“as of” 查询</strong>（最强大）：<strong>重建「某时刻我知道什么」</strong>——过滤掉该时间戳之后创建的记忆。</li> <li><strong>事件时间线</strong>：按 <code class="language-plaintext highlighter-rouge">event_time</code> 排序，还原因果链。</li> </ul> <h3 id="19-forgetting真的删">19 Forgetting：真的删</h3> <ol> <li><strong>指数衰减</strong>：<code class="language-plaintext highlighter-rouge">S(t) = S₀·e^(−λt)</code>，λ 越大忘得越快（艾宾浩斯遗忘曲线）。</li> <li><strong>访问强化（spaced repetition）</strong>：每次命中，<code class="language-plaintext highlighter-rouge">S(t)</code> 反弹回 <code class="language-plaintext highlighter-rouge">S₀</code>——常用记忆长生不老。</li> <li><strong>剪枝阈值</strong>：<code class="language-plaintext highlighter-rouge">S(t)</code> 跌破阈值 → 删除或归档。</li> <li><strong>生命周期</strong>：创建 → 活跃使用 → 衰减 → 剪枝/归档。</li> </ol> <h3 id="18-和-19-的实战区别同一场景">18 和 19 的实战区别（同一场景）</h3> <blockquote> <p>用户 3 月说”住在纽约”，6 月说”搬到洛杉矶”。</p> <ul> <li><strong>18</strong>：两条都留，检索时洛杉矶（更新鲜）排前面；”我 4 月住哪？” → as-of 查询回答纽约。</li> <li><strong>19</strong>：纽约那条衰减、跌破阈值后被<strong>剪枝删除</strong>；4 月的状态无法重建。</li> </ul> </blockquote> <p><strong>什么时候用哪个</strong>：需要审计/回溯 → 用 18；存储爆炸、成本敏感 → 加 19。<strong>先上 18 更安全</strong>——不丢数据、可回退。</p> <hr/> <h2 id="模块-6--学习与进化1116">模块 6 · 学习与进化（11、16）</h2> <table> <thead> <tr> <th>#</th> <th>技术</th> <th>本质</th> </tr> </thead> <tbody> <tr> <td>11</td> <td>Procedural</td> <td>存「怎么做」的可复用工作流（skill library），不是「发生了什么」</td> </tr> <tr> <td>16</td> <td>Self-Reflection</td> <td>任务后自省「为何成/败」，存 lesson 供未来检索（Reflexion 框架）</td> </tr> </tbody> </table> <ul> <li><strong>11 Procedural</strong>：从一次成功执行里剥离「可复用动作序列」，<strong>泛化</strong>（具体值换成占位符——”部署 app X 到 server Y” → “部署 {app} 到 {server}”），存进 skill library。标杆案例是 Voyager（Minecraft agent 写可复用技能）。</li> <li><strong>16 Self-Reflection</strong>：任务结束后三问——”什么做对了？什么做错了？下次怎么做？”，蒸馏成可复用原则入库，下次任务前检索注入。<strong>核心价值：不用重训模型就能让 agent 变好</strong>——用语言反馈替代梯度更新。</li> </ul> <p><strong>和 09/10 的区别</strong>：09 存”发生了什么”，10 存”事实是什么”，11 存”怎么把它做成”，16 存”上次为什么成/败”。这也正是 TencentDB roadmap 里「Skill generation」要做而还没做的方向。</p> <hr/> <h2 id="模块-7--多会话--多-agent--agent-自主212223">模块 7 · 多会话 / 多 agent / agent 自主（21、22、23）</h2> <table> <thead> <tr> <th>#</th> <th>技术</th> <th>本质</th> </tr> </thead> <tbody> <tr> <td>21</td> <td>Cross-Session</td> <td>序列化 + 按 user ID 持久化 + 冷启动</td> </tr> <tr> <td>22</td> <td>Multi-Agent Shared</td> <td>共享黑板 + namespace 分区 + 冲突解决（乐观锁）</td> </tr> <tr> <td>23</td> <td>Memory-as-a-Tool</td> <td>把 CRUD 暴露成工具，agent 自己决定存/查/改/删</td> </tr> </tbody> </table> <ul> <li><strong>21 Cross-Session</strong>：会话结束序列化存盘 → 按 user ID 分片 → 冷启动给模板/问卷 → 记忆大了<strong>只加载「最近 N 条 + 最重要 M 条」</strong>，不全量加载。</li> <li><strong>22 Multi-Agent Shared</strong>：经典 blackboard 架构。关键设计是 <strong>私有 scratchpad</strong>——agent 的草稿只自己可见，<strong>只有成品才写入共享区</strong>，防止半成品污染公共空间。冲突解决用 last-write-wins 或乐观锁（带版本号写，冲突重试）。</li> <li><strong>23 Memory-as-a-Tool</strong>：把 <code class="language-plaintext highlighter-rouge">save_memory</code> / <code class="language-plaintext highlighter-rouge">search_memory</code> / <code class="language-plaintext highlighter-rouge">update_memory</code> / <code class="language-plaintext highlighter-rouge">delete_memory</code> 做成 4 个 tool，agent 在正常的 tool-use 循环里决定何时调用。<strong>这是「显式工具」vs「隐式管线」两种哲学的分野</strong>——前者透明可控可审计，后者（如 Mem0 自动提取）省心但黑盒。</li> </ul> <hr/> <h2 id="模块-8--生产框架2427别自己造轮子的四家成品">模块 8 · 生产框架（24–27）：「别自己造轮子」的四家成品</h2> <table> <thead> <tr> <th>框架</th> <th>本质</th> <th>底层</th> </tr> </thead> <tbody> <tr> <td>24 Graphiti</td> <td>时序知识图谱，每条边带时间戳</td> <td>Neo4j，自托管</td> </tr> <tr> <td>25 Mem0</td> <td>托管记忆层，自动提取 + 去重</td> <td>语义记忆为核心</td> </tr> <tr> <td>26 Letta/MemGPT</td> <td>分层记忆，agent 自己分页</td> <td>core / archival / recall</td> </tr> <tr> <td>27 Zep</td> <td>全自动：实体 + 事实 + 时序图，异步</td> <td>Graphiti 加持</td> </tr> </tbody> </table> <p><strong>记住</strong>：这四家 = 模块 2–5 概念的<strong>成品封装</strong>。Mem0 = 语义记忆，Zep/Graphiti = 图，Letta = 分层。TencentDB Agent Memory 是第 5 家，差异化在「团队级共享 + 符号化短时记忆」。</p> <hr/> <h2 id="模块-9--评测2829">模块 9 · 评测（28、29）</h2> <table> <thead> <tr> <th>#</th> <th>技术</th> <th>本质</th> </tr> </thead> <tbody> <tr> <td>28</td> <td>Evaluation</td> <td>自建数据集，指标 = precision/recall/staleness/contradiction + LLM-as-judge</td> </tr> <tr> <td>29</td> <td>LoCoMo/LongMemEval</td> <td>标准基准：单跳/多跳/时间/开放/对抗五类题</td> </tr> </tbody> </table> <p><strong>四个指标（记牢，多数人只测前两个）：</strong></p> <ul> <li><strong>precision = TP/(TP+FP)</strong>：召回的记忆里有多少相关？（找得准不准）</li> <li><strong>recall = TP/(TP+FN)</strong>：所有相关记忆里找回多少？（找得全不全）</li> <li><strong>staleness</strong>：agent 是否用了<strong>过时</strong>数据？← <strong>记忆系统真正的死穴</strong></li> <li><strong>contradiction</strong>：库里是否存了<strong>矛盾</strong>事实，能否检测/解决？</li> </ul> <p><strong>29 标准基准</strong>：LoCoMo 有 10 段多会话对话、约 2000 个 QA 对，分五类（单跳/多跳/时间/开放/对抗），用 BLEU / ROUGE-L / token F1 / LLM judge 打分，跑一轮带 LLM judge 约 $5–15。关键做法是<strong>跑「无记忆 baseline」做对照</strong>，量化检索到底加了多少价值。</p> <hr/> <h2 id="模块-10--生产工程30">模块 10 · 生产工程（30）</h2> <p>caching(Redis) / TTL / 水平分片 / 备份恢复 / <strong>GDPR 删除（连 embedding、图节点一起删）</strong> / 可观测性 / 成本管理。</p> <hr/> <h2 id="收束核心心智模型--实践清单">收束：核心心智模型 + 实践清单</h2> <p>30 个技术最终收敛成 5 条底层逻辑：</p> <ol> <li><strong>上下文窗口是稀缺资源</strong>——记忆的本质是「换出 + 换回」。</li> <li><strong>存储是分层的，检索是混合的</strong>——拒绝扁平向量堆。</li> <li><strong>记忆有生命周期</strong>——去重、解冲突、让旧知识失效。</li> <li><strong>谁来管记忆有两种范式</strong>——agent 显式工具 vs 系统隐式管线。</li> <li><strong>评测是硬指标</strong>——重点看 staleness 和 contradiction，不只是 precision/recall。</li> </ol> <p><strong>最小阅读路径（只看 6 个就够）</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>04 → 10 → 20 → 14 → 19 → 28
</code></pre></div></div> <p>这条链 = 短期分割点 → 长期事实抽取 → 混合检索 → 维护 → 遗忘 → 评测，覆盖全部 5 条底层逻辑，其余 24 个是变体。</p> <p><strong>给自建记忆系统的四步清单</strong>（对应四个最常见的短板）：</p> <table> <thead> <tr> <th>短板</th> <th>解法</th> <th>最小可行</th> </tr> </thead> <tbody> <tr> <td>只有人工导航，没有语义召回</td> <td>06 + 20</td> <td>向量 + BM25 + RRF（hybrid）</td> </tr> <tr> <td>重复、冲突、不可逆合并</td> <td>14 + 15</td> <td>三策略解冲突 + 留原文</td> </tr> <tr> <td>旧结论永远占坑</td> <td>18 + 19</td> <td>先上 18（recency 加权，不删数据）</td> </tr> <tr> <td>无法量化好坏</td> <td>28</td> <td>precision/recall + staleness</td> </tr> </tbody> </table> <hr/> <h2 id="参考">参考</h2> <ul> <li><a href="https://github.com/NirDiamant/Agent_Memory_Techniques">NirDiamant/Agent_Memory_Techniques</a></li> <li><a href="https://github.com/TencentCloud/TencentDB-Agent-Memory">TencentCloud/TencentDB-Agent-Memory</a></li> <li><a href="https://github.com/getzep/graphiti">Graphiti</a>、<a href="https://github.com/mem0ai/mem0">Mem0</a>、<a href="https://github.com/letta-ai/letta">Letta/MemGPT</a>、<a href="https://github.com/getzep/zep">Zep</a></li> <li>Shinn et al., “Reflexion: Language Agents with Verbal Reinforcement Learning,” 2023</li> <li>Wang et al., “Voyager: An Open-Ended Embodied Agent with Large Language Models,” 2023</li> </ul>]]></content><author><name></name></author><category term="Agent"/><category term="Memory"/><category term="agent"/><category term="memory"/><category term="rag"/><category term="llm"/><category term="vector-store"/><category term="knowledge-graph"/><category term="retrieval"/><category term="consolidation"/><category term="evaluation"/><summary type="html"><![CDATA[Agent Memory 全景：30 个记忆技术的模块化拆解]]></summary></entry><entry><title type="html">LightRAG 深度解析：简单快速的图增强 RAG</title><link href="https://leesanyee.me/blog/2026/lightrag-deep-dive/" rel="alternate" type="text/html" title="LightRAG 深度解析：简单快速的图增强 RAG"/><published>2026-08-25T14:00:00+00:00</published><updated>2026-08-25T14:00:00+00:00</updated><id>https://leesanyee.me/blog/2026/lightrag-deep-dive</id><content type="html" xml:base="https://leesanyee.me/blog/2026/lightrag-deep-dive/"><![CDATA[<h1 id="lightrag-深度解析简单快速的图增强-rag">LightRAG 深度解析：简单快速的图增强 RAG</h1> <blockquote> <p>基于论文 <em>LightRAG: Simple and Fast Retrieval-Augmented Generation</em>（arXiv:2410.05779v3）及延伸讨论整理。</p> </blockquote> <h2 id="一句话理解">一句话理解</h2> <p>LightRAG 是 GraphRAG 的「轻量版」：同样把文档建成知识图谱来增强检索，但<strong>砍掉了 GraphRAG 最贵的社区报告机制</strong>，改用「检索时动态关联」替代「索引时静态聚合」，从而换来极低的检索成本和真正的增量更新能力。</p> <hr/> <h2 id="1-解决什么痛点">1. 解决什么痛点？</h2> <p>传统 RAG 把文档切成 chunk 后做向量检索，三大局限：</p> <table> <thead> <tr> <th>局限</th> <th>说明</th> <th>示例</th> </tr> </thead> <tbody> <tr> <td><strong>扁平化数据表示</strong></td> <td>无法理解实体间的复杂依赖关系</td> <td>只能分别检索「电动汽车」「空气污染」「公共交通」，无法关联三者</td> </tr> <tr> <td><strong>上下文感知不足</strong></td> <td>难以跨实体保持连贯性</td> <td>答案碎片化，无法解释「电动汽车→空气质量→交通规划」的连锁影响</td> </tr> <tr> <td><strong>检索效率低</strong></td> <td>需遍历大量文本块或社区报告</td> <td>响应慢、Token 消耗高</td> </tr> </tbody> </table> <p>LightRAG 的三大设计目标：<strong>全面信息检索</strong>、<strong>高效低成本检索</strong>、<strong>快速适应新数据（增量更新）</strong>。</p> <hr/> <h2 id="2-整体架构">2. 整体架构</h2> <p>三个阶段：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>原始文档 D
    │
    ▼
分块 (Chunking)
    │
    ├──► 实体与关系提取 R(·) ──► 实体集 V, 关系集 E
    │
    ├──► LLM 分析 P(·) ───────► 键值对 (K, V)   Key=检索关键词, Value=描述+原文摘要
    │
    └──► 去重 D(·) ───────────► 合并重复实体/关系 → 知识图谱 D̂ = (V̂, Ê)
</code></pre></div></div> <ul> <li><strong>R(·) 提取实体和关系</strong>：LLM 从文本中识别实体（节点）和关系（边）</li> <li><strong>P(·) 生成 KV 对</strong>：实体以实体名作为唯一 Key；关系以多个全局主题词作为 Key（如 “Agriculture”, “Production”）</li> <li><strong>D(·) 去重</strong>：合并跨文档的重复实体和关系，减小图规模</li> </ul> <hr/> <h2 id="3-核心创新双层检索范式">3. 核心创新：双层检索范式</h2> <p>双层检索是 LightRAG 区别于 GraphRAG 的灵魂，流程：<strong>分层关键词提取 → 向量分层匹配 → 子图邻居扩展</strong>。</p> <h3 id="31-查询关键词分层提取">3.1 查询关键词分层提取</h3> <p>面对查询，LLM 提取两类关键词（输出 JSON，含 <code class="language-plaintext highlighter-rouge">high_level_keywords</code> 和 <code class="language-plaintext highlighter-rouge">low_level_keywords</code> 两个字段）：</p> <table> <thead> <tr> <th>类型</th> <th>定义</th> <th>示例（”哪些指标对评估电影推荐系统最有信息量？”）</th> </tr> </thead> <tbody> <tr> <td><strong>局部关键词</strong> k^(l)</td> <td>具体实体、细节、术语</td> <td>“Accuracy”, “Precision”, “Recall”, “F1 score”</td> </tr> <tr> <td><strong>全局关键词</strong> k^(g)</td> <td>宏观概念、主题</td> <td>“Metrics”, “Movie recommendation systems”, “Evaluation methods”</td> </tr> </tbody> </table> <h3 id="32-分层向量匹配">3.2 分层向量匹配</h3> <table> <thead> <tr> <th> </th> <th>低级检索 (Low-Level)</th> <th>高级检索 (High-Level)</th> </tr> </thead> <tbody> <tr> <td><strong>目标</strong></td> <td>精确定位特定实体及属性/关系</td> <td>捕获跨实体的宏观主题</td> </tr> <tr> <td><strong>匹配方式</strong></td> <td>局部关键词 ↔ <strong>实体节点</strong>向量</td> <td>全局关键词 ↔ <strong>关系边</strong>向量</td> </tr> <tr> <td><strong>关键设计</strong></td> <td>—</td> <td>关系边索引时由 LLM 生成多个「全局主题」索引键，使其能被抽象概念检索到</td> </tr> <tr> <td><strong>返回</strong></td> <td>实体名称、描述、类型、关联原文块</td> <td>关系描述、关联实体概要、跨文档综合信息</td> </tr> </tbody> </table> <h3 id="33-高阶关联扩展子图邻居收集">3.3 高阶关联扩展（子图邻居收集）</h3> <p>初步匹配后自动拉取<strong>一跳邻居</strong>增强关联性：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>{v_i | v_i ∈ V ∧ (v_i ∈ N_v ∨ v_i ∈ N_e)}
</code></pre></div></div> <p>即使查询只匹配到某个实体，系统也会自动补充与其直接相连的其他实体和关系，避免答案孤立。</p> <h3 id="34-检索结果整合">3.4 检索结果整合</h3> <p>检索到的信息组织为三个组件输入 LLM：<strong>Entities</strong>（实体+描述）、<strong>Relationships</strong>（关系+描述）、<strong>Sources</strong>（原文片段）。</p> <hr/> <h2 id="4-增量更新机制lightrag-为什么能graphrag-为什么不能">4. 增量更新机制：LightRAG 为什么能、GraphRAG 为什么不能</h2> <h3 id="41-lightrag直接并集合并">4.1 LightRAG：直接并集合并</h3> <p>LightRAG 的索引是<strong>去中心化的键值对集合</strong>，没有全局聚合层：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>新文档 D' ──► 同样的图索引流程 ──► 新实体 V̂' + 新关系 Ê'
                                        │
                                        ▼
                              与原图做并集：V̂ ∪ V̂', Ê ∪ Ê'
</code></pre></div></div> <ul> <li>新增实体/关系只是<strong>局部节点扩充</strong>，不影响已有节点</li> <li>已有实体再次出现时，去重模块 D(·) 自动合并</li> <li>向量库直接追加新嵌入，原有索引不动</li> </ul> <h3 id="42-graphrag必须完全重建">4.2 GraphRAG：必须完全重建</h3> <p>GraphRAG 的核心是<strong>社区报告 (Community Report)</strong> 机制，这是它无法增量更新的根源：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>原始文本 → 实体/关系提取 → 知识图谱 → 社区检测 (Leiden) → 为每个社区生成报告
</code></pre></div></div> <p>增量更新的灾难：新实体可能连接多个已有社区导致<strong>社区边界失效</strong>；新信息改变社区内实体语义导致<strong>报告失效</strong>；于是必须拆解社区 → 重新聚类 → 重新生成所有报告。</p> <h3 id="43-成本对比legal-数据集1399-个社区">4.3 成本对比（Legal 数据集，1,399 个社区）</h3> <table> <thead> <tr> <th>阶段</th> <th>GraphRAG</th> <th>LightRAG</th> </tr> </thead> <tbody> <tr> <td><strong>检索阶段</strong></td> <td>610,000 tokens + 数百次 API 调用</td> <td>&lt;100 tokens + 1 次 API 调用</td> </tr> <tr> <td><strong>增量更新</strong></td> <td>~14,000,000 tokens（重建社区报告）</td> <td>仅新文档提取开销</td> </tr> </tbody> </table> <h3 id="44-本质设计目标的取舍">4.4 本质：设计目标的取舍</h3> <ul> <li><strong>GraphRAG</strong> 为「全局理解」牺牲增量能力：社区报告与全库数据强耦合</li> <li><strong>LightRAG</strong> 用「检索时动态关联」替代「索引时静态聚合」：高级查询靠「关系全局关键词匹配 + 子图扩展」实现，无需预计算全局摘要</li> </ul> <hr/> <h2 id="5-图存储结构深度解析">5. 图存储结构深度解析</h2> <h3 id="51-逻辑存储模型key-value--邻接表">5.1 逻辑存储模型：Key-Value + 邻接表</h3> <p><strong>节点（实体）存储</strong>——实体只有<strong>一个索引键</strong>（实体名）：</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"key"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Beekeeper"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"value"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"entity_type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"PERSON"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A Beekeeper is an individual who produces honey..."</span><span class="p">,</span><span class="w">
    </span><span class="nl">"original_chunks"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"chunk_id_xxx"</span><span class="p">,</span><span class="w"> </span><span class="s2">"chunk_id_yyy"</span><span class="p">],</span><span class="w">
    </span><span class="nl">"vector_embedding"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="mf">0.23</span><span class="p">,</span><span class="w"> </span><span class="mf">-0.15</span><span class="p">,</span><span class="w"> </span><span class="mf">0.88</span><span class="p">,</span><span class="w"> </span><span class="err">...</span><span class="p">]</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p><strong>边（关系）存储</strong>——关系可以有<strong>多个索引键</strong>（LLM 生成的高层主题词）：</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"keys"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"Agriculture"</span><span class="p">,</span><span class="w"> </span><span class="s2">"Production"</span><span class="p">,</span><span class="w"> </span><span class="s2">"Environmental Impact"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"value"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"source_entity"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Beekeeper"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"target_entity"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Honey Bee"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"relationship_description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A Beekeeper observes bees to manage..."</span><span class="p">,</span><span class="w">
    </span><span class="nl">"relationship_strength"</span><span class="p">:</span><span class="w"> </span><span class="mf">0.85</span><span class="p">,</span><span class="w">
    </span><span class="nl">"original_chunks"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"chunk_id_xxx"</span><span class="p">],</span><span class="w">
    </span><span class="nl">"vector_embedding"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">]</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p><strong>关键设计</strong>：实体一个键、关系多个键，使关系既能被具体实体检索，也能被抽象概念检索。</p> <h3 id="52-图拓扑的存储邻接表--图数据库">5.2 图拓扑的存储：邻接表 / 图数据库</h3> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">"Beekeeper"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
  </span><span class="nl">"outgoing_edges"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="nl">"target"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Honey Bee"</span><span class="p">,</span><span class="w"> </span><span class="nl">"relation_key"</span><span class="p">:</span><span class="w"> </span><span class="s2">"observe"</span><span class="p">,</span><span class="w"> </span><span class="nl">"edge_id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"e_001"</span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="nl">"target"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Hive"</span><span class="p">,</span><span class="w"> </span><span class="nl">"relation_key"</span><span class="p">:</span><span class="w"> </span><span class="s2">"manage"</span><span class="p">,</span><span class="w"> </span><span class="nl">"edge_id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"e_002"</span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>或用 Neo4j / NebulaGraph：节点标签 <code class="language-plaintext highlighter-rouge">Entity</code>、边类型 <code class="language-plaintext highlighter-rouge">RELATION</code>，原生支持 <code class="language-plaintext highlighter-rouge">MATCH (n)-[r]-&gt;(m)</code> 遍历。</p> <h3 id="53-向量索引的存储">5.3 向量索引的存储</h3> <table> <thead> <tr> <th>存储对象</th> <th>向量来源</th> <th>用途</th> </tr> </thead> <tbody> <tr> <td>实体向量</td> <td>实体名称 + 描述文本的 Embedding</td> <td>匹配局部关键词（具体实体）</td> </tr> <tr> <td>关系向量</td> <td>关系描述 + 全局关键词的 Embedding</td> <td>匹配全局关键词（抽象主题）</td> </tr> </tbody> </table> <h3 id="54-与算法题图结构的对比">5.4 与算法题图结构的对比</h3> <p>拓扑层完全等价，但图 RAG 是「重量级超集」：</p> <table> <thead> <tr> <th>维度</th> <th>算法题图</th> <th>图 RAG</th> </tr> </thead> <tbody> <tr> <td><strong>节点内容</strong></td> <td>一个整数 ID</td> <td>实体名称 + 类型 + 描述 + 向量 + 原文出处</td> </tr> <tr> <td><strong>边内容</strong></td> <td>可能只有权重 w</td> <td>关系描述 + 强度 + 全局关键词 + 向量 + 原文块</td> </tr> <tr> <td><strong>存储目的</strong></td> <td>跑 DFS/BFS/最短路</td> <td>支持语义检索 + 子图扩展 + LLM 生成</td> </tr> <tr> <td><strong>存储位置</strong></td> <td>内存数组</td> <td>磁盘/数据库</td> </tr> <tr> <td><strong>邻居遍历</strong></td> <td>CPU 内存访问</td> <td>可能涉及磁盘 I/O 或网络请求</td> </tr> </tbody> </table> <p><strong>为什么用邻接表不用邻接矩阵？</strong> 和算法题一样——知识图谱是<strong>稀疏图</strong>：10,000 个实体每个平均只连 5-10 个，邻接矩阵需 1 亿单元格、99.9% 为空，邻接表只需存 5-10 万条边。</p> <h3 id="55-工程上的存储形态">5.5 工程上的存储形态</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">LightRAGStorage</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">entities</span> <span class="o">=</span> <span class="p">{}</span>               <span class="c1"># KV：name -&gt; Entity
</span>        <span class="n">self</span><span class="p">.</span><span class="n">relationships</span> <span class="o">=</span> <span class="p">{}</span>          <span class="c1"># KV：edge_key -&gt; Relation
</span>        <span class="n">self</span><span class="p">.</span><span class="n">vector_db</span> <span class="o">=</span> <span class="nc">NanoVectorDB</span><span class="p">()</span>  <span class="c1"># 向量索引
</span>        <span class="n">self</span><span class="p">.</span><span class="n">graph</span> <span class="o">=</span> <span class="n">nx</span><span class="p">.</span><span class="nc">Graph</span><span class="p">()</span>          <span class="c1"># 邻接表（NetworkX）
</span>
    <span class="k">def</span> <span class="nf">add_entity</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">entity</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">entities</span><span class="p">[</span><span class="n">entity</span><span class="p">.</span><span class="n">name</span><span class="p">]</span> <span class="o">=</span> <span class="n">entity</span>
        <span class="n">self</span><span class="p">.</span><span class="n">vector_db</span><span class="p">.</span><span class="nf">add</span><span class="p">(</span><span class="n">entity</span><span class="p">.</span><span class="n">vector</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">graph</span><span class="p">.</span><span class="nf">add_node</span><span class="p">(</span><span class="n">entity</span><span class="p">.</span><span class="n">name</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">add_relation</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">rel</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">relationships</span><span class="p">[</span><span class="n">rel</span><span class="p">.</span><span class="n">key</span><span class="p">]</span> <span class="o">=</span> <span class="n">rel</span>
        <span class="n">self</span><span class="p">.</span><span class="n">vector_db</span><span class="p">.</span><span class="nf">add</span><span class="p">(</span><span class="n">rel</span><span class="p">.</span><span class="n">vector</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">graph</span><span class="p">.</span><span class="nf">add_edge</span><span class="p">(</span><span class="n">rel</span><span class="p">.</span><span class="n">source</span><span class="p">,</span> <span class="n">rel</span><span class="p">.</span><span class="n">target</span><span class="p">)</span>
</code></pre></div></div> <p><strong>类比</strong>：图 RAG 存储 = 「带档案柜的社交网络」——邻接表是通讯录（只记认识谁），KV 是档案袋（姓名/职业/简介），向量库是按相似度排序的索引卡（只看 Embedding 像不像）。</p> <hr/> <h2 id="6-与-graphrag-的对比总结">6. 与 GraphRAG 的对比总结</h2> <table> <thead> <tr> <th>维度</th> <th>LightRAG</th> <th>GraphRAG</th> </tr> </thead> <tbody> <tr> <td><strong>索引结构</strong></td> <td>去中心化 KV 对 + 向量 + 邻接表</td> <td>知识图谱 + 层级社区 + 社区报告</td> </tr> <tr> <td><strong>全局信息获取</strong></td> <td>双层检索 + 子图扩展（动态）</td> <td>社区报告摘要（静态预计算）</td> </tr> <tr> <td><strong>检索 Token</strong></td> <td>&lt;100</td> <td>~610,000</td> </tr> <tr> <td><strong>检索 API 调用</strong></td> <td>1 次</td> <td>数百次</td> </tr> <tr> <td><strong>增量更新</strong></td> <td>直接并集合并，低成本</td> <td>必须重建社区结构，高成本</td> </tr> <tr> <td><strong>优势场景</strong></td> <td>动态数据、高频查询、成本敏感</td> <td>超大规模静态语料、深度全局摘要</td> </tr> <tr> <td><strong>核心权衡</strong></td> <td>用「检索时动态关联」换增量能力</td> <td>用「索引时静态聚合」换全局理解深度</td> </tr> </tbody> </table> <hr/> <h2 id="核心-insight">核心 Insight</h2> <p>LightRAG 的本质是把 GraphRAG 的「<strong>索引时静态聚合</strong>（社区报告）」替换成「<strong>检索时动态关联</strong>（双层关键词 + 子图扩展）」。</p> <p>这是一个通用的工程权衡：</p> <ul> <li><strong>静态聚合</strong>：一次算好、查询快，但数据一变就全废（GraphRAG 社区报告）</li> <li><strong>动态关联</strong>：查询时现算、无全局耦合，数据随便增删（LightRAG 子图扩展）</li> </ul> <p>选择取决于数据是「静态语料」还是「动态增量」——而绝大多数真实场景是后者，这正是 LightRAG 的实用价值所在。</p> <h2 id="相关技术">相关技术</h2> <ul> <li>[[microsoft-graphrag-summary]]：GraphRAG 的社区报告机制（LightRAG 的对立面）</li> <li>[[GraphRAG-with-Milvus]]：纯向量库造图的多跳推理，与 LightRAG 同属「图增强检索」</li> <li>[[self-rag]]：另一条「轻量化 RAG」路线（反思式生成）</li> </ul> <h2 id="参考">参考</h2> <ul> <li>Guo, Z., Xia, L., Yu, Y., Ao, T., &amp; Huang, C. (2025). <em>LightRAG: Simple and Fast Retrieval-Augmented Generation</em>. arXiv:2410.05779v3 [cs.IR].</li> <li>Edge, D., et al. (2024). <em>From Local to Global: A Graph RAG Approach to Query-Focused Summarization</em>.</li> </ul>]]></content><author><name></name></author><category term="RAG"/><category term="rag"/><category term="lightrag"/><category term="graphrag"/><category term="knowledge-graph"/><category term="graph"/><summary type="html"><![CDATA[LightRAG 深度解析：简单快速的图增强 RAG]]></summary></entry><entry><title type="html">双指针算法复盘总结：从元素思维到边界思维</title><link href="https://leesanyee.me/blog/2026/%E5%8F%8C%E6%8C%87%E9%92%88%E7%AE%97%E6%B3%95%E5%A4%8D%E7%9B%98%E6%80%BB%E7%BB%93/" rel="alternate" type="text/html" title="双指针算法复盘总结：从元素思维到边界思维"/><published>2026-08-23T14:00:00+00:00</published><updated>2026-08-23T14:00:00+00:00</updated><id>https://leesanyee.me/blog/2026/%E5%8F%8C%E6%8C%87%E9%92%88%E7%AE%97%E6%B3%95%E5%A4%8D%E7%9B%98%E6%80%BB%E7%BB%93</id><content type="html" xml:base="https://leesanyee.me/blog/2026/%E5%8F%8C%E6%8C%87%E9%92%88%E7%AE%97%E6%B3%95%E5%A4%8D%E7%9B%98%E6%80%BB%E7%BB%93/"><![CDATA[<h1 id="双指针算法复盘总结从元素思维到边界思维">双指针算法复盘总结：从元素思维到边界思维</h1> <blockquote> <p><strong>核心主题</strong>：从”元素思维”转向”边界思维”。</p> </blockquote> <p>刷双指针的题,最大的坎不是不熟悉模板,而是<strong>身份认知</strong>的问题:你总把指针当成”当前元素的下标”,但它实际上是”区域的边界”。这篇复盘把双指针完整分类、每类的区域定义与不变式、错题解剖、防错检查单整理成一份可复用笔记。</p> <hr/> <h2 id="一双指针完整分类地图">一、双指针完整分类地图</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>双指针
├── 同向（两个指针朝同一方向走）
│   ├── 快慢指针：速度不同，fast探路，slow守家
│   └── 滑动窗口：距离动态变化，right扩张找可行解，left收缩找最优解
│
├── 对向（两个指针从两头往中间走）
│   ├── 对撞指针：left←————→right，直到相遇
│   └── 三指针/荷兰国旗：对撞 + 中间加侦察兵i
│
└── 分离（两个指针在两条数组/链表上）
    └── 分离双指针：各走各的，谁小谁移动
</code></pre></div></div> <p><strong>面试核心就三类</strong>：快慢指针、对撞指针、滑动窗口。荷兰国旗是对撞的扩展。</p> <hr/> <h2 id="二每类指针的区域定义与不变式">二、每类指针的”区域定义”与不变式</h2> <h3 id="21-快慢指针fast--slow">2.1 快慢指针（Fast &amp; Slow）</h3> <p><strong>本质</strong>：fast 在未知区探路，slow 标记有效区边界。</p> <p><strong>区域图</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[ 有效区 ) [ 废弃/未知区 )
0       slow        fast         n
</code></pre></div></div> <p><strong>不变式</strong>：<code class="language-plaintext highlighter-rouge">[0, slow)</code> 永远是处理好的有效区，<code class="language-plaintext highlighter-rouge">slow</code> 是”下一个待写入位置”（不是最后一个有效元素的下标）。</p> <p><strong>代码骨架</strong>：</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">slow</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">fast</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">fast</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="n">fast</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">fast</span><span class="p">]</span> <span class="err">满足条件</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">nums</span><span class="p">[</span><span class="n">slow</span><span class="p">]</span> <span class="o">=</span> <span class="n">nums</span><span class="p">[</span><span class="n">fast</span><span class="p">];</span>  <span class="c1">// 先写入边界位置</span>
        <span class="n">slow</span><span class="o">++</span><span class="p">;</span>                   <span class="c1">// 再扩界</span>
    <span class="p">}</span>
<span class="p">}</span>
<span class="k">return</span> <span class="n">slow</span><span class="p">;</span>  <span class="c1">// slow就是有效区长度</span>
</code></pre></div></div> <p><strong>致命易错点</strong>：</p> <ul> <li>必须先 <code class="language-plaintext highlighter-rouge">nums[slow] = ...</code> 再 <code class="language-plaintext highlighter-rouge">slow++</code>，不能反过来</li> <li><code class="language-plaintext highlighter-rouge">slow</code> 是”边界/长度”，不是”下标”</li> </ul> <h3 id="22-对撞指针two-pointers-from-both-ends">2.2 对撞指针（Two Pointers from Both Ends）</h3> <p><strong>本质</strong>：left 和 right 从两头往中间挤，利用有序性或对称性。</p> <p><strong>区域图</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[ 已排除 ) [ 待处理 ] [ 已排除 )
0        left     right         n-1
</code></pre></div></div> <p><strong>不变式</strong>：<code class="language-plaintext highlighter-rouge">[0,left)</code> 和 <code class="language-plaintext highlighter-rouge">(right,n-1]</code> 是已处理/已排除区，<code class="language-plaintext highlighter-rouge">[left,right]</code> 是待处理区。</p> <p><strong>代码骨架</strong>：</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">left</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="n">right</span> <span class="o">=</span> <span class="n">n</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span>
<span class="k">while</span> <span class="p">(</span><span class="n">left</span> <span class="o">&lt;</span> <span class="n">right</span><span class="p">)</span> <span class="p">{</span>  <span class="c1">// 注意是 &lt;，不是 &lt;=</span>
    <span class="k">if</span> <span class="p">(</span><span class="err">条件满足</span><span class="p">)</span> <span class="n">left</span><span class="o">++</span><span class="p">;</span>
    <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="err">另一条件</span><span class="p">)</span> <span class="n">right</span><span class="o">--</span><span class="p">;</span>
    <span class="k">else</span> <span class="p">{</span> <span class="n">left</span><span class="o">++</span><span class="p">;</span> <span class="n">right</span><span class="o">--</span><span class="p">;</span> <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="23-滑动窗口sliding-window">2.3 滑动窗口（Sliding Window）</h3> <p><strong>本质</strong>：快慢指针的变体。right 扩张找”可行解”，left 收缩找”最优解”。</p> <p>窗口是连续区域 <code class="language-plaintext highlighter-rouge">[left, right)</code>。整套算法的节奏只有三个动作，每个动作各回答一个问题——记清楚这三个问题，模板就不会写乱。</p> <h4 id="-什么时候扩张-right加入字符时更新什么数据">① 什么时候扩张 right？加入字符时更新什么数据？</h4> <blockquote> <p><strong>扩张是默认动作</strong>：right 是”读指针”，每个字符都必须被它看过一次，所以外层循环每轮无条件走一步。真正要判断的不是”什么时候扩张”，而是”什么时候停止扩张”——信号不在扩张侧，而在收缩侧：<strong>一旦加入字符后窗口状态翻转，就暂停扩张，转入收缩</strong>（两种题型的”翻转信号”正好相反，见 ③ 的表）。</p> </blockquote> <p>窗口加入字符时，更新的是<strong>记账数据（window state）</strong>——描述”窗口现状”的变量，<strong>不是答案</strong>：</p> <table> <thead> <tr> <th>记什么账</th> <th>对应题目</th> </tr> </thead> <tbody> <tr> <td>频次表 <code class="language-plaintext highlighter-rouge">window[c]++</code></td> <td>无重复字符最长子串、最小覆盖子串</td> </tr> <tr> <td>不同字符计数 <code class="language-plaintext highlighter-rouge">++</code></td> <td>至多 K 个不同字符的最长子串</td> </tr> <tr> <td>窗口和 <code class="language-plaintext highlighter-rouge">sum += x</code></td> <td>长度最小子数组（和 ≥ target）</td> </tr> <tr> <td>已覆盖 target 的字符数</td> <td>最小覆盖子串的”够不够数”</td> </tr> </tbody> </table> <blockquote> <p><strong>规则：扩张 = 入账。入账只改状态，不碰答案。</strong></p> </blockquote> <h4 id="-什么时候暂停扩张开始收缩-left移出字符时更新什么数据">② 什么时候暂停扩张、开始收缩 left？移出字符时更新什么数据？</h4> <blockquote> <p>收到”翻转信号”后，<code class="language-plaintext highlighter-rouge">while</code> 收缩：移出 <code class="language-plaintext highlighter-rouge">s[left]</code> → 更新记账 → <code class="language-plaintext highlighter-rouge">left++</code>，直到窗口达到目标状态（最长型：重新合法；最短型：即将非法）。</p> </blockquote> <p>窗口移出字符时，更新<strong>同一套记账数据</strong>，方向与入账严格相反：</p> <table> <thead> <tr> <th>销什么账</th> <th>对应入账</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">window[d]--</code></td> <td><code class="language-plaintext highlighter-rouge">window[c]++</code></td> </tr> <tr> <td>不同字符计数 <code class="language-plaintext highlighter-rouge">--</code></td> <td><code class="language-plaintext highlighter-rouge">++</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">sum -= x</code></td> <td><code class="language-plaintext highlighter-rouge">sum += x</code></td> </tr> <tr> <td>覆盖计数视情况 <code class="language-plaintext highlighter-rouge">--</code></td> <td><code class="language-plaintext highlighter-rouge">++</code></td> </tr> </tbody> </table> <blockquote> <p><strong>规则：收缩 = 销账，与入账严格对称</strong>——从哪个字符入的账，移出它时就要原样退回。</p> </blockquote> <p><strong>关键</strong>：收缩必须是 <code class="language-plaintext highlighter-rouge">while</code> 不是 <code class="language-plaintext highlighter-rouge">if</code>——移出一个字符可能仍不满足条件，需要连续收缩。</p> <h4 id="-答案在哪里更新最长型-vs-最短型">③ 答案在哪里更新？——最长型 vs 最短型</h4> <blockquote> <p><strong>候选解必须是”合法窗口”，所以答案只在窗口合法的时刻更新。</strong> 但”合法时刻”落在哪一侧，决定了两种题型的写法——这是滑动窗口唯一需要分类的地方：</p> </blockquote> <table> <thead> <tr> <th> </th> <th><strong>最长型</strong>（最长子串/子数组）</th> <th><strong>最短型</strong>（最小覆盖/最短子数组）</th> </tr> </thead> <tbody> <tr> <td>例题</td> <td>无重复最长子串、至多 K 个不同字符</td> <td>最小覆盖子串、长度最小子数组</td> </tr> <tr> <td>收缩循环的条件</td> <td><code class="language-plaintext highlighter-rouge">while (窗口非法)</code> —— 缩到<strong>重新合法</strong></td> <td><code class="language-plaintext highlighter-rouge">while (窗口合法)</code> —— 缩到<strong>即将非法</strong></td> </tr> <tr> <td>合法时刻在哪</td> <td>收缩循环的<strong>出口</strong>（缩回合法的瞬间）</td> <td>收缩循环的<strong>体内</strong>（每缩一步仍合法）</td> </tr> <tr> <td>答案更新位置</td> <td><strong>收缩循环结束后</strong>：<code class="language-plaintext highlighter-rouge">max(ans, right - left)</code></td> <td><strong>收缩循环内部</strong>：<code class="language-plaintext highlighter-rouge">min(ans, right - left)</code></td> </tr> <tr> <td>为什么</td> <td>出口处的窗口 = 以 right 为右端的<strong>最长</strong>合法窗</td> <td>体内每个瞬间都是合法窗，且<strong>越来越短</strong></td> </tr> </tbody> </table> <blockquote> <p><strong>口诀：最长型在”恢复合法”时取 max，最短型在”维持合法”时取 min。</strong></p> </blockquote> <p>反例验证（为什么不能乱放）：</p> <ul> <li>max 放在扩张时 → 把非法窗口（含重复字符 / 超过 K 个 distinct）也算进去，答案虚高</li> <li>min 放在收缩循环外 → 循环退出时窗口已经非法，最小值永远取不到</li> </ul> <p><strong>两种题型，同一副骨架</strong>：</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 最长型：收缩条件 = 非法，更新在收缩循环之后（合法出口）</span>
<span class="kt">int</span> <span class="n">left</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="n">right</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="n">ans</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="k">while</span> <span class="p">(</span><span class="n">right</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">add</span><span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">right</span><span class="p">]);</span>            <span class="c1">// 扩张：入账</span>
    <span class="n">right</span><span class="o">++</span><span class="p">;</span>
    <span class="k">while</span> <span class="p">(</span><span class="err">窗口非法</span><span class="p">)</span> <span class="p">{</span>            <span class="c1">// 缩到重新合法</span>
        <span class="n">remove</span><span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">left</span><span class="p">]);</span>      <span class="c1">// 收缩：销账</span>
        <span class="n">left</span><span class="o">++</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="n">ans</span> <span class="o">=</span> <span class="n">max</span><span class="p">(</span><span class="n">ans</span><span class="p">,</span> <span class="n">right</span> <span class="o">-</span> <span class="n">left</span><span class="p">);</span>   <span class="c1">// 合法时刻（出口）：取 max</span>
<span class="p">}</span>

<span class="c1">// 最短型：收缩条件 = 合法，更新在收缩循环之内（每个合法瞬间）</span>
<span class="kt">int</span> <span class="n">left</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="n">right</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="n">ans</span> <span class="o">=</span> <span class="n">INT_MAX</span><span class="p">;</span>
<span class="k">while</span> <span class="p">(</span><span class="n">right</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">add</span><span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">right</span><span class="p">]);</span>            <span class="c1">// 扩张：入账</span>
    <span class="n">right</span><span class="o">++</span><span class="p">;</span>
    <span class="k">while</span> <span class="p">(</span><span class="err">窗口合法</span><span class="p">)</span> <span class="p">{</span>            <span class="c1">// 缩到即将非法</span>
        <span class="n">ans</span> <span class="o">=</span> <span class="n">min</span><span class="p">(</span><span class="n">ans</span><span class="p">,</span> <span class="n">right</span> <span class="o">-</span> <span class="n">left</span><span class="p">);</span>   <span class="c1">// 合法时刻（体内）：取 min</span>
        <span class="n">remove</span><span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">left</span><span class="p">]);</span>      <span class="c1">// 收缩：销账</span>
        <span class="n">left</span><span class="o">++</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p><strong>走一遍（无重复字符最长子串，s = “abcabcbb”）</strong>：</p> <ul> <li>right=0、1、2：依次入账 a、b、c → 窗口合法 → 出口取 max → ans = 1 → 2 → 3</li> <li>right=3：入账 a → 频次 a 变 2 → 窗口非法 → 收缩：销掉 s[0]=’a’ → 重新合法（left=1，窗口 “bca”）→ 出口取 max(3, 2) = 3</li> <li>全程节奏：<strong>入账 → 可能非法 → 销账到合法 → 出口取 max</strong></li> </ul> <p><strong>走一遍（最小覆盖子串，LeetCode 76——最短型的完整代码）</strong>：</p> <p>想找「覆盖 t 的最短子串」，用的正是<strong>最短型骨架</strong>，和上面的模板一一对照：</p> <table> <thead> <tr> <th>骨架占位</th> <th>76 里的对应</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">add</code>（入账）</td> <td><code class="language-plaintext highlighter-rouge">window[c]++; if (window[c] == need[c]) valid++;</code></td> </tr> <tr> <td>合法条件</td> <td><code class="language-plaintext highlighter-rouge">valid == need.size()</code>（每种字符都凑齐）</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">while (合法)</code> 收缩</td> <td><code class="language-plaintext highlighter-rouge">while (valid == need.size())</code></td> </tr> <tr> <td>收缩内更新 <code class="language-plaintext highlighter-rouge">min</code></td> <td><code class="language-plaintext highlighter-rouge">if (right - left &lt; len) { start = left; len = right - left; }</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">remove</code>（销账）</td> <td><code class="language-plaintext highlighter-rouge">if (window[d] == need[d]) valid--; window[d]--;</code></td> </tr> </tbody> </table> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Solution</span> <span class="p">{</span>
<span class="nl">public:</span>
    <span class="n">string</span> <span class="n">minWindow</span><span class="p">(</span><span class="n">string</span> <span class="n">s</span><span class="p">,</span> <span class="n">string</span> <span class="n">t</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">unordered_map</span><span class="o">&lt;</span><span class="kt">char</span><span class="p">,</span> <span class="kt">int</span><span class="o">&gt;</span> <span class="n">need</span><span class="p">,</span> <span class="n">window</span><span class="p">;</span>
        <span class="k">for</span> <span class="p">(</span><span class="kt">char</span> <span class="n">c</span> <span class="o">:</span> <span class="n">t</span><span class="p">)</span> <span class="n">need</span><span class="p">[</span><span class="n">c</span><span class="p">]</span><span class="o">++</span><span class="p">;</span>        <span class="c1">// need：t 的需求清单</span>

        <span class="kt">int</span> <span class="n">left</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="n">right</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
        <span class="kt">int</span> <span class="n">valid</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>                     <span class="c1">// 已「数量凑齐」的字符种类数</span>
        <span class="kt">int</span> <span class="n">start</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="n">len</span> <span class="o">=</span> <span class="n">INT_MAX</span><span class="p">;</span>      <span class="c1">// 答案：起始下标 + 长度</span>

        <span class="k">while</span> <span class="p">(</span><span class="n">right</span> <span class="o">&lt;</span> <span class="n">s</span><span class="p">.</span><span class="n">size</span><span class="p">())</span> <span class="p">{</span>
            <span class="kt">char</span> <span class="n">c</span> <span class="o">=</span> <span class="n">s</span><span class="p">[</span><span class="n">right</span><span class="p">];</span>             <span class="c1">// ① 即将入窗口的字符</span>
            <span class="n">right</span><span class="o">++</span><span class="p">;</span>                       <span class="c1">//    扩张（默认动作）</span>
            <span class="k">if</span> <span class="p">(</span><span class="n">need</span><span class="p">.</span><span class="n">count</span><span class="p">(</span><span class="n">c</span><span class="p">))</span> <span class="p">{</span>           <span class="c1">//    入账：只记 need 关心的字符</span>
                <span class="n">window</span><span class="p">[</span><span class="n">c</span><span class="p">]</span><span class="o">++</span><span class="p">;</span>
                <span class="k">if</span> <span class="p">(</span><span class="n">window</span><span class="p">[</span><span class="n">c</span><span class="p">]</span> <span class="o">==</span> <span class="n">need</span><span class="p">[</span><span class="n">c</span><span class="p">])</span> <span class="n">valid</span><span class="o">++</span><span class="p">;</span>  <span class="c1">// 这种字符凑齐了，种类数 +1</span>
            <span class="p">}</span>

            <span class="k">while</span> <span class="p">(</span><span class="n">valid</span> <span class="o">==</span> <span class="n">need</span><span class="p">.</span><span class="n">size</span><span class="p">())</span> <span class="p">{</span> <span class="c1">// ② 合法 = 每种字符都凑齐 → 收缩</span>
                <span class="k">if</span> <span class="p">(</span><span class="n">right</span> <span class="o">-</span> <span class="n">left</span> <span class="o">&lt;</span> <span class="n">len</span><span class="p">)</span> <span class="p">{</span>  <span class="c1">// ③ 最短型：在收缩循环内更新 min</span>
                    <span class="n">start</span> <span class="o">=</span> <span class="n">left</span><span class="p">;</span>          <span class="c1">//    （每步都合法，且越缩越短）</span>
                    <span class="n">len</span> <span class="o">=</span> <span class="n">right</span> <span class="o">-</span> <span class="n">left</span><span class="p">;</span>
                <span class="p">}</span>
                <span class="kt">char</span> <span class="n">d</span> <span class="o">=</span> <span class="n">s</span><span class="p">[</span><span class="n">left</span><span class="p">];</span>          <span class="c1">// ② 即将出窗口的字符</span>
                <span class="n">left</span><span class="o">++</span><span class="p">;</span>                    <span class="c1">//    收缩</span>
                <span class="k">if</span> <span class="p">(</span><span class="n">need</span><span class="p">.</span><span class="n">count</span><span class="p">(</span><span class="n">d</span><span class="p">))</span> <span class="p">{</span>       <span class="c1">//    销账：对称退回</span>
                    <span class="k">if</span> <span class="p">(</span><span class="n">window</span><span class="p">[</span><span class="n">d</span><span class="p">]</span> <span class="o">==</span> <span class="n">need</span><span class="p">[</span><span class="n">d</span><span class="p">])</span> <span class="n">valid</span><span class="o">--</span><span class="p">;</span>  <span class="c1">// 减之前先判断（减完就不够了）</span>
                    <span class="n">window</span><span class="p">[</span><span class="n">d</span><span class="p">]</span><span class="o">--</span><span class="p">;</span>
                <span class="p">}</span>
            <span class="p">}</span>
        <span class="p">}</span>
        <span class="k">return</span> <span class="n">len</span> <span class="o">==</span> <span class="n">INT_MAX</span> <span class="o">?</span> <span class="s">""</span> <span class="o">:</span> <span class="n">s</span><span class="p">.</span><span class="n">substr</span><span class="p">(</span><span class="n">start</span><span class="p">,</span> <span class="n">len</span><span class="p">);</span>
    <span class="p">}</span>
<span class="p">};</span>
</code></pre></div></div> <p><strong><code class="language-plaintext highlighter-rouge">valid</code> 的计数技巧</strong>：<code class="language-plaintext highlighter-rouge">valid</code> 不是「覆盖了几个字符」，而是<strong>「数量恰好凑齐」的字符种类数</strong>。入账时数量刚好到 <code class="language-plaintext highlighter-rouge">need[c]</code> 就 <code class="language-plaintext highlighter-rouge">valid++</code>；销账时这个字符在移出前数量刚好等于 <code class="language-plaintext highlighter-rouge">need[d]</code>，移出后就差一个，所以先 <code class="language-plaintext highlighter-rouge">valid--</code> 再 <code class="language-plaintext highlighter-rouge">window[d]--</code>。这一收一放，正是 ② 里说的「<strong>入账销账严格对称</strong>」，也把 ② 表格里「已覆盖 target 的字符数」这一行落到了实处。</p> <p><strong>一句话收束</strong>：入账（扩张）只改状态，销账（收缩）对称退回，答案只在”合法”的瞬间诞生——最长型诞生在缩回合法的<strong>出口</strong>，最短型诞生在维持合法的<strong>体内</strong>。</p> <h3 id="24-荷兰国旗三指针--dutch-national-flag">2.4 荷兰国旗（三指针 / Dutch National Flag）</h3> <p><strong>本质</strong>：对撞指针的扩展，处理”3 类元素原地三分区”。</p> <p><strong>区域图</strong>（必须刻进 DNA）：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[0, left)     → 0区（已搞定）
[left, i)     → 1区（已搞定）
[i, right]    → 未知区（待探索）
(right, n-1]  → 2区（已搞定）
</code></pre></div></div> <p><strong>不变式</strong>：<code class="language-plaintext highlighter-rouge">i</code> 是未知区的左边界，<code class="language-plaintext highlighter-rouge">right</code> 是未知区的右边界。<code class="language-plaintext highlighter-rouge">i</code> 绝对不可以踏入 <code class="language-plaintext highlighter-rouge">(right, n-1]</code> 这个已完工的 2 区。</p> <p><strong>代码骨架</strong>：</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">left</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="n">right</span> <span class="o">=</span> <span class="n">n</span> <span class="o">-</span> <span class="mi">1</span><span class="p">,</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="k">while</span> <span class="p">(</span><span class="n">i</span> <span class="o">&lt;=</span> <span class="n">right</span><span class="p">)</span> <span class="p">{</span>  <span class="c1">// 关键：未知区是[i,right]，不是[0,n)</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">swap</span><span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">],</span> <span class="n">nums</span><span class="p">[</span><span class="n">left</span><span class="p">]);</span>
        <span class="n">left</span><span class="o">++</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">;</span>        <span class="c1">// 换过来的是1（来自1区），安全</span>
    <span class="p">}</span> <span class="k">else</span> <span class="nf">if</span> <span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">i</span><span class="o">++</span><span class="p">;</span>                <span class="c1">// 1就在中间，不动</span>
    <span class="p">}</span> <span class="k">else</span> <span class="p">{</span> <span class="c1">// nums[i] == 2</span>
        <span class="n">swap</span><span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">],</span> <span class="n">nums</span><span class="p">[</span><span class="n">right</span><span class="p">]);</span>
        <span class="n">right</span><span class="o">--</span><span class="p">;</span>            <span class="c1">// i不动！换过来的来自未知区，必须再检查</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p><strong>致命易错点</strong>：</p> <ul> <li>循环条件必须是 <code class="language-plaintext highlighter-rouge">i &lt;= right</code>，绝不能是 <code class="language-plaintext highlighter-rouge">i &lt; n</code></li> <li>swap 2 时 <code class="language-plaintext highlighter-rouge">i</code> 绝对不能动</li> </ul> <h3 id="25-分离双指针">2.5 分离双指针</h3> <p><strong>本质</strong>：两个指针在两个数组上，协作而非竞争。</p> <p><strong>铁律</strong>：谁小谁移动，相等时一起移动。</p> <hr/> <h2 id="三错题解剖">三、错题解剖</h2> <h3 id="31-remove-duplicates保留最多-k-个重复">3.1 Remove Duplicates（保留最多 k 个重复）</h3> <p><strong>错误代码</strong>：</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">slow</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="n">unordered_map</span><span class="o">&lt;</span><span class="kt">int</span><span class="p">,</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">check</span><span class="p">;</span>  <span class="c1">// 错误1：不需要HashMap</span>
<span class="k">for</span><span class="p">(</span><span class="kt">int</span> <span class="n">fast</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">fast</span> <span class="o">&lt;</span> <span class="n">nums</span><span class="p">.</span><span class="n">size</span><span class="p">();</span> <span class="n">fast</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span><span class="p">(</span><span class="n">check</span><span class="p">[</span><span class="n">fast</span><span class="p">]</span><span class="o">!=</span><span class="n">check</span><span class="p">[</span><span class="n">slow</span><span class="p">]</span> <span class="o">&amp;&amp;</span> <span class="n">check</span><span class="p">[</span><span class="n">nums</span><span class="p">[</span><span class="n">fast</span><span class="p">]]</span><span class="o">&lt;</span><span class="mi">2</span><span class="p">)</span> <span class="p">{</span>  <span class="c1">// 错误2：索引和值混用</span>
        <span class="n">slow</span><span class="o">++</span><span class="p">;</span>              <span class="c1">// 错误3：先移动</span>
        <span class="n">nums</span><span class="p">[</span><span class="n">slow</span><span class="p">]</span><span class="o">=</span><span class="n">nums</span><span class="p">[</span><span class="n">fast</span><span class="p">];</span> <span class="c1">// 再写入，导致偏移</span>
        <span class="n">check</span><span class="p">[</span><span class="n">nums</span><span class="p">[</span><span class="n">fast</span><span class="p">]]</span><span class="o">++</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>
<span class="k">return</span> <span class="n">slow</span><span class="o">+</span><span class="mi">1</span><span class="p">;</span>  <span class="c1">// 错误4：返回slow+1来弥补偏移</span>
</code></pre></div></div> <p><strong>错误根因</strong>：</p> <ol> <li><strong>没利用”有序数组”特征</strong>：有序数组去重不需要 HashMap，直接和边界比</li> <li><strong>slow 语义错误</strong>：把 slow 当成”最后一个有效元素下标”，实际上 slow 是”下一个待写入位置”</li> <li><strong>写入顺序错误</strong>：必须先 <code class="language-plaintext highlighter-rouge">nums[slow] = ...</code> 再 <code class="language-plaintext highlighter-rouge">slow++</code></li> </ol> <p><strong>正确通用模板（保留 k 个重复）</strong>：</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">removeDuplicates</span><span class="p">(</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;&amp;</span> <span class="n">nums</span><span class="p">,</span> <span class="kt">int</span> <span class="n">k</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">n</span> <span class="o">=</span> <span class="n">nums</span><span class="p">.</span><span class="n">size</span><span class="p">();</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">n</span> <span class="o">&lt;=</span> <span class="n">k</span><span class="p">)</span> <span class="k">return</span> <span class="n">n</span><span class="p">;</span>
    <span class="kt">int</span> <span class="n">slow</span> <span class="o">=</span> <span class="n">k</span><span class="p">;</span>  <span class="c1">// 前k个无条件保留</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">fast</span> <span class="o">=</span> <span class="n">k</span><span class="p">;</span> <span class="n">fast</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="n">fast</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">fast</span><span class="p">]</span> <span class="o">!=</span> <span class="n">nums</span><span class="p">[</span><span class="n">slow</span> <span class="o">-</span> <span class="n">k</span><span class="p">])</span> <span class="p">{</span>  <span class="c1">// 和有效区倒数第k个比（警戒线）</span>
            <span class="n">nums</span><span class="p">[</span><span class="n">slow</span><span class="p">]</span> <span class="o">=</span> <span class="n">nums</span><span class="p">[</span><span class="n">fast</span><span class="p">];</span>  <span class="c1">// 先写入</span>
            <span class="n">slow</span><span class="o">++</span><span class="p">;</span>                   <span class="c1">// 再扩界</span>
        <span class="p">}</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">slow</span><span class="p">;</span>  <span class="c1">// 边界就是长度</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="32-sort-colors荷兰国旗">3.2 Sort Colors（荷兰国旗）</h3> <p><strong>错误代码</strong>：</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">while</span><span class="p">(</span><span class="n">i</span> <span class="o">&lt;</span> <span class="n">nums</span><span class="p">.</span><span class="n">size</span><span class="p">())</span> <span class="p">{</span>  <span class="c1">// 致命错误</span>
    <span class="k">if</span><span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">]</span><span class="o">==</span><span class="mi">0</span><span class="p">)</span> <span class="p">{</span> <span class="n">swap</span><span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">slow</span><span class="p">],</span><span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">]);</span> <span class="n">slow</span><span class="o">++</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">;</span> <span class="p">}</span>
    <span class="k">else</span> <span class="nf">if</span><span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">]</span><span class="o">==</span><span class="mi">1</span><span class="p">)</span> <span class="p">{</span> <span class="n">i</span><span class="o">++</span><span class="p">;</span> <span class="p">}</span>
    <span class="k">else</span> <span class="p">{</span> <span class="n">swap</span><span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">],</span><span class="n">nums</span><span class="p">[</span><span class="n">fast</span><span class="p">]);</span> <span class="n">fast</span><span class="o">--</span><span class="p">;</span> <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p><strong>错误根因</strong>：</p> <ul> <li><strong>循环条件错误</strong>：用了 <code class="language-plaintext highlighter-rouge">i &lt; n</code>，导致 <code class="language-plaintext highlighter-rouge">i</code> 冲进已经排好的 2 区，把 2 又 swap 回中间</li> <li><strong>没理解区域定义</strong>：<code class="language-plaintext highlighter-rouge">i</code> 不是”遍历整个数组的游标”，而是”未知区的左哨兵”；<code class="language-plaintext highlighter-rouge">fast</code> 右边是已完工区，禁止踏入</li> </ul> <p><strong>正确代码</strong>：</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">while</span> <span class="p">(</span><span class="n">i</span> <span class="o">&lt;=</span> <span class="n">fast</span><span class="p">)</span> <span class="p">{</span>  <span class="c1">// 未知区是[i,fast]</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span> <span class="n">swap</span><span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">],</span> <span class="n">nums</span><span class="p">[</span><span class="n">slow</span><span class="p">]);</span> <span class="n">left</span><span class="o">++</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">;</span> <span class="p">}</span>
    <span class="k">else</span> <span class="nf">if</span> <span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span> <span class="n">i</span><span class="o">++</span><span class="p">;</span> <span class="p">}</span>
    <span class="k">else</span> <span class="p">{</span> <span class="n">swap</span><span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">i</span><span class="p">],</span> <span class="n">nums</span><span class="p">[</span><span class="n">fast</span><span class="p">]);</span> <span class="n">right</span><span class="o">--</span><span class="p">;</span> <span class="cm">/* i不动 */</span> <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="33-其他常见错误模式">3.3 其他常见错误模式</h3> <table> <thead> <tr> <th>坑</th> <th>表现</th> <th>本质</th> </tr> </thead> <tbody> <tr> <td><strong>原地修改依赖症</strong></td> <td>merge 题用 <code class="language-plaintext highlighter-rouge">insert</code>、squares 题直接覆盖 <code class="language-plaintext highlighter-rouge">nums[right]</code></td> <td>把”数据源”和”结果容器”当成同一个东西</td> </tr> <tr> <td><strong>正向遍历思维定式</strong></td> <td>总想从左到右、边遍历边处理</td> <td>忽略了”从后往前”往往没有覆盖风险</td> </tr> <tr> <td><strong>边界条件前置化</strong></td> <td>写一堆 <code class="language-plaintext highlighter-rouge">if(size==0)</code> 特判</td> <td>没有把边界判断融入循环条件</td> </tr> </tbody> </table> <hr/> <h2 id="四核心根因元素思维-vs-边界思维">四、核心根因：”元素思维” vs “边界思维”</h2> <blockquote> <p><strong>你总把指针当成”当前元素的下标”，但它实际上是”区域的边界”。</strong></p> </blockquote> <table> <thead> <tr> <th>题目</th> <th>错误表现</th> <th>指针的真实身份</th> </tr> </thead> <tbody> <tr> <td><strong>Remove Duplicates</strong></td> <td><code class="language-plaintext highlighter-rouge">slow++</code> 后再写入，返回 <code class="language-plaintext highlighter-rouge">slow+1</code> 凑长度</td> <td><code class="language-plaintext highlighter-rouge">slow</code> 是<strong>边界</strong>，指向”下一个待写入位置”，它本身就是长度</td> </tr> <tr> <td><strong>Sort Colors</strong></td> <td><code class="language-plaintext highlighter-rouge">i &lt; n</code> 遍历全数组</td> <td><code class="language-plaintext highlighter-rouge">i</code> 是<strong>未知区左哨兵</strong>，不是普通遍历游标</td> </tr> <tr> <td><strong>Merge Sorted Array</strong></td> <td>想原地 <code class="language-plaintext highlighter-rouge">insert</code> 或正向覆盖</td> <td>没意识到 <code class="language-plaintext highlighter-rouge">nums1</code> 尾部有”空白缓冲区”，应该从后往前填</td> </tr> <tr> <td><strong>Squares of Sorted Array</strong></td> <td>直接覆盖原数组</td> <td>没意识到”结果数组”和”数据源”可以是两个东西</td> </tr> </tbody> </table> <p><strong>一句话总结</strong>：写代码时脑子里想的是”这个元素怎么处理”，但双指针要求你想的是”这个区域怎么变化”。</p> <h3 id="为什么容易混淆">为什么容易混淆？</h3> <p>这是<strong>非常正常</strong>的编程思维惯性：</p> <ul> <li>平时写 <code class="language-plaintext highlighter-rouge">for (int i = 0; i &lt; n; i++)</code>，<code class="language-plaintext highlighter-rouge">i</code> 就是”当前元素”</li> <li>但双指针里，<code class="language-plaintext highlighter-rouge">slow</code> / <code class="language-plaintext highlighter-rouge">left</code> / <code class="language-plaintext highlighter-rouge">i</code> 往往是<strong>左闭右开区间的右边界</strong></li> <li>这个转变是反直觉的，所以你会用 <code class="language-plaintext highlighter-rouge">slow+1</code> 去”修正”，用 <code class="language-plaintext highlighter-rouge">i &lt; n</code> 去”遍历”</li> </ul> <p><strong><code class="language-plaintext highlighter-rouge">slow+1</code> 和 <code class="language-plaintext highlighter-rouge">i &lt; n</code> 不是代码错误，是”身份认知错误”的补丁。</strong> 补丁打多了，逻辑就乱了。</p> <hr/> <h2 id="五防错检查单">五、防错检查单</h2> <p>写代码前必须回答：</p> <ul class="task-list"> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>我画出了区域图吗？（<code class="language-plaintext highlighter-rouge">[搞定区) ptr [待处理区) ptr [搞定区]</code>）</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>每个指针是”位置”还是”边界”？（边界 = 指向区域外第一个元素）</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>循环条件是从”待处理区是否为空”推导的吗？</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>指针移动后，区域定义还成立吗？</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>返回的是边界（长度）还是下标？</li> </ul> <hr/> <h2 id="六一句话决策树">六、一句话决策树</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>看到"原地修改/分区" + "一个数组"
    │
    ├─ 找连续子串/子数组的最长最短 → 滑动窗口
    ├─ 3类固定值原地三分区 → 荷兰国旗（left/i/right）
    ├─ 有序数组 + 去重/分区 → 快慢指针（slow/fast）
    └─ 两头往中间凑 → 对撞指针（left/right）

看到"两个数组/链表"找关系
    └─ 分离双指针（谁小谁走）
</code></pre></div></div> <hr/> <h2 id="七核心口诀">七、核心口诀</h2> <blockquote> <p><strong>两类对撞挤，三类荷兰旗；</strong><br/> <strong>换零两边走，换二右边缩；</strong><br/> <strong>中间是净土，遇一直接过。</strong><br/> <strong>slow是边界，先写再扩界；</strong><br/> <strong>i是侦察兵，只逛未知区。</strong></p> </blockquote> <p><strong>补充口诀：</strong></p> <blockquote> <p><strong>“怕覆盖，就倒着填；两头大，取一边；同方向，快慢走。”</strong></p> </blockquote> <hr/> <h2 id="八关键认知转变">八、关键认知转变</h2> <h3 id="81-数组是廉价的思维是昂贵的">8.1 数组是廉价的，思维是昂贵的</h3> <p>总想原地操作（<code class="language-plaintext highlighter-rouge">insert</code>、直接覆盖），是因为觉得”开新数组浪费空间”。但算法题里：</p> <ul> <li><strong>O(n) 额外空间</strong>是常规操作</li> <li><strong>破坏原始数据</strong>才是致命错误</li> </ul> <p>下次拿到数组题，先问自己：<strong>“我能不能倒着填？能不能开个新数组？”</strong> 如果答案是可以，大概率这就是正解。</p> <h3 id="82-进步轨迹">8.2 进步轨迹</h3> <p>对比第一题和难题（对角线排序）：</p> <ul> <li><strong>第一题</strong>：<code class="language-plaintext highlighter-rouge">insert</code> 导致长度爆炸，逻辑完全失控</li> <li><strong>难题</strong>：已经会利用数学特征做分组，会用 <code class="language-plaintext highlighter-rouge">back()</code> + <code class="language-plaintext highlighter-rouge">pop_back()</code> 优雅回填</li> </ul> <p>难题的难度其实更高，但核心逻辑写对了。这说明不是不会双指针，只是<strong>还没习惯”从后往前”和”另开数组”这两个 trick</strong>。</p> <hr/> <h2 id="九练习建议">九、练习建议</h2> <h3 id="91-每日必做看到数组题先画两个箭头">9.1 每日必做：看到数组题，先画两个箭头</h3> <ul> <li>箭头从两端往中间走 → <strong>对向双指针</strong>（平方和、两数之和）</li> <li>箭头从后往前走 → <strong>逆向双指针</strong>（合并、替换空格）</li> <li>一快一慢往右走 → <strong>快慢双指针</strong>（移除元素、链表判环）</li> </ul> <p>画 5 分钟箭头，再写代码。代码写对了，再把箭头擦掉。</p> <h3 id="92-专项突破计划">9.2 专项突破计划</h3> <ol> <li>接下来 3 天，只做快慢指针和荷兰国旗，不碰滑动窗口和对撞</li> <li>每道题强制画区域图，用 <code class="language-plaintext highlighter-rouge">[ )</code> 符号标出”已搞定区”和”未知区”</li> <li>写代码前，先口头解释：<code class="language-plaintext highlighter-rouge">slow</code> 是下标还是边界？<code class="language-plaintext highlighter-rouge">i</code> 能踏进 <code class="language-plaintext highlighter-rouge">right</code> 右边吗？</li> <li>允许自己开新数组，先放弃”原地修改”的执念，把逻辑跑通再优化</li> </ol> <h3 id="93-画区域图的正确姿势">9.3 画区域图的正确姿势</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>错误画法（直觉）：
[1, 1, 2, 2, 3]
    ↑
   slow（指向1）

正确画法（双指针语义）：
[1, 1) [2, 2, 3)
      ↑
     slow（边界，指向2）
</code></pre></div></div> <p><strong>把指针画成”栅栏”而不是”箭头”。</strong></p> <hr/> <h2 id="十边界认知自测题">十、边界认知自测题</h2> <blockquote> <p><strong>数组 <code class="language-plaintext highlighter-rouge">[1,1,1,2,2,3]</code>，保留最多 2 个重复，用快慢指针。</strong></p> <ol> <li>初始时 <code class="language-plaintext highlighter-rouge">slow</code> 应该等于几？</li> <li><code class="language-plaintext highlighter-rouge">fast</code> 从几开始？</li> <li>第一次满足 <code class="language-plaintext highlighter-rouge">nums[fast] != nums[slow-2]</code> 时，<code class="language-plaintext highlighter-rouge">slow</code> 指向的位置会发生什么？</li> </ol> </blockquote> <p><strong>正确答案</strong>：</p> <ol> <li><code class="language-plaintext highlighter-rouge">slow</code> 初始是 <strong>2</strong>（前 2 个无条件保留，<strong>边界</strong>停在第 3 个位置）</li> <li><code class="language-plaintext highlighter-rouge">fast</code> 从 <strong>2</strong> 开始探路</li> <li>第一次满足条件时，<code class="language-plaintext highlighter-rouge">nums[slow] = nums[fast]</code>，然后 <code class="language-plaintext highlighter-rouge">slow++</code> 扩界</li> </ol> <p>如果第一反应是 <code class="language-plaintext highlighter-rouge">slow=1</code>、<code class="language-plaintext highlighter-rouge">fast=0</code>、先 <code class="language-plaintext highlighter-rouge">slow++</code> 再赋值 → <strong>身份认知还没转过来，需要再练。</strong></p> <hr/> <h2 id="十一更高纬度双指针到底在干嘛">十一、更高纬度：双指针到底在干嘛</h2> <blockquote> <p>前面十节是”术”，这一节是”道”。学会之后，你看到的不再是三道题，而是一个原理的三个投影。</p> </blockquote> <h3 id="111-为什么双指针都是-on单调前沿扫描">11.1 为什么双指针都是 O(n)：单调前沿扫描</h3> <p>暴力做法是在 O(n²) 的 <code class="language-plaintext highlighter-rouge">(i,j)</code> 配对上找答案。双指针做的事从来不是”扫得聪明”，而是：<strong>证明绝大多数配对永远不可能是答案，然后永久丢髥它们</strong>。</p> <blockquote> <p>每个指针单调移动（永不回头）→ 每个指针总共只走 O(n) 步 → 摊还 O(n)。</p> </blockquote> <p>剩下的候选解排成一条<strong>单调阶梯（staircase）</strong>——<code class="language-plaintext highlighter-rouge">l</code> 每前进一步，对应的 <code class="language-plaintext highlighter-rouge">r</code> 只往一个方向动。沿着这条阶梯走一遍就是 O(n)。滑动窗口、对撞、分离，全是这个图景，区别只是那条前沿长什么样。</p> <h3 id="112-指针移动--支配证明">11.2 指针移动 = 支配证明</h3> <p>把 <code class="language-plaintext highlighter-rouge">l++</code> / <code class="language-plaintext highlighter-rouge">r--</code> 从”移动变量”重命名为”删除候选解集”：</p> <blockquote> <p><strong>“我删掉了所有以当前 <code class="language-plaintext highlighter-rouge">l</code>（或 <code class="language-plaintext highlighter-rouge">r</code>）为界的候选解，并证明它们不可能最优。”</strong></p> </blockquote> <p>接雨水那道题：<code class="language-plaintext highlighter-rouge">leftMax &lt; rightMax</code> 时 <code class="language-plaintext highlighter-rouge">left</code> 的水量被左墙锁死，右边再高也救不了这个位置——所以以它为左界的全部候选解都是死的，可以理直气壮地 <code class="language-plaintext highlighter-rouge">left++</code>。<strong>每次移动前问一句：”我删掉了什么，凭什么？” 答不上来的移动，就是靠背模板在写。</strong></p> <h3 id="113-双指针其实是三个家族">11.3 “双指针”其实是三个家族</h3> <p>宽度上记住三张面孔，别再当一个东西背：</p> <table> <thead> <tr> <th>家族</th> <th>代表</th> <th>利用的结构</th> </tr> </thead> <tbody> <tr> <td><strong>原地划分</strong></td> <td>数组型快慢、荷兰旗</td> <td>不需要有序！靠”值分类 + 区域不变式”做 partition</td> </tr> <tr> <td><strong>合并/配对</strong></td> <td>对撞、分离、归并</td> <td>需要有序性（或支配关系），比大小决定谁走</td> </tr> <tr> <td><strong>结构检测</strong></td> <td>Floyd 判环</td> <td>相对采样差 + 鸽巢，跟数组派无共享机制</td> </tr> </tbody> </table> <p>两个彩蛋：荷兰国旗本质是<strong>三路划分 = quicksort 3-way partition 的内核</strong>；归并那道题本质是<strong>归并排序的 merge 步骤</strong>。你刷的每道双指针，都是在单练排序/检索里切出来的一刀。</p> <h3 id="114-数组型快慢--读写分离流水线">11.4 数组型快慢 = 读写分离流水线</h3> <p>忘掉”快慢”，改叫”读写”：<code class="language-plaintext highlighter-rouge">fast</code> 是<strong>读指针</strong>（唯一信息源，必须遍历每个元素），<code class="language-plaintext highlighter-rouge">slow</code> 是<strong>写指针</strong>（有效区边界）。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>fast (迭代器，读) → [ 过滤规则 ] → slow (输出流，写)
</code></pre></div></div> <p>这是 <strong>in-place 版的 filter 流水线</strong>，输出流和输入流在同一个数组上重叠。真正的问题只有一个：<strong>什么时候原地安全？</strong></p> <blockquote> <p><strong>写指针永远不能越过尚未读取的数据源。</strong></p> </blockquote> <p>正向写（去重/移除，写 ≤ 读）安全；结果比源长时必须<strong>倒着填</strong>（Merge Sorted Array、替换空格），从尾部空白缓冲区出发——正写和反写是同一枚硬币，方向反转只是为了让写指针继续躲着源走。别再纠结”开新数组是不是浪费”，画两个箭头问：<strong>“这样写我会踩到还没读的元素吗？”</strong></p> <h3 id="115-决策所需的最小状态量错题一的最终解释">11.5 决策所需的最小状态量（错题一的最终解释）</h3> <p>第三章的错题一，当时说是”有序数组不需要 HashMap”。现在可以讲透：区别是<strong>做一次留/删决策，最少要记住多少东西</strong>。</p> <table> <thead> <tr> <th>情况</th> <th>决策规则</th> <th>所需状态</th> </tr> </thead> <tbody> <tr> <td>移除元素（无序也行）</td> <td><code class="language-plaintext highlighter-rouge">值 == target → 删</code>，纯函数</td> <td><strong>无状态</strong>，O(1) 都不需要</td> </tr> <tr> <td>去重（无序）</td> <td>“这个值我见过第 k+1 次了吗？”</td> <td><strong>完整历史</strong>（multiset），O(n)</td> </tr> <tr> <td>去重（有序）</td> <td><code class="language-plaintext highlighter-rouge">nums[fast] != nums[slow-k]</code></td> <td><strong>一个位置</strong>，O(1)</td> </tr> </tbody> </table> <p>关键的表述：<strong>有序性把”全局支配”压成”局部支配”</strong>。移除元素是全局恒真命题（值本身决定去留，不依赖任何前缀）；去重依赖历史。有序后，相同的值被迫相邻——”这个值出现过几次”的完整历史<strong>坍缩成一个下标</strong>（<code class="language-plaintext highlighter-rouge">slow-k</code> 哨兵）。排序没有替你推导，排序让<strong>记忆降维成指针</strong>。</p> <h3 id="116-这架电梯还能升到别的题">11.6 这架电梯还能升到别的题</h3> <p>“结构单调 → 决策状态可压缩 → 摊还 O(n)”不止双指针：滑动窗口 <code class="language-plaintext highlighter-rouge">while</code> 收缩、单调栈每个元素进出一次、KMP 失配回退有界，<strong>全是同一架电梯的不同楼层</strong>。</p> <p>下次做题别只问”这题是不是双指针”，改问：<strong>“这题的决策，最少的必要记忆是什么？”</strong> 答案是常数 ⇒ 存在一趟流水的做法；答案必须是全部历史 ⇒ 要么排序/建结构把它压下来，要么这题就吃定 O(n) 空间。</p> <hr/> <h2 id="十二通俗版专项对撞指针的瓶颈锁定额水">十二、通俗版专项：对撞指针的”瓶颈锁定额水”</h2> <blockquote> <p>第十一章讲了道，这一章把对撞家族里的”瓶颈锁死型”（盛水、接雨水）用最舒服的方式再讲一遍——全部浓缩成一个原理：<strong>木桶效应，瓶颈锁定额水</strong>。</p> </blockquote> <h3 id="121-核心原理矮的是天花板">12.1 核心原理：矮的是天花板</h3> <p>不管盛水还是接雨水，全篇只有一句话：<strong>水能存多少，由矮的那边决定。</strong></p> <p>这是木桶效应——杯壁再高也没用，水从矮的那边溢出去。所以”矮墙 = 天花板”，谁矮谁定水。</p> <h3 id="122-盛最多水矮墙淘汰最单纯的版本">12.2 盛最多水：矮墙淘汰（最单纯的版本）</h3> <p>一排墙，选两堵当杯壁，装水量 = <strong>矮的那堵墙的高度 × 两墙距离</strong>。双指针从最左和最右开始（距离最大），每轮都”淘汰”对里比较矮的那堵墙。</p> <p>为什么淘汰矮的？把道理说得特别白：</p> <blockquote> <p>左边这堵墙矮。把它留着，右边换任何一堵墙——宽度只会更窄（右边只能往左挪），水量天花板永远是矮的那边的高度。<strong>所以这堵矮墙跟谁配都赢不了，直接枪毙它这一整列候选。</strong></p> </blockquote> <p>注意这个动作不是”试试别的”，而是<strong>一次性判死刑</strong>：以矮墙为边的所有组合都不可能超过当前这对——因为当前这对的宽度已经拉满（两端最远），矮墙又封了顶。这个角度上已经到达它的人生巅峰，往里走埋伏更高墙去了。</p> <h3 id="123-接雨水同一个原理搬到每个坑上">12.3 接雨水：同一个原理，搬到每个坑上</h3> <p>接雨水麻烦一点：水不是装在选定的两堵墙之间，而是<strong>积在每一格坑里</strong>，左右都得有墙才存得住水。每个格子能存多少，公式一句话：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>水量 = min(左边最高的墙, 右边最高的墙) − 我这格的高度
</code></pre></div></div> <p>还是木桶效应：左边最高墙 5、右边最高墙 8，水面最多到 5（从矮那边流走），减去坑深 1，存 4 格水。</p> <p><strong>难点</strong>：每个坑都要”左右最高的墙”，难道每个坑都往左右扫一遍（O(n²)）？双指针的笨办法变聪明之处——从两头往中间边走边记，只记两个数：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>leftMax  = 左指针一路走来见过的墙里最高的
rightMax = 右指针一路走来见过的墙里最高的
</code></pre></div></div> <p>现在盯住左指针脚下的格子，要做判断：<strong>它右边到底有没有够高的墙？</strong></p> <p>这里是最容易卡住的一步，请勾重点：</p> <blockquote> <p><code class="language-plaintext highlighter-rouge">rightMax</code> 不是”右边那堵墙有多高”，而是”右边这一路已经看过的墙里最高的”。右边那片还没看过的区域里，真正的右最高<strong>只会比 <code class="language-plaintext highlighter-rouge">rightMax</code> 高（或持平），绝不可能更矮</strong>。</p> </blockquote> <p>所以可以放心地说一句：<strong>右边至少有一堵 <code class="language-plaintext highlighter-rouge">rightMax</code> 这么高的墙。</strong></p> <p>判断就清脆了：</p> <ul> <li><code class="language-plaintext highlighter-rouge">leftMax &lt; rightMax</code> → 右边保证有比 <code class="language-plaintext highlighter-rouge">leftMax</code> 更高的墙 → 右边当不了瓶颈 → <strong>瓶颈就是 <code class="language-plaintext highlighter-rouge">leftMax</code></strong> → 这格水量 = <code class="language-plaintext highlighter-rouge">leftMax − 自己</code>，定死，左指针往前走</li> <li>反过来，右边一格的积水量被 <code class="language-plaintext highlighter-rouge">rightMax</code> 锁定，右指针往前走</li> </ul> <h3 id="124-一张表收尾淘汰-vs-锁定都是木桶效应">12.4 一张表收尾：淘汰 vs 锁定，都是木桶效应</h3> <table> <thead> <tr> <th> </th> <th>盛最多水</th> <th>接雨水</th> </tr> </thead> <tbody> <tr> <td>每次动作</td> <td>矮墙<strong>淘汰</strong>，换更近的墙</td> <td>矮边<strong>锁定</strong>水面，算完走人</td> </tr> <tr> <td>比较的对象</td> <td>当前两堵墙的高度</td> <td>两侧 running max（压缩的历史）</td> </tr> <tr> <td>依赖结构</td> <td>两端最远 + 矮边封顶（无需排序）</td> <td>逐个格子的左右最高墙（无需排序）</td> </tr> <tr> <td>木桶效应形态</td> <td>全局砍掉整列候选</td> <td>局部钉死一个格子的水位</td> </tr> </tbody> </table> <p>慢节奏上手建议：在纸上随便画一排墙的高度数组，用手模拟 <code class="language-plaintext highlighter-rouge">leftMax</code> / <code class="language-plaintext highlighter-rouge">rightMax</code> 从左到右蠕动一遍，每个格子停下来算一次——走完一遍，代码自然就默写得出来了。</p> <hr/> <h2 id="总结">总结</h2> <blockquote> <p><strong>不是不会双指针，是还没习惯”用边界思考，而不是用元素思考”。</strong></p> </blockquote> <p>这个转变没有捷径，但每次写代码前问一句 <strong>“这个指针是箭头，还是栅栏？”</strong>，一周之内就能彻底改过来。</p> <p><strong>好算法往往是反直觉的，而直觉正在被慢慢修正。</strong> 这个过程就叫学习。</p>]]></content><author><name></name></author><category term="Leetcode"/><category term="TwoPointer"/><category term="two-pointer"/><category term="sliding-window"/><category term="fast-slow"/><category term="cpp"/><summary type="html"><![CDATA[双指针算法复盘总结：从元素思维到边界思维]]></summary></entry><entry><title type="html">LangChain v1 Agent Middleware 笔记</title><link href="https://leesanyee.me/blog/2026/LangChain-v1-Agent-Middleware/" rel="alternate" type="text/html" title="LangChain v1 Agent Middleware 笔记"/><published>2026-08-10T07:50:00+00:00</published><updated>2026-08-10T07:50:00+00:00</updated><id>https://leesanyee.me/blog/2026/LangChain-v1-Agent-Middleware</id><content type="html" xml:base="https://leesanyee.me/blog/2026/LangChain-v1-Agent-Middleware/"><![CDATA[<h1 id="langchain-v1-agent-middleware-笔记">LangChain v1 Agent Middleware 笔记</h1> <blockquote> <p>整理自课程 notebook 与 LangChain 中文文档 <a href="https://langchain-doc.cn/v1/python/langchain/middleware.html">中间件</a> 章节。</p> </blockquote> <h2 id="一句话定位">一句话定位</h2> <p><strong>Middleware 是插入在 Agent 核心循环前后的 hooks</strong>, 用来控制输入、输出、工具调用、消息修剪、日志监控等。</p> <p>核心循环:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>调用模型 → 模型选择工具 → 执行工具 → 重复直到模型不再调用工具
</code></pre></div></div> <p>中间件在以上步骤的<strong>之前和之后</strong>暴露钩子。</p> <hr/> <h2 id="1-为什么需要中间件">1. 为什么需要中间件</h2> <p>Agent 自动调用模型和工具, 但有时你需要在关键节点”插手”:</p> <ul> <li>历史消息太长 → 自动摘要或截断</li> <li>工具结果太脏 → 清洗后再给模型</li> <li>某些 ToolMessage 不想进上下文 → 删掉</li> <li>想记录日志、监控耗时、做权限检查</li> <li>想修改模型输入或输出</li> </ul> <p>这些横切关注点不该写进每个 tool 或 prompt 里, 用中间件统一处理最干净。</p> <hr/> <h2 id="2-两种写法">2. 两种写法</h2> <h3 id="21-内置中间件">2.1 内置中间件</h3> <p><code class="language-plaintext highlighter-rouge">SummarizationMiddleware</code>: 自动摘要历史消息。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langchain.agents</span> <span class="kn">import</span> <span class="n">create_agent</span>
<span class="kn">from</span> <span class="n">langgraph.checkpoint.memory</span> <span class="kn">import</span> <span class="n">InMemorySaver</span>
<span class="kn">from</span> <span class="n">langchain.agents.middleware</span> <span class="kn">import</span> <span class="n">SummarizationMiddleware</span>

<span class="n">agent</span> <span class="o">=</span> <span class="nf">create_agent</span><span class="p">(</span>
    <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">anthropic:deepseek-v4-flash</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">checkpointer</span><span class="o">=</span><span class="nc">InMemorySaver</span><span class="p">(),</span>
    <span class="n">middleware</span><span class="o">=</span><span class="p">[</span>
        <span class="nc">SummarizationMiddleware</span><span class="p">(</span>
            <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">anthropic:deepseek-v4-flash</span><span class="sh">"</span><span class="p">,</span>  <span class="c1"># 负责摘要的模型
</span>            <span class="n">trigger</span><span class="o">=</span><span class="p">(</span><span class="sh">"</span><span class="s">tokens</span><span class="sh">"</span><span class="p">,</span> <span class="mi">100</span><span class="p">),</span>               <span class="c1"># token 超过 100 触发
</span>            <span class="n">keep</span><span class="o">=</span><span class="p">(</span><span class="sh">"</span><span class="s">messages</span><span class="sh">"</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span>                   <span class="c1"># 保留最近 1 条原消息
</span>        <span class="p">)</span>
    <span class="p">],</span>
<span class="p">)</span>
</code></pre></div></div> <p>参数含义:</p> <table> <thead> <tr> <th>参数</th> <th>含义</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">model</code></td> <td>做摘要的模型, 可与主模型不同</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">trigger</code></td> <td>触发条件, 这里是总 token 数</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">keep</code></td> <td>摘要后保留最近几条原消息</td> </tr> </tbody> </table> <p>效果: 老对话被压缩成 summary + 最近消息, 省 token 又不失上下文。</p> <h3 id="22-自定义中间件">2.2 自定义中间件</h3> <p>用 <code class="language-plaintext highlighter-rouge">@before_agent</code> 或 <code class="language-plaintext highlighter-rouge">@after_agent</code> 装饰函数。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">Any</span>
<span class="kn">from</span> <span class="n">langchain.agents</span> <span class="kn">import</span> <span class="n">AgentState</span><span class="p">,</span> <span class="n">create_agent</span>
<span class="kn">from</span> <span class="n">langchain.agents.middleware</span> <span class="kn">import</span> <span class="n">before_agent</span>
<span class="kn">from</span> <span class="n">langchain.messages</span> <span class="kn">import</span> <span class="n">RemoveMessage</span><span class="p">,</span> <span class="n">ToolMessage</span>
<span class="kn">from</span> <span class="n">langgraph.checkpoint.memory</span> <span class="kn">import</span> <span class="n">InMemorySaver</span>
<span class="kn">from</span> <span class="n">langgraph.runtime</span> <span class="kn">import</span> <span class="n">Runtime</span>

<span class="nd">@before_agent</span>
<span class="k">def</span> <span class="nf">trim_messages</span><span class="p">(</span><span class="n">state</span><span class="p">:</span> <span class="n">AgentState</span><span class="p">,</span> <span class="n">runtime</span><span class="p">:</span> <span class="n">Runtime</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="n">Any</span><span class="p">]</span> <span class="o">|</span> <span class="bp">None</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">每次 agent 运行前, 删除所有 ToolMessage</span><span class="sh">"""</span>
    <span class="n">messages</span> <span class="o">=</span> <span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">messages</span><span class="sh">"</span><span class="p">]</span>
    <span class="n">tool_messages</span> <span class="o">=</span> <span class="p">[</span><span class="n">m</span> <span class="k">for</span> <span class="n">m</span> <span class="ow">in</span> <span class="n">messages</span> <span class="k">if</span> <span class="nf">isinstance</span><span class="p">(</span><span class="n">m</span><span class="p">,</span> <span class="n">ToolMessage</span><span class="p">)]</span>
    <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">messages</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span><span class="nc">RemoveMessage</span><span class="p">(</span><span class="nb">id</span><span class="o">=</span><span class="n">m</span><span class="p">.</span><span class="nb">id</span><span class="p">)</span> <span class="k">for</span> <span class="n">m</span> <span class="ow">in</span> <span class="n">tool_messages</span><span class="p">]}</span>

<span class="n">agent</span> <span class="o">=</span> <span class="nf">create_agent</span><span class="p">(</span>
    <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">anthropic:deepseek-v4-flash</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">checkpointer</span><span class="o">=</span><span class="nc">InMemorySaver</span><span class="p">(),</span>
    <span class="n">middleware</span><span class="o">=</span><span class="p">[</span><span class="n">trim_messages</span><span class="p">],</span>
<span class="p">)</span>
</code></pre></div></div> <p>函数签名:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">hook</span><span class="p">(</span><span class="n">state</span><span class="p">:</span> <span class="n">AgentState</span><span class="p">,</span> <span class="n">runtime</span><span class="p">:</span> <span class="n">Runtime</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span> <span class="o">|</span> <span class="bp">None</span><span class="p">:</span>
    <span class="bp">...</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">state</code>: 当前 Agent 状态, 含 <code class="language-plaintext highlighter-rouge">messages</code></li> <li><code class="language-plaintext highlighter-rouge">runtime</code>: 运行时上下文, 含 <code class="language-plaintext highlighter-rouge">config</code>、<code class="language-plaintext highlighter-rouge">context</code> 等</li> <li>返回 <code class="language-plaintext highlighter-rouge">dict</code>: 合并进 state</li> <li>返回 <code class="language-plaintext highlighter-rouge">None</code>: 不修改 state</li> </ul> <hr/> <h2 id="3-与-hook-的关系">3. 与 hook 的关系</h2> <p>概念上完全等价:</p> <table> <thead> <tr> <th>通用说法</th> <th>LangChain v1 名字</th> <th>执行时机</th> </tr> </thead> <tbody> <tr> <td>pre-hook</td> <td><code class="language-plaintext highlighter-rouge">@before_agent</code></td> <td>模型调用前</td> </tr> <tr> <td>post-hook</td> <td><code class="language-plaintext highlighter-rouge">@after_agent</code></td> <td>模型调用后</td> </tr> <tr> <td>pre-tool hook</td> <td><code class="language-plaintext highlighter-rouge">@before_tool</code></td> <td>工具执行前</td> </tr> <tr> <td>post-tool hook</td> <td><code class="language-plaintext highlighter-rouge">@after_tool</code></td> <td>工具执行后</td> </tr> <tr> <td>middleware</td> <td><code class="language-plaintext highlighter-rouge">middleware=[...]</code> 列表</td> <td>统一注册</td> </tr> </tbody> </table> <h2 id="4-执行顺序">4. 执行顺序</h2> <p>假设:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">middleware</span><span class="o">=</span><span class="p">[</span><span class="n">A</span><span class="p">,</span> <span class="n">B</span><span class="p">,</span> <span class="n">C</span><span class="p">]</span>
</code></pre></div></div> <p>before 阶段正序, after 阶段逆序:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>输入 → A.before → B.before → C.before → agent 核心 → C.after → B.after → A.after → 输出
</code></pre></div></div> <p>和常见 Web 框架的中间件洋葱模型一致。</p> <hr/> <h2 id="5-常见用途总结">5. 常见用途总结</h2> <table> <thead> <tr> <th>用途</th> <th>实现方式</th> </tr> </thead> <tbody> <tr> <td>消息摘要</td> <td><code class="language-plaintext highlighter-rouge">SummarizationMiddleware</code></td> </tr> <tr> <td>消息修剪/删除</td> <td><code class="language-plaintext highlighter-rouge">@before_agent</code> 返回 <code class="language-plaintext highlighter-rouge">RemoveMessage</code></td> </tr> <tr> <td>工具参数校验</td> <td><code class="language-plaintext highlighter-rouge">@before_tool</code></td> </tr> <tr> <td>工具结果清洗</td> <td><code class="language-plaintext highlighter-rouge">@after_tool</code></td> </tr> <tr> <td>输入输出日志</td> <td><code class="language-plaintext highlighter-rouge">@before_agent</code> + <code class="language-plaintext highlighter-rouge">@after_agent</code></td> </tr> <tr> <td>权限检查</td> <td><code class="language-plaintext highlighter-rouge">@before_agent</code></td> </tr> <tr> <td>缓存</td> <td><code class="language-plaintext highlighter-rouge">@before_agent</code> 查缓存, <code class="language-plaintext highlighter-rouge">@after_agent</code> 写缓存</td> </tr> </tbody> </table> <hr/> <h2 id="6-关键对象">6. 关键对象</h2> <h3 id="agentstate"><code class="language-plaintext highlighter-rouge">AgentState</code></h3> <p>Agent 的当前状态, 至少包含:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="sh">"</span><span class="s">messages</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span><span class="n">HumanMessage</span><span class="p">,</span> <span class="n">AIMessage</span><span class="p">,</span> <span class="n">ToolMessage</span><span class="p">,</span> <span class="p">...]}</span>
</code></pre></div></div> <p>中间件可以读、改这个 state。</p> <h3 id="runtime"><code class="language-plaintext highlighter-rouge">Runtime</code></h3> <p>运行时容器, 包含:</p> <ul> <li><code class="language-plaintext highlighter-rouge">config</code>: 当前调用配置, 如 <code class="language-plaintext highlighter-rouge">{"configurable": {"thread_id": "1"}}</code></li> <li><code class="language-plaintext highlighter-rouge">context</code>: 通过 <code class="language-plaintext highlighter-rouge">context_schema</code> 注入的用户上下文</li> <li>其他运行信息</li> </ul> <h3 id="removemessage"><code class="language-plaintext highlighter-rouge">RemoveMessage</code></h3> <p>用于从 state 中删除消息:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langchain.messages</span> <span class="kn">import</span> <span class="n">RemoveMessage</span>

<span class="nc">RemoveMessage</span><span class="p">(</span><span class="nb">id</span><span class="o">=</span><span class="n">message</span><span class="p">.</span><span class="nb">id</span><span class="p">)</span>
</code></pre></div></div> <p>返回它, LangGraph 会在状态更新时移除对应消息。</p> <hr/> <h2 id="7-一句话总结">7. 一句话总结</h2> <p>LangChain v1 的 middleware 是 <strong>Agent 核心循环上的 hook 层</strong>: <code class="language-plaintext highlighter-rouge">@before_agent</code> 在模型前改输入, <code class="language-plaintext highlighter-rouge">@after_agent</code> 在模型后改输出, 内置 <code class="language-plaintext highlighter-rouge">SummarizationMiddleware</code> 做自动摘要, 所有逻辑通过 <code class="language-plaintext highlighter-rouge">middleware=[...]</code> 注册到 agent 上统一执行。</p>]]></content><author><name></name></author><category term="agent"/><category term="agent"/><category term="langchain"/><category term="middleware"/><category term="hook"/><category term="agent-loop"/><summary type="html"><![CDATA[LangChain v1 中间件机制详解——Agent 核心循环前后的 hooks, 控制输入输出、工具调用、消息摘要与修剪。]]></summary></entry><entry><title type="html">LangGraph Checkpoint 详解</title><link href="https://leesanyee.me/blog/2026/LangGraph-Checkpoint/" rel="alternate" type="text/html" title="LangGraph Checkpoint 详解"/><published>2026-08-10T07:36:52+00:00</published><updated>2026-08-10T07:36:52+00:00</updated><id>https://leesanyee.me/blog/2026/LangGraph-Checkpoint</id><content type="html" xml:base="https://leesanyee.me/blog/2026/LangGraph-Checkpoint/"><![CDATA[<h1 id="langgraph-checkpoint-详解">LangGraph Checkpoint 详解</h1> <blockquote> <p>本文是面试向的回忆笔记: LangGraph 的短期记忆底层 —— checkpoint。从”是什么”到”怎么实现”再到”能干嘛”, 按面试答题结构组织。</p> </blockquote> <h2 id="一句话定位">一句话定位</h2> <p>LangGraph 不是普通 API 调用, 是 Pregel 风格状态机。每次 <code class="language-plaintext highlighter-rouge">invoke</code> 是图上跑一轮 <strong>superstep</strong> (节点执行)。<strong>checkpoint = 某个 superstep 结束后, 对整个图 state 做的一次快照</strong>, 存进 checkpointer。图跑多少步, 就留多少个快照 —— 类似 git, 每次 commit 一个状态。</p> <h2 id="何时写存什么">何时写、存什么</h2> <p>每次节点执行完 (一个 superstep 完成), 自动写一个 checkpoint。存三样:</p> <table> <thead> <tr> <th>字段</th> <th>内容</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">checkpoint</code></td> <td>所有 channel 的值 (即完整 state, 含 messages 列表) + 各 channel 版本号</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">metadata</code></td> <td><code class="language-plaintext highlighter-rouge">created_at</code>、<code class="language-plaintext highlighter-rouge">step</code>、<code class="language-plaintext highlighter-rouge">source</code> (input/loop/update)、<code class="language-plaintext highlighter-rouge">writes</code> (这步写了啥)、<code class="language-plaintext highlighter-rouge">parents</code> (父线程引用)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">key</code></td> <td><code class="language-plaintext highlighter-rouge">thread_id</code> + <code class="language-plaintext highlighter-rouge">checkpoint_id</code></td> </tr> </tbody> </table> <p>channel 版本号很关键: 并发写时靠它做冲突检测。<code class="language-plaintext highlighter-rouge">checkpoint_id</code> 是<strong>时间序 UUID (v7)</strong>, 保证排序可靠。</p> <h2 id="核心接口-basecheckpointsaver">核心接口 <code class="language-plaintext highlighter-rouge">BaseCheckpointSaver</code></h2> <p>4 个方法, 能背这个就是加分:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">BaseCheckpointSaver</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">get_tuple</span><span class="p">(</span><span class="n">config</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">CheckpointTuple</span><span class="p">:</span> <span class="p">...</span>   <span class="c1"># 读某 checkpoint
</span>    <span class="k">def</span> <span class="nf">list</span><span class="p">(</span><span class="n">config</span><span class="p">,</span> <span class="o">*</span><span class="p">,</span> <span class="n">limit</span><span class="p">,</span> <span class="n">before</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">iterator</span><span class="p">:</span> <span class="p">...</span> <span class="c1"># 枚举某线程历史
</span>    <span class="k">def</span> <span class="nf">put</span><span class="p">(</span><span class="n">config</span><span class="p">,</span> <span class="n">checkpoint</span><span class="p">,</span> <span class="n">metadata</span><span class="p">,</span> <span class="n">new_versions</span><span class="p">):</span> <span class="p">...</span> <span class="c1"># 写快照
</span>    <span class="k">def</span> <span class="nf">put_writes</span><span class="p">(</span><span class="n">config</span><span class="p">,</span> <span class="n">writes</span><span class="p">,</span> <span class="n">task_id</span><span class="p">,</span> <span class="n">task_path</span><span class="p">):</span> <span class="p">...</span>   <span class="c1"># 写 pending writes
</span></code></pre></div></div> <p><strong><code class="language-plaintext highlighter-rouge">put_writes</code> 是精髓 —— 预写日志 (WAL)</strong>。节点输出先落盘 <code class="language-plaintext highlighter-rouge">writes</code>, 再写 checkpoint。好处:</p> <ul> <li>崩溃恢复: 进程挂了, checkpoint 在但 writes 未刷? 从 WAL 重放节点输出, 不丢半步。</li> <li>不需要重跑已完成节点, 直接接着跑。这就是 LangGraph 的<strong>耐久执行 (durable execution)</strong>。</li> </ul> <h2 id="thread_id-vs-checkpoint_id"><code class="language-plaintext highlighter-rouge">thread_id</code> vs <code class="language-plaintext highlighter-rouge">checkpoint_id</code></h2> <p>面试最容易考这个区分:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">thread_id</code></strong> = 会话维度。同一次对话共享。换 id = 新对话。定位”哪个人”。</li> <li><strong><code class="language-plaintext highlighter-rouge">checkpoint_id</code></strong> = 步骤维度。线程内每次快照一个。像 git 的 commit hash。定位”哪一步”。</li> </ul> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>thread_id: "1"  ── checkpoint_id: aaa (step 0)
                ── checkpoint_id: bbb (step 1, 含工具调用)
                ── checkpoint_id: ccc (step 2, 最终回答)
</code></pre></div></div> <p>线程”当前状态” = 最新 <code class="language-plaintext highlighter-rouge">checkpoint_id</code> 那份。所以记忆 = 每次 invoke 从最新快照恢复 state, 再往里塞新输入。</p> <h2 id="一次-invoke-完整流程">一次 <code class="language-plaintext highlighter-rouge">invoke</code> 完整流程</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>agent.invoke({messages:[Q2]}, config)
  1. 查 checkpointer: thread "1" 最新 checkpoint → 取回 [Q1, A1]
  2. state = 旧state + [Q2]
  3. 图上跑: model 节点 → 可能 tool 节点 → 每步写新 checkpoint
  4. 返回最终 state
</code></pre></div></div> <p>所以第二问能看到 Q1+A1 不是魔法, 是 <strong>state 恢复 + 全量重发</strong>。<code class="language-plaintext highlighter-rouge">checkpointer=None</code> 时第 1 步查不到任何东西 → 裸调用, 无记忆。</p> <h2 id="短记忆-vs-长记忆-关键辨析">短记忆 vs 长记忆 (关键辨析)</h2> <ul> <li><strong>checkpoint = short-term memory</strong>: 按 thread 记对话内状态。<code class="language-plaintext highlighter-rouge">InMemorySaver</code> 存内存, 进程重启全没 → 纯短期。换 <code class="language-plaintext highlighter-rouge">SqliteSaver</code>/<code class="language-plaintext highlighter-rouge">PostgresSaver</code> (durable store) → 跨重启, 但仍是”会话内”。</li> <li><strong>长期记忆 = <code class="language-plaintext highlighter-rouge">Store</code> API</strong> (<code class="language-plaintext highlighter-rouge">BaseStore</code>), 不是 checkpointer。跨线程、经语义检索、专门挑出的长期事实。两者别混, 面试常见坑。</li> </ul> <h2 id="checkpoint-解锁的高级能力">checkpoint 解锁的高级能力</h2> <h3 id="a-time-travel-时间旅行">a) Time travel (时间旅行)</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">state_history</span> <span class="o">=</span> <span class="n">agent</span><span class="p">.</span><span class="nf">get_state_history</span><span class="p">(</span><span class="n">config</span><span class="p">)</span>   <span class="c1"># 该线程所有快照
</span><span class="n">past</span> <span class="o">=</span> <span class="n">state_history</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span>                           <span class="c1"># 回退到任意一步
</span><span class="n">agent</span><span class="p">.</span><span class="nf">update_state</span><span class="p">(</span><span class="n">config</span><span class="p">,</span> <span class="n">values</span><span class="p">,</span> <span class="n">as_node</span><span class="o">=</span><span class="sh">"</span><span class="s">...</span><span class="sh">"</span><span class="p">)</span> <span class="c1"># 改某个中间状态
</span></code></pre></div></div> <p>拿到任意 <code class="language-plaintext highlighter-rouge">checkpoint_id</code>, 就能<strong>从历史任一步 fork 出新分支</strong> —— 调试、回溯、回滚都靠这个。</p> <h3 id="b-interrupt--human-in-the-loop">b) Interrupt / Human-in-the-loop</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langgraph.types</span> <span class="kn">import</span> <span class="n">interrupt</span>
<span class="n">value</span> <span class="o">=</span> <span class="nf">interrupt</span><span class="p">({</span><span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">approve?</span><span class="sh">"</span><span class="p">})</span>       <span class="c1"># 暂停, 已存 checkpoint
# 之后:
</span><span class="n">agent</span><span class="p">.</span><span class="nf">invoke</span><span class="p">(</span><span class="nc">Command</span><span class="p">(</span><span class="n">resume</span><span class="o">=</span><span class="sh">"</span><span class="s">yes</span><span class="sh">"</span><span class="p">),</span> <span class="n">config</span><span class="p">)</span>       <span class="c1"># 同 thread 恢复, 不用重跑
</span></code></pre></div></div> <p>中断时 checkpoint 已落盘 → 暂停多久都行, 恢复时从断点继续。这是 HITL 的底层机制。</p> <h3 id="c-失败重试">c) 失败重试</h3> <p>checkpoint 让”跑了一半挂了”变得廉价, 从最近快照续跑。</p> <h2 id="面试加分总结句">面试加分总结句</h2> <p>“LangGraph 的 checkpoint 是每步 state 快照 + WAL 的耐久执行机制。<code class="language-plaintext highlighter-rouge">thread_id</code> 定位会话、<code class="language-plaintext highlighter-rouge">checkpoint_id</code> 定位步骤; 靠它实现记忆、时间旅行、中断恢复。短记忆是会话级 checkpoint, 长记忆是 Store。”</p> <h2 id="结合实践">结合实践</h2> <p>课程 notebook 里的写法:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langgraph.checkpoint.memory</span> <span class="kn">import</span> <span class="n">InMemorySaver</span>

<span class="n">agent</span> <span class="o">=</span> <span class="nf">create_agent</span><span class="p">(</span>
    <span class="sh">"</span><span class="s">anthropic:deepseek-v4-flash</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">checkpointer</span><span class="o">=</span><span class="nc">InMemorySaver</span><span class="p">(),</span>  <span class="c1"># 开启记忆
</span><span class="p">)</span>

<span class="n">config</span> <span class="o">=</span> <span class="p">{</span><span class="sh">"</span><span class="s">configurable</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span><span class="sh">"</span><span class="s">thread_id</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">1</span><span class="sh">"</span><span class="p">}}</span>

<span class="c1"># 第一问
</span><span class="n">response</span> <span class="o">=</span> <span class="n">agent</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">messages</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span><span class="n">question</span><span class="p">]},</span> <span class="n">config</span><span class="p">)</span>

<span class="c1"># 第二问: 模型能"记得"第一问, 因为 state 从 thread "1" 恢复
</span></code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">create_agent(...)</code> + <code class="language-plaintext highlighter-rouge">checkpointer=InMemorySaver()</code> + 传 <code class="language-plaintext highlighter-rouge">config</code> 带 <code class="language-plaintext highlighter-rouge">thread_id</code> —— 就是开了短记忆。内存存, 重启即失。</p>]]></content><author><name></name></author><category term="agent"/><category term="agent"/><category term="langgraph"/><category term="checkpoint"/><category term="memory"/><category term="durable-execution"/><summary type="html"><![CDATA[LangGraph checkpoint 机制深挖——每步 state 快照 + WAL 耐久执行, thread_id 定位会话, checkpoint_id 定位步骤。]]></summary></entry><entry><title type="html">图的遍历（点 / 边 / 路径）—— C++ 模板与 visited 的本质</title><link href="https://leesanyee.me/blog/2026/%E5%9B%BE%E7%9A%84%E9%81%8D%E5%8E%86-%E7%82%B9%E8%BE%B9%E8%B7%AF%E5%BE%84-C++/" rel="alternate" type="text/html" title="图的遍历（点 / 边 / 路径）—— C++ 模板与 visited 的本质"/><published>2026-08-08T14:00:00+00:00</published><updated>2026-08-08T14:00:00+00:00</updated><id>https://leesanyee.me/blog/2026/%E5%9B%BE%E7%9A%84%E9%81%8D%E5%8E%86-%E7%82%B9%E8%BE%B9%E8%B7%AF%E5%BE%84-C++</id><content type="html" xml:base="https://leesanyee.me/blog/2026/%E5%9B%BE%E7%9A%84%E9%81%8D%E5%8E%86-%E7%82%B9%E8%BE%B9%E8%B7%AF%E5%BE%84-C++/"><![CDATA[<h1 id="图的遍历点--边--路径-c-模板与-visited-的本质">图的遍历（点 / 边 / 路径）—— C++ 模板与 visited 的本质</h1> <p>二叉树刷题那篇（<a href="/blog/2026/二叉树刷题总结/">二叉树刷题总结</a>）讲了”遍历 + 分解”两种思维。这篇补上<strong>图</strong>的部分：点、边、路径三种遍历，邻接矩阵 / 邻接表两种存法都写 C++ 模板，重点厘清 <code class="language-plaintext highlighter-rouge">visited</code> 到底解决什么、为什么二叉树不需要它、以及路径遍历为什么还要额外的存储变量。</p> <p>图上的 Dijkstra 扩散（<code class="language-plaintext highlighter-rouge">1/edge_weight</code> 作跳转成本）正是 <a href="/blog/2026/GraphRAG-chunk-level-traversal/">GraphRAG（Chunk 级遍历版）</a> 的检索核心，这篇是它的底层算法。</p> <h2 id="一图的三要素点边路径">一、图的三要素：点、边、路径</h2> <table> <thead> <tr> <th>要素</th> <th>定义</th> <th>关键点</th> </tr> </thead> <tbody> <tr> <td><strong>点</strong> vertex / node</td> <td>实体、状态、位置</td> <td>存”是什么”</td> </tr> <tr> <td><strong>边</strong> edge</td> <td>两点间的关系</td> <td>可带方向（有向图）、权重（有权图）、类型</td> </tr> <tr> <td><strong>路径</strong> path</td> <td>顶点序列，相邻两点间有边</td> <td><code class="language-plaintext highlighter-rouge">v1 → v2 → v3</code></td> </tr> </tbody> </table> <p>相关名词：</p> <ul> <li><strong>路径长度</strong>：经过的边数（或权值和）</li> <li><strong>环</strong>：起点 = 终点的路径</li> <li><strong>连通 / 可达</strong>：两点间存在路径</li> </ul> <p>图本质就是：一堆点 + 点与点之间连不连、怎么连。</p> <h2 id="二两种存法">二、两种存法</h2> <table> <thead> <tr> <th>存法</th> <th>空间</th> <th>找邻居</th> <th>适合</th> </tr> </thead> <tbody> <tr> <td>邻接矩阵 <code class="language-plaintext highlighter-rouge">g[v][v]</code></td> <td>O(V²)</td> <td>O(V)，任意两点查边 O(1)</td> <td>稠密图</td> </tr> <tr> <td>邻接表 <code class="language-plaintext highlighter-rouge">adj[v]→列表</code></td> <td>O(V+E)</td> <td>O(邻居数)</td> <td>稀疏图（实际大多数情况）</td> </tr> </tbody> </table> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;bits/stdc++.h&gt;</span><span class="cp">
</span><span class="k">using</span> <span class="k">namespace</span> <span class="n">std</span><span class="p">;</span>

<span class="kt">int</span> <span class="n">n</span><span class="p">,</span> <span class="n">m</span><span class="p">;</span>                       <span class="c1">// 点数、边数</span>

<span class="c1">// ---- 邻接矩阵：g[u][v] = 边权（无权用 1），0 = 无边 ----</span>
<span class="n">vector</span><span class="o">&lt;</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;&gt;</span> <span class="n">g</span><span class="p">(</span><span class="n">n</span><span class="p">,</span> <span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span><span class="p">(</span><span class="n">n</span><span class="p">,</span> <span class="mi">0</span><span class="p">));</span>
<span class="c1">// 读边：g[u][v] = g[v][u] = w;   （有向只写一行）</span>

<span class="c1">// ---- 邻接表：adj[u] = {(邻居v, 边权w), ...}；无权可省 w ----</span>
<span class="n">vector</span><span class="o">&lt;</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">pair</span><span class="o">&lt;</span><span class="kt">int</span><span class="p">,</span> <span class="kt">int</span><span class="o">&gt;&gt;&gt;</span> <span class="n">adj</span><span class="p">(</span><span class="n">n</span><span class="p">);</span>
<span class="c1">// 读边：adj[u].push_back({v, w}); adj[v].push_back({u, w});   （有向只写一行）</span>
</code></pre></div></div> <p>DFS/BFS 在两种表示下都能跑，复杂度：邻接表 O(V+E)，矩阵 O(V²)。</p> <h2 id="三点的遍历dfs--bfs">三、点的遍历：DFS / BFS</h2> <p>点遍历解决”从某点出发能到哪些点”。<strong>图有环，必须 visited</strong>。</p> <h3 id="31-dfs栈--递归一条路走到底">3.1 DFS（栈 / 递归，一条路走到底）</h3> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 矩阵版</span>
<span class="n">vector</span><span class="o">&lt;</span><span class="kt">bool</span><span class="o">&gt;</span> <span class="n">vis</span><span class="p">(</span><span class="n">n</span><span class="p">,</span> <span class="nb">false</span><span class="p">);</span>
<span class="n">function</span><span class="o">&lt;</span><span class="kt">void</span><span class="p">(</span><span class="kt">int</span><span class="p">)</span><span class="o">&gt;</span> <span class="n">dfs</span> <span class="o">=</span> <span class="p">[</span><span class="o">&amp;</span><span class="p">](</span><span class="kt">int</span> <span class="n">u</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">vis</span><span class="p">[</span><span class="n">u</span><span class="p">]</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
    <span class="c1">// 处理点 u</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">v</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">v</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">v</span><span class="p">)</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">g</span><span class="p">[</span><span class="n">u</span><span class="p">][</span><span class="n">v</span><span class="p">]</span> <span class="o">&amp;&amp;</span> <span class="o">!</span><span class="n">vis</span><span class="p">[</span><span class="n">v</span><span class="p">])</span>
            <span class="n">dfs</span><span class="p">(</span><span class="n">v</span><span class="p">);</span>
<span class="p">};</span>

<span class="c1">// 表版</span>
<span class="n">vector</span><span class="o">&lt;</span><span class="kt">bool</span><span class="o">&gt;</span> <span class="n">vis</span><span class="p">(</span><span class="n">n</span><span class="p">,</span> <span class="nb">false</span><span class="p">);</span>
<span class="kt">void</span> <span class="nf">dfs</span><span class="p">(</span><span class="kt">int</span> <span class="n">u</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">vis</span><span class="p">[</span><span class="n">u</span><span class="p">]</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
    <span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="p">[</span><span class="n">v</span><span class="p">,</span> <span class="n">w</span><span class="p">]</span> <span class="o">:</span> <span class="n">adj</span><span class="p">[</span><span class="n">u</span><span class="p">])</span>
        <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">vis</span><span class="p">[</span><span class="n">v</span><span class="p">])</span> <span class="n">dfs</span><span class="p">(</span><span class="n">v</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="32-bfs队列一层一层扩">3.2 BFS（队列，一层一层扩）</h3> <p>BFS 天然给出无权图最短路径。<strong>入队时就标记，不是出队时</strong>——否则一个点会被多个邻居重复推入队，最坏 O(V²)。</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 矩阵版：dist[v] = s 到 v 步数，-1 未到。dist 兼任 visited + 最短距离</span>
<span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">bfs</span><span class="p">(</span><span class="kt">int</span> <span class="n">s</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">dist</span><span class="p">(</span><span class="n">n</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">);</span>
    <span class="n">queue</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">q</span><span class="p">;</span>
    <span class="n">dist</span><span class="p">[</span><span class="n">s</span><span class="p">]</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">q</span><span class="p">.</span><span class="n">push</span><span class="p">(</span><span class="n">s</span><span class="p">);</span>
    <span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">q</span><span class="p">.</span><span class="n">empty</span><span class="p">())</span> <span class="p">{</span>
        <span class="kt">int</span> <span class="n">u</span> <span class="o">=</span> <span class="n">q</span><span class="p">.</span><span class="n">front</span><span class="p">();</span> <span class="n">q</span><span class="p">.</span><span class="n">pop</span><span class="p">();</span>
        <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">v</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">v</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">v</span><span class="p">)</span>
            <span class="k">if</span> <span class="p">(</span><span class="n">g</span><span class="p">[</span><span class="n">u</span><span class="p">][</span><span class="n">v</span><span class="p">]</span> <span class="o">&amp;&amp;</span> <span class="n">dist</span><span class="p">[</span><span class="n">v</span><span class="p">]</span> <span class="o">==</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
                <span class="n">dist</span><span class="p">[</span><span class="n">v</span><span class="p">]</span> <span class="o">=</span> <span class="n">dist</span><span class="p">[</span><span class="n">u</span><span class="p">]</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>   <span class="c1">// 先到者最短</span>
                <span class="n">q</span><span class="p">.</span><span class="n">push</span><span class="p">(</span><span class="n">v</span><span class="p">);</span>
            <span class="p">}</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">dist</span><span class="p">;</span>
<span class="p">}</span>

<span class="c1">// 表版</span>
<span class="kt">void</span> <span class="nf">bfs</span><span class="p">(</span><span class="kt">int</span> <span class="n">s</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">dist</span><span class="p">(</span><span class="n">n</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">);</span>
    <span class="n">queue</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">q</span><span class="p">;</span>
    <span class="n">dist</span><span class="p">[</span><span class="n">s</span><span class="p">]</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">q</span><span class="p">.</span><span class="n">push</span><span class="p">(</span><span class="n">s</span><span class="p">);</span>
    <span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">q</span><span class="p">.</span><span class="n">empty</span><span class="p">())</span> <span class="p">{</span>
        <span class="kt">int</span> <span class="n">u</span> <span class="o">=</span> <span class="n">q</span><span class="p">.</span><span class="n">front</span><span class="p">();</span> <span class="n">q</span><span class="p">.</span><span class="n">pop</span><span class="p">();</span>
        <span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="p">[</span><span class="n">v</span><span class="p">,</span> <span class="n">w</span><span class="p">]</span> <span class="o">:</span> <span class="n">adj</span><span class="p">[</span><span class="n">u</span><span class="p">])</span>
            <span class="k">if</span> <span class="p">(</span><span class="n">dist</span><span class="p">[</span><span class="n">v</span><span class="p">]</span> <span class="o">==</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
                <span class="n">dist</span><span class="p">[</span><span class="n">v</span><span class="p">]</span> <span class="o">=</span> <span class="n">dist</span><span class="p">[</span><span class="n">u</span><span class="p">]</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>
                <span class="n">q</span><span class="p">.</span><span class="n">push</span><span class="p">(</span><span class="n">v</span><span class="p">);</span>
            <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <h2 id="四visited-的本质与二叉树的例外">四、visited 的本质与二叉树的例外</h2> <h3 id="41-visited-解决两件事">4.1 visited 解决两件事</h3> <ol> <li><strong>防重复访问</strong>：每个点被多个邻居触发时，只算一次</li> <li><strong>防死循环</strong>：有环图上不标记，递归无限套娃 → 栈溢出</li> </ol> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 图，环存在。没有 vis：1→2→3→1 无限循环</span>
<span class="kt">void</span> <span class="nf">dfs</span><span class="p">(</span><span class="kt">int</span> <span class="n">u</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="p">[</span><span class="n">v</span><span class="p">,</span> <span class="n">w</span><span class="p">]</span> <span class="o">:</span> <span class="n">adj</span><span class="p">[</span><span class="n">u</span><span class="p">])</span>
        <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">vis</span><span class="p">[</span><span class="n">v</span><span class="p">])</span> <span class="n">dfs</span><span class="p">(</span><span class="n">v</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="42-为什么二叉树不需要-visited">4.2 为什么二叉树不需要 visited</h3> <p>二叉树<strong>天生无环</strong>，且<strong>每个节点只有一条父路径能到达</strong>（单亲）——所以：</p> <ol> <li><strong>不可能重复访问</strong>：每个点只会被它的唯一父节点触发一次</li> <li><strong>不会死循环</strong>：没有环，递归必然终止（深度 = 树高）</li> </ol> <p>visited 是空转——写上不报错，纯属白写：</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 树：天然无环 + 单亲，去掉 visited 依然正确终止</span>
<span class="kt">void</span> <span class="nf">dfs</span><span class="p">(</span><span class="kt">int</span> <span class="n">u</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="p">[</span><span class="n">v</span><span class="p">,</span> <span class="n">w</span><span class="p">]</span> <span class="o">:</span> <span class="n">adj</span><span class="p">[</span><span class="n">u</span><span class="p">])</span>   <span class="c1">// 或 dfs(left); dfs(right);</span>
        <span class="n">dfs</span><span class="p">(</span><span class="n">v</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <blockquote> <p>注意：树不用 visited 不是”树上的 DFS 不是 DFS”。DFS 还是 DFS，只是树<strong>把防重这个职责让结构扛了</strong>。别误解成”树只在深层走、图不是”——图也是深度优先往深层走，区别在<strong>会不会绕回来</strong>：树到叶子自然停（深度有限），图会顺着环绕回（深度无限），没有 visited 停不下来。</p> </blockquote> <table> <thead> <tr> <th>结构</th> <th>需 visited 吗</th> <th>原因</th> </tr> </thead> <tbody> <tr> <td>图（有环）</td> <td><strong>必用</strong></td> <td>防重复访问 + 防死循环</td> </tr> <tr> <td>树（无环单亲）</td> <td><strong>不用</strong></td> <td>结构保证不重访</td> </tr> <tr> <td>DAG（无环）</td> <td>看情况</td> <td>有公共子节点就可能重访</td> </tr> </tbody> </table> <h2 id="五边的遍历">五、边的遍历</h2> <h3 id="51-枚举所有边纯读做汇总--过滤">5.1 枚举所有边（纯读，做汇总 / 过滤）</h3> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 矩阵版：只扫上半三角，自动去重</span>
<span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">u</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">u</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">u</span><span class="p">)</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">v</span> <span class="o">=</span> <span class="n">u</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span> <span class="n">v</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">v</span><span class="p">)</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">g</span><span class="p">[</span><span class="n">u</span><span class="p">][</span><span class="n">v</span><span class="p">])</span> <span class="p">{</span> <span class="cm">/* 处理边 (u,v,w) */</span> <span class="p">}</span>

<span class="c1">// 表版：u&lt;v 去重；有向免去重</span>
<span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">u</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">u</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">u</span><span class="p">)</span>
    <span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="p">[</span><span class="n">v</span><span class="p">,</span> <span class="n">w</span><span class="p">]</span> <span class="o">:</span> <span class="n">adj</span><span class="p">[</span><span class="n">u</span><span class="p">])</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">u</span> <span class="o">&lt;</span> <span class="n">v</span><span class="p">)</span> <span class="p">{</span> <span class="cm">/* 处理边 (u,v,w) */</span> <span class="p">}</span>
</code></pre></div></div> <p>纯枚举不需要任何标记。</p> <h3 id="52-dfs-给边分类树边--返边">5.2 DFS 给边分类：树边 / 返边</h3> <p>判环、Tarjan、桥的前置。用 <code class="language-plaintext highlighter-rouge">tin</code> <strong>时间戳</strong>，不是普通 visited——<code class="language-plaintext highlighter-rouge">tin</code> 干两份活：当 visited 用（<code class="language-plaintext highlighter-rouge">== -1</code> 判断来过没有）+ 记录发现顺序（判”返边”靠”v 是不是 u 的祖先”，光靠 visited 判不了先后）。</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">tin</span><span class="p">(</span><span class="n">n</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">);</span>
<span class="kt">int</span> <span class="n">timer</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="c1">// 表版；矩阵版只换邻居枚举</span>
<span class="kt">void</span> <span class="nf">dfs_edges</span><span class="p">(</span><span class="kt">int</span> <span class="n">u</span><span class="p">,</span> <span class="kt">int</span> <span class="n">p</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">tin</span><span class="p">[</span><span class="n">u</span><span class="p">]</span> <span class="o">=</span> <span class="n">timer</span><span class="o">++</span><span class="p">;</span>
    <span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="p">[</span><span class="n">v</span><span class="p">,</span> <span class="n">w</span><span class="p">]</span> <span class="o">:</span> <span class="n">adj</span><span class="p">[</span><span class="n">u</span><span class="p">])</span> <span class="p">{</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">v</span> <span class="o">==</span> <span class="n">p</span><span class="p">)</span> <span class="k">continue</span><span class="p">;</span>               <span class="c1">// 往回走的树边，跳过</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">tin</span><span class="p">[</span><span class="n">v</span><span class="p">]</span> <span class="o">==</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="n">dfs_edges</span><span class="p">(</span><span class="n">v</span><span class="p">,</span> <span class="n">u</span><span class="p">);</span>  <span class="c1">// 树边（首次发现）</span>
        <span class="cm">/* else：返边 u→v，v 是 u 的祖先（有环） */</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <h2 id="六路径的遍历">六、路径的遍历</h2> <h3 id="61-枚举-st-的所有简单路径回溯">6.1 枚举 s→t 的所有简单路径（回溯）</h3> <p>路径遍历除了防重，还要<strong>存路径</strong>。两个额外变量：</p> <ul> <li><code class="language-plaintext highlighter-rouge">path</code>：存当前路径</li> <li><code class="language-plaintext highlighter-rouge">on_path</code>：标记当前路径上的点，防重复经过</li> </ul> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">path</span><span class="p">;</span>
<span class="n">vector</span><span class="o">&lt;</span><span class="kt">bool</span><span class="o">&gt;</span> <span class="n">on_path</span><span class="p">(</span><span class="n">n</span><span class="p">,</span> <span class="nb">false</span><span class="p">);</span>
<span class="c1">// 表版；矩阵版把邻居循环换成 for v in 0..n-1</span>
<span class="kt">void</span> <span class="nf">dfs_paths</span><span class="p">(</span><span class="kt">int</span> <span class="n">u</span><span class="p">,</span> <span class="kt">int</span> <span class="n">t</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">on_path</span><span class="p">[</span><span class="n">u</span><span class="p">]</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
    <span class="n">path</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">u</span><span class="p">);</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">u</span> <span class="o">==</span> <span class="n">t</span><span class="p">)</span> <span class="p">{</span> <span class="cm">/* 处理 path：一条完整路径 */</span> <span class="p">}</span>
    <span class="k">else</span> <span class="nf">for</span> <span class="p">(</span><span class="k">auto</span> <span class="p">[</span><span class="n">v</span><span class="p">,</span> <span class="n">w</span><span class="p">]</span> <span class="o">:</span> <span class="n">adj</span><span class="p">[</span><span class="n">u</span><span class="p">])</span>
        <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">on_path</span><span class="p">[</span><span class="n">v</span><span class="p">])</span>
            <span class="n">dfs_paths</span><span class="p">(</span><span class="n">v</span><span class="p">,</span> <span class="n">t</span><span class="p">);</span>
    <span class="n">path</span><span class="p">.</span><span class="n">pop_back</span><span class="p">();</span>
    <span class="n">on_path</span><span class="p">[</span><span class="n">u</span><span class="p">]</span> <span class="o">=</span> <span class="nb">false</span><span class="p">;</span>       <span class="c1">// 关键：回溯复位！</span>
<span class="p">}</span>
</code></pre></div></div> <blockquote> <p>陷阱：<code class="language-plaintext highlighter-rouge">on_path</code> 必须在递归后<strong>复位</strong>。否则第一条路走完、换路时，别的路径想再经过 u 会被挡死 → 漏路径。</p> </blockquote> <p><code class="language-plaintext highlighter-rouge">vis</code> 和 <code class="language-plaintext highlighter-rouge">on_path</code> 是两种东西：</p> <table> <thead> <tr> <th>标记</th> <th>性质</th> <th>生命周期</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">vis</code>（点遍历）</td> <td>永久，清不回</td> <td>整个图遍历一遍就结束</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">on_path</code>（路径枚举）</td> <td>临时，<strong>回溯复位</strong></td> <td>只标记”当前这条路上”，换路放行</td> </tr> </tbody> </table> <h3 id="62-最短路径--还原">6.2 最短路径 + 还原</h3> <p>无权图用 BFS + <code class="language-plaintext highlighter-rouge">prev</code> 前驱；加权图把 BFS 换成 Dijkstra，<code class="language-plaintext highlighter-rouge">prev</code> 逻辑相同。</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">prev</span><span class="p">(</span><span class="n">n</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">);</span>                 <span class="c1">// prev[v] = 最短路径上 v 的前驱</span>
<span class="kt">void</span> <span class="nf">bfs_shortest</span><span class="p">(</span><span class="kt">int</span> <span class="n">s</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">queue</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">q</span><span class="p">;</span>
    <span class="n">prev</span><span class="p">[</span><span class="n">s</span><span class="p">]</span> <span class="o">=</span> <span class="n">s</span><span class="p">;</span> <span class="n">q</span><span class="p">.</span><span class="n">push</span><span class="p">(</span><span class="n">s</span><span class="p">);</span>
    <span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">q</span><span class="p">.</span><span class="n">empty</span><span class="p">())</span> <span class="p">{</span>
        <span class="kt">int</span> <span class="n">u</span> <span class="o">=</span> <span class="n">q</span><span class="p">.</span><span class="n">front</span><span class="p">();</span> <span class="n">q</span><span class="p">.</span><span class="n">pop</span><span class="p">();</span>
        <span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="p">[</span><span class="n">v</span><span class="p">,</span> <span class="n">w</span><span class="p">]</span> <span class="o">:</span> <span class="n">adj</span><span class="p">[</span><span class="n">u</span><span class="p">])</span>
            <span class="k">if</span> <span class="p">(</span><span class="n">prev</span><span class="p">[</span><span class="n">v</span><span class="p">]</span> <span class="o">==</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
                <span class="n">prev</span><span class="p">[</span><span class="n">v</span><span class="p">]</span> <span class="o">=</span> <span class="n">u</span><span class="p">;</span>             <span class="c1">// 先到者定前驱</span>
                <span class="n">q</span><span class="p">.</span><span class="n">push</span><span class="p">(</span><span class="n">v</span><span class="p">);</span>
            <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
<span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">reconstruct</span><span class="p">(</span><span class="kt">int</span> <span class="n">s</span><span class="p">,</span> <span class="kt">int</span> <span class="n">t</span><span class="p">)</span> <span class="p">{</span>  <span class="c1">// 还原 s→t 的路径</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">prev</span><span class="p">[</span><span class="n">t</span><span class="p">]</span> <span class="o">==</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="k">return</span> <span class="p">{};</span>        <span class="c1">// 不可达</span>
    <span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">p</span><span class="p">;</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">cur</span> <span class="o">=</span> <span class="n">t</span><span class="p">;;</span> <span class="n">cur</span> <span class="o">=</span> <span class="n">prev</span><span class="p">[</span><span class="n">cur</span><span class="p">])</span> <span class="p">{</span>
        <span class="n">p</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">cur</span><span class="p">);</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">cur</span> <span class="o">==</span> <span class="n">s</span><span class="p">)</span> <span class="k">break</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="n">reverse</span><span class="p">(</span><span class="n">p</span><span class="p">.</span><span class="n">begin</span><span class="p">(),</span> <span class="n">p</span><span class="p">.</span><span class="n">end</span><span class="p">());</span>
    <span class="k">return</span> <span class="n">p</span><span class="p">;</span>                            <span class="c1">// 无权图必是最短路径</span>
<span class="p">}</span>
</code></pre></div></div> <h2 id="七遍历-vs-分解树和图的分野">七、遍历 vs 分解：树和图的分野</h2> <p>二叉树刷题那篇说”DFS = 遍历 + 分解两种思维”。<strong>分解（分治）思想在图里存在，但只在”图结构允许子问题独立”时能用</strong>。</p> <ul> <li><strong>树</strong>：子树是独立子问题，<code class="language-plaintext highlighter-rouge">f(node) = combine(f(left), f(right))</code>，父问题解 = 子问题解拼接 → 分解可行且高效</li> <li><strong>图</strong>：没有天然分割，一个节点连向四面八方，子问题重叠、互相共享状态 → 通用图上分解会重复计算</li> </ul> <table> <thead> <tr> <th>结构</th> <th>能分解吗</th> <th>为什么</th> </tr> </thead> <tbody> <tr> <td>树</td> <td>能，还最优</td> <td>子树独立，无重叠</td> </tr> <tr> <td>DAG</td> <td>有条件</td> <td>拓扑序 + 记忆化</td> </tr> <tr> <td>连通分量</td> <td>能（整体层面）</td> <td>块间无连接</td> </tr> <tr> <td>一般有环图</td> <td>难</td> <td>子问题重叠，遍历兜底</td> </tr> </tbody> </table> <p>一个例子：数路径条数。</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 树：分解，子问题独立，不用 visited</span>
<span class="kt">int</span> <span class="nf">count_paths</span><span class="p">(</span><span class="n">node</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">node</span><span class="p">)</span> <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
    <span class="k">return</span> <span class="mi">1</span> <span class="o">+</span> <span class="n">count_paths</span><span class="p">(</span><span class="n">node</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">)</span> <span class="o">+</span> <span class="n">count_paths</span><span class="p">(</span><span class="n">node</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">);</span>
<span class="p">}</span>

<span class="c1">// 图：不能分解，子问题重叠，visited + 回溯</span>
<span class="kt">int</span> <span class="nf">count_paths</span><span class="p">(</span><span class="n">u</span><span class="p">,</span> <span class="n">t</span><span class="p">,</span> <span class="n">visited</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">u</span> <span class="o">==</span> <span class="n">t</span><span class="p">)</span> <span class="k">return</span> <span class="mi">1</span><span class="p">;</span>
    <span class="kt">int</span> <span class="n">total</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="p">[</span><span class="n">v</span><span class="p">,</span> <span class="n">w</span><span class="p">]</span> <span class="o">:</span> <span class="n">adj</span><span class="p">[</span><span class="n">u</span><span class="p">])</span>
        <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">visited</span><span class="p">[</span><span class="n">v</span><span class="p">])</span> <span class="p">{</span>
            <span class="n">visited</span><span class="p">[</span><span class="n">v</span><span class="p">]</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
            <span class="n">total</span> <span class="o">+=</span> <span class="n">count_paths</span><span class="p">(</span><span class="n">v</span><span class="p">,</span> <span class="n">t</span><span class="p">,</span> <span class="n">visited</span><span class="p">);</span>
            <span class="n">visited</span><span class="p">[</span><span class="n">v</span><span class="p">]</span> <span class="o">=</span> <span class="nb">false</span><span class="p">;</span>   <span class="c1">// 回溯复位</span>
        <span class="p">}</span>
    <span class="k">return</span> <span class="n">total</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>图要分解，得先解决”怎么把图切成独立块”——这本身就是个图问题（割点、连通分量、桥）。</p> <h2 id="八总结">八、总结</h2> <p>每种遍历要什么标记，总账：</p> <table> <thead> <tr> <th>遍历</th> <th>需要什么</th> <th>额外变量</th> </tr> </thead> <tbody> <tr> <td>点遍历 DFS/BFS</td> <td><code class="language-plaintext highlighter-rouge">vis</code>（必用）</td> <td>BFS 的 <code class="language-plaintext highlighter-rouge">dist</code> 兼任距离</td> </tr> <tr> <td>边枚举</td> <td>无</td> <td>无</td> </tr> <tr> <td>边分类</td> <td><code class="language-plaintext highlighter-rouge">tin</code> 时间戳</td> <td>无（既是防重又是判环依据）</td> </tr> <tr> <td>路径枚举</td> <td><code class="language-plaintext highlighter-rouge">on_path</code> + <strong>复位</strong></td> <td><code class="language-plaintext highlighter-rouge">path</code> 存路径</td> </tr> <tr> <td>最短路径</td> <td>BFS/Dijkstra</td> <td><code class="language-plaintext highlighter-rouge">prev</code> 前驱 + 还原</td> </tr> </tbody> </table> <p>一句话：<strong>visited 是图特有的防御（防环 / 防重），树靠结构干净免了；path 是路径问题特有的存储，树和图都要</strong>。图的默认武器是遍历，树的默认武器是分解——不是图不能分解，是图要先付出”找独立子问题”的代价，而树天生自带。</p>]]></content><author><name></name></author><category term="Leetcode"/><category term="Graph"/><category term="dfs"/><category term="bfs"/><category term="graph"/><category term="cpp"/><summary type="html"><![CDATA[图的遍历（点 / 边 / 路径）—— C++ 模板与 visited 的本质]]></summary></entry><entry><title type="html">GraphRAG（Chunk 级遍历版）—— 带图的检索，还是假的图谱？</title><link href="https://leesanyee.me/blog/2026/GraphRAG-chunk-level-traversal/" rel="alternate" type="text/html" title="GraphRAG（Chunk 级遍历版）—— 带图的检索，还是假的图谱？"/><published>2026-08-08T13:00:00+00:00</published><updated>2026-08-08T13:00:00+00:00</updated><id>https://leesanyee.me/blog/2026/GraphRAG-chunk-level-traversal</id><content type="html" xml:base="https://leesanyee.me/blog/2026/GraphRAG-chunk-level-traversal/"><![CDATA[<h1 id="graphragchunk-级遍历版-带图的检索还是假的图谱">GraphRAG（Chunk 级遍历版）—— 带图的检索，还是假的图谱？</h1> <p>对应 notebook是<code class="language-plaintext highlighter-rouge">https://github.com/NirDiamant/RAG_Techniques</code>里面的：<code class="language-plaintext highlighter-rouge">all_rag_techniques/graph_rag.ipynb</code>。跟微软 GraphRAG、Milvus 版完全是三套思路。这篇拆它的架构、和普通 RAG 的区别、以及为什么它”挂着 GraphRAG 的名，干的还是检索的活”。</p> <h2 id="1-一句话理解">1. 一句话理解</h2> <p>节点 = 文本 chunk，边 = chunk 之间的内容相似度。查询时先向量检索找起点，再顺着图用类似 Dijkstra 的方式扩散，每走一步问 LLM”答案够了吗”，够了就停。 不像前二者主要依赖提取实体和关系</p> <h2 id="2-架构总览">2. 架构总览</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>DocumentProcessor ──分块+embedding──▶ FAISS 向量库
KnowledgeGraph   ──建节点+概念+边──▶ NetworkX 图
QueryEngine      ──检索+Dijkstra遍历──▶ 答案
Visualizer       ──画图+高亮路径──▶ 图
</code></pre></div></div> <h2 id="3-建图一次性">3. 建图（一次性）</h2> <h3 id="31-节点--chunk不是实体">3.1 节点 = chunk，不是实体</h3> <p>每个文本 chunk 是一个节点。概念只是节点属性，不单独建实体节点。所以这张图表达的不是”世界知识”，而是”哪些段落之间有关联”。</p> <h3 id="32-概念抽取双通道">3.2 概念抽取，双通道</h3> <ul> <li>spaCy NER：只留 <code class="language-plaintext highlighter-rouge">PERSON / ORG / GPE / WORK_OF_ART</code> 四类</li> <li>LLM 结构化输出：抽通用概念（<code class="language-plaintext highlighter-rouge">Concepts</code> Pydantic 模型）</li> <li>合并后 lemmatize（词形还原），用于共享概念匹配</li> </ul> <p>每块要调一次 LLM。30 块 = 30 次调用，这是建图成本之一。</p> <h3 id="33-on-两两建边">3.3 O(n²) 两两建边</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">node1</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_nodes</span><span class="p">):</span>
    <span class="k">for</span> <span class="n">node2</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">node1</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="n">num_nodes</span><span class="p">):</span>
        <span class="n">sim</span> <span class="o">=</span> <span class="n">similarity_matrix</span><span class="p">[</span><span class="n">node1</span><span class="p">][</span><span class="n">node2</span><span class="p">]</span>
        <span class="k">if</span> <span class="n">sim</span> <span class="o">&gt;</span> <span class="n">edges_threshold</span><span class="p">:</span>   <span class="c1"># 0.8
</span>            <span class="n">shared</span> <span class="o">=</span> <span class="n">concepts</span><span class="p">[</span><span class="n">node1</span><span class="p">]</span> <span class="o">&amp;</span> <span class="n">concepts</span><span class="p">[</span><span class="n">node2</span><span class="p">]</span>
            <span class="n">weight</span> <span class="o">=</span> <span class="mf">0.7</span> <span class="o">*</span> <span class="n">sim</span> <span class="o">+</span> <span class="mf">0.3</span> <span class="o">*</span> <span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">shared</span><span class="p">)</span> <span class="o">/</span> <span class="nf">min</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">c1</span><span class="p">),</span> <span class="nf">len</span><span class="p">(</span><span class="n">c2</span><span class="p">)))</span>
            <span class="n">graph</span><span class="p">.</span><span class="nf">add_edge</span><span class="p">(</span><span class="n">node1</span><span class="p">,</span> <span class="n">node2</span><span class="p">,</span> <span class="n">weight</span><span class="o">=</span><span class="n">weight</span><span class="p">,</span> <span class="p">...)</span>
</code></pre></div></div> <p>边权 = 相似度 + 共享概念比例，两个信号加权：</p> \[w = \alpha \cdot sim + \beta \cdot \frac{|shared|}{\min(|c_1|, |c_2|)}\] <p>30 节点 = 435 对，很快。但 O(n²) 是硬伤——一万块就是五千万对，直接崩。</p> <blockquote> <p><strong>Insight</strong>：这里的”图”本质是<strong>检索的扩散机制</strong>，不是实体知识库。向量检索只给你直接相似的 chunk；图遍历让你从起点顺着强边跳到”不直接像但强关联”的 chunk——解决答案散落在多块、单块相似度都不够的问题。</p> </blockquote> <h2 id="4-查询--向量找入口图做扩散">4. 查询 = 向量找入口，图做扩散</h2> <h3 id="41-两段式各用各的数据">4.1 两段式，各用各的数据</h3> <table> <thead> <tr> <th>相似度</th> <th>谁对谁</th> <th>何时算</th> <th>用在哪</th> </tr> </thead> <tbody> <tr> <td>边权</td> <td>chunk ↔ chunk</td> <td>建图时，一次</td> <td>遍历时读取</td> </tr> <tr> <td>起点分数</td> <td>query ↔ chunk</td> <td>查询时，一次</td> <td>找起点 top-5</td> </tr> </tbody> </table> <h3 id="42-流程">4.2 流程</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>query embedding → 点积全库 → top-5 chunk 入最小堆（priority = 1/sim）

循环：
  堆.pop() → 取累计距离最小的节点
  content 加入 context
  LLM judge：context 能完整回答 query 吗？
     能 → 输出答案，结束
     不能 → 对每个邻居：
            new_dist = dist + 1/edge_weight   ← 读预存边权，不算向量
            邻居入堆
堆空还没完整答案 → 全部 context 硬生成兜底
</code></pre></div></div> <h3 id="43-为什么是-dijkstra不是单调栈">4.3 为什么是 Dijkstra，不是单调栈</h3> <ul> <li>用 <code class="language-plaintext highlighter-rouge">heapq</code> <strong>最小堆</strong>：堆顶永远是累计距离最小（优先级最高）的节点，push/pop 都 O(log n)。Dijkstra 需要反复”取当前最优 + 更新邻居”，堆正好支持。</li> <li><strong>单调栈</strong>是另一个数据结构（保持栈内单调，用于”找下一个更大/更小元素”），这里无关。</li> </ul> <p><code class="language-plaintext highlighter-rouge">1/edge_weight</code>：边越粗，增量越小，累计距离越小，越早被弹出。<strong>遍历完全不碰 embedding</strong>，只读建图时存好的数字 + 堆操作。</p> <h2 id="5-和普通-rag-的唯一实质区别">5. 和普通 RAG 的唯一实质区别</h2> <p>普通 RAG：检索 top-k → 拼 context → 一次生成，结束。中间没有决策。</p> <p>这个版本：top-k 只是<strong>起点</strong>，真正的检索发生在图上——每跳扩展 + 每跳一次 LLM 判断”够不够”。LLM 不只是最后生成答案，遍历中每一步都在当”答案完整性裁判”。</p> <p>代价也在这：每跳烧一次 LLM 调用，token 和美元成本高；judge 误判”够”→ 答案残缺，误判”不够”→ 白白多爬。</p> <h2 id="6-和微软-graphrag--milvus-版对比">6. 和微软 GraphRAG / Milvus 版对比</h2> <table> <thead> <tr> <th>维度</th> <th>本 notebook</th> <th>微软 GraphRAG</th> <th>Milvus 版</th> </tr> </thead> <tbody> <tr> <td>节点</td> <td>文本 chunk</td> <td>实体</td> <td>实体 / 关系（向量库）</td> </tr> <tr> <td>边</td> <td>embedding 相似度 + 概念重叠</td> <td>实体关系</td> <td>邻接矩阵</td> </tr> <tr> <td>社区 / 摘要</td> <td>无</td> <td>有（社区检测 + 摘要）</td> <td>无</td> </tr> <tr> <td>查询</td> <td>向量起点 + Dijkstra 遍历</td> <td>社区定位 + 全局搜索</td> <td>双路召回 + 矩阵多跳</td> </tr> <tr> <td>多跳成本</td> <td>每跳 1 次 LLM judge</td> <td>便宜（预建摘要）</td> <td>矩阵乘法现算</td> </tr> </tbody> </table> <p>一句话：微软版是<strong>真图谱</strong>（图里存可查询的知识），这个版本是<strong>伪图谱</strong>（图里只存相似度缓存）。</p> <h2 id="7-优势与争议">7. 优势与争议</h2> <h3 id="优势">优势</h3> <ol> <li><strong>比纯向量捞得全</strong>：图遍历让起点顺着强边跳出相似度天花板。</li> <li><strong>LLM judge 让遍历有目标、能早停</strong>：每跳判断”够不够”，不盲目爬全网。</li> <li><strong>可解释、可展示</strong>：遍历路径可画出，能看到答案从哪几块、按什么顺序拼出来。</li> <li><strong>实现简单</strong>：networkx + 堆 + 两次 LLM 调用，几十行。</li> </ol> <h3 id="争议">争议</h3> <ol> <li><strong>名不副实</strong>：节点是 chunk 不是实体，边是”像不像”不是关系，图不编码世界知识。</li> <li><strong>O(n²) 建边</strong>：规模天花板低，文档一大就崩。</li> <li><strong>查询烧 token</strong>：每跳一次 judge，且 judge 本身会误判。</li> <li><strong>起点强依赖向量检索</strong>：top-5 起点全错，图再爬也爬不回对的——图只能扩围，不能纠错。</li> <li><strong>边权建立在两个噪声信号上</strong>：概念抽错 → 共享概念错 → 边权错 → 遍历歪。</li> </ol> <h2 id="8-关于黑盒">8. 关于”黑盒”</h2> <ul> <li>普通 RAG：top-k 依据是”相似度最高”，数字层面完全可审计。但相似度高的都对的世界里它够用；一旦正确答案相似度不高（答案散落需拼合），数字依据就解释不了为什么该选它。</li> <li>本版本：能展示”走了哪些节点、每跳 judge 判了什么”——<strong>过程透明</strong>。</li> <li>微软版：能解释”为什么捞这段——因为它属于’碳排放’社区”——<strong>语义透明</strong>。</li> </ul> <p>三档透明度递进：<code class="language-plaintext highlighter-rouge">数字依据 → 过程透明 → 语义透明</code>。</p> <h2 id="9-设计评价为什么说它设计得一般">9. 设计评价：为什么说它”设计得一般”</h2> <ul> <li>图是死的：边权建图时固化，文档变更全图重建，无增量。</li> <li>O(n²) 建边：好设计该用近似最近邻 / 概念倒排压缩候选。</li> <li>LLM judge 是贵而不稳的单点：正确性押在一个频繁调用、又可能误判的组件上。</li> <li>起点错则全错，无纠错机制。</li> <li>两个检索没有协同：向量 top-5 和遍历是两条独立线路，没有反馈环。</li> <li>图的语义信息量几乎为零，只是”预存相似度”。</li> </ul> <p><strong>核心缺陷：没想清楚图到底要贡献什么。</strong> 好设计先回答”图提供向量检索给不了的东西是什么”，再谈实现。这里图的贡献（预存相似度 + 跳转），向量检索用更便宜的方式也能近似做到。图没有提供不可替代的语义增量，所以它只是<strong>加了张缓存表的 RAG</strong>。</p> <h2 id="总结">总结</h2> <ul> <li>本质：向量找入口 + 图做扩散 + LLM judge 定停止，是”向量检索”到”真图谱”之间最便宜的升级档。</li> <li>适用：中小规模文档、答案依赖多段落拼合、需要展示检索过程的场景。</li> <li>局限：规模、语义、成本三头都压不过微软版；作为教学 demo 及格，作为生产 GraphRAG 不合格。</li> </ul>]]></content><author><name></name></author><category term="RAG"/><category term="rag"/><category term="graphrag"/><category term="dijkstra"/><category term="traversal"/><category term="knowledge-graph"/><summary type="html"><![CDATA[GraphRAG（Chunk 级遍历版）—— 带图的检索，还是假的图谱？]]></summary></entry></feed>